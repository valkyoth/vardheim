# Vardheim Release Plan To 1.0

Status: complete scope plan

This plan assigns every currently known Vardheim requirement to a version
before `1.0.0`. Nothing listed here is an unversioned deferral. If new required
work is discovered, add or split a version before implementation crosses the
affected boundary.

## Mandatory Exit Contract For Every Version

Every row below is a complete release boundary. A version is not implemented
until all of its public functions, error paths, state transitions, parsers,
feature combinations, and platform branches have unit and adversarial tests.
Where relevant it also needs vectors, property tests, fuzz targets, integration
tests, crash/fault injection, and conformance evidence.

For **every** `v0.N.0`, patch, RC, and `v1.0.0`:

1. update RFC/source evidence, changelog, release notes, compatibility, and
   known limitations;
2. run `scripts/checks.sh`, the Rust `1.90.0` through `1.97.0` matrix,
   `cargo deny check`, `cargo audit`, package checks, and SBOM generation;
3. after `v0.4.4`, replay every applicable historical evidence set and review
   the semantic difference report; after `v0.119.0`, also compare against the
   latest released public compatibility baseline; after `v0.37.4`, every new
   adapter must pass its applicable shared conformance suite; after `v0.4.7`,
   every changed feature graph must pass the declared minimal, additive,
   intentional multi-provider, maximal, MSRV, and current-stable profiles;
   after `v0.4.24`, every changed nominal `no_alloc` path must rerun allocator-
   free/stack/scratch/executor-contract evidence; after `v0.6.6`, every changed
   external-effect adapter must rerun observation/evidence-promotion and
   orthogonal-outcome conformance; after `v0.4.26`, every cross-process adapter
   must pass its purpose-specific protocol/replay/dispatch/recovery suite; after
   `v0.10.23`, every changed security-sensitive identity path must rerun fork,
   clone, restore, collision, concurrency, and durable-ordering evidence;
4. meet milestone-specific proof in the table;
5. freeze the implementation candidate and print:
   `vX.Y.Z implementation stop reached. Run pentest for this reviewed implementation commit.`;
6. fix all findings and rerun all evidence;
7. require a permanent `Status: PASS` report bound to the reviewed
   implementation commit before tagging; after `v0.3.3`, any later tagged
   commit may differ only by the mechanically allowlisted release-evidence
   finalization paths, and any executable, manifest, workflow, dependency, or
   tooling change requires a new retest.

Passing one version never authorizes implementation to skip the next version's
stop. A security fix receives its own patch version and pentest.

## Release Principles

- Rust stable `1.97.0` is pinned; Rust `1.90.0` remains the MSRV through 1.0.
- Core and challenge-calculation crates remain `no_std`; `alloc` and `std` are
  explicit local features only where their behavior is unavoidable.
- Backend features make implementations available but never select one.
- Protocol behavior is first-party and strictly follows published RFCs and
  verified errata. Third-party crates enter through reviewed narrow boundaries.
- No source file may exceed 500 physical lines.
- No certificate is production-safe until verified and transactionally
  activated; receipt from a CA is not trust evidence by itself.
- Experimental drafts use exact revisions, remain disabled by default, and do
  not count toward stable RFC conformance.

The owner-defined completion contract deliberately keeps every assigned stable,
experimental, provider, platform, and draft boundary before `1.0.0`, with a
pentest stop for every version. Recommendations to collapse these rows into a
few public trains, move moving drafts past `1.0.0`, or give adapters a weaker
MSRV are therefore not adopted. `v0.3.9` makes the large plan a checked work
graph with stable internal IDs, dependencies, ownership, and critical-path
views; it does not weaken or replace any version, proof, or pentest boundary.

## Tactical Milestone Size Rule

One version may introduce only one independently reviewable boundary: one
parser layer, one protocol resource or transition family, one persistence
primitive, one backend/provider, one deployment target, or one assurance
technique. A version must be split before implementation when it would require
more than one new external dependency family, more than one independently
deployable adapter, or more than one unrelated state-machine slice. Integration
releases may connect already completed boundaries but may not introduce their
missing primitives. Patch-numbered roadmap releases below are planned feature
releases, not afterthoughts; each receives the same pentest stop as a minor.

## Phase 0 - Repository, Evidence, And Specification

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.1.0` | Repository foundation: five dependency-free `no_std` crates, facade re-exports, dual license, Rust matrix, CI, CodeQL Default policy, deny/audit/SBOM policy, docs, file-size rule, release notes, and pentest template. | Clean full checks on Rust 1.97.0 and compatibility checks on every stable line from 1.90.0. |
| `v0.2.0` | Initial release discipline: release metadata validator, reviewed-commit pentest field, tag guard, publish order, independent crate versions, package checks, SBOM presence, and negative tests of every refusal path. | Release-readiness test suite proves missing, malformed, stale, nonexistent, non-ancestor, and failed pentest evidence cannot pass. |
| `v0.3.0` | Normative source baseline: complete section-level published RFC inventory, local RFC fetch/check script, source hashes, and verified errata log. | Machine-check every claimed RFC and accepted/rejected erratum against the pinned local snapshot. |
| `v0.3.1` | Registry baseline: pinned IANA ACME registries, current HTTP replacement mapping, supporting-standard inventory, and external profile revisions. | Every known field, error, resource, identifier, validation method, and extension value has a generated bounded registry fixture. |
| `v0.3.2` | Complete offline errata evidence: byte-pinned official records retain exact original/corrected text, notes, status history, checked date, reviewed applicability, affected requirement IDs, and explicit treatment of Verified, Reported, Held, and Rejected records. | Offline generation reproduces every stored correction and hash; tampered, omitted, duplicated, truncated, reclassified, or requirement-unlinked errata fail, and RFC 8555 challenge/status/JWK leading-zero corrections have reviewed fixtures. |
| `v0.3.3` | Release evidence binding: distinguish the reviewed implementation commit from the evidence-finalization commit, strictly allowlist the intervening paths and content classes, split pre-tag and tag-verification modes, and bind tag, package, SBOM, and pentest metadata without changing the established retest workflow. | Negative tests prove any source, Cargo metadata, dependency, workflow, executable script, generated protocol evidence, or non-allowlisted documentation change after review blocks release; tag verification is replayable after the tag exists. |
| `v0.3.4` | Registry-reference closure: classify every RFC, Internet-Draft, IANA, external-profile, and other normative registry reference as resolved, superseded, experimental, recognized-only, or unsupported, and byte-pin every source required for semantic implementation at an exact revision. The DER/PKIX/transport/DNS set explicitly includes the applicable exact ITU-T X.690 edition, SEC 1 v2.0, FIPS 186-5 plus reviewed errata state, NIST SP 800-56A Rev. 3 and its announced-update status, NIST SP 800-186, plus RFC 2136, 2181, 2308, 3225, 4518, 4519, 5011, 5155, 5280, 5452, 5480, 5652, 6672, 6840, 6891, 7633, 7766, 7873, 8017, 8027, 8032, 8410, 8470, 8906, 8914, 8933, 8945, 9000, 9001, 9002, 9018, 9113, 9114, 9162, 9204, 9276, 9364, 9471, 9499, 9904, and pinned IANA DNS/EDNS/TSIG/DNSSEC registries; RFC 2845, 3454 where referenced by RFC 5280/4518, 4635, 6962, 8499, and 8624 remain superseded historical or interoperability input. | Every reference has one classification, provenance, immutable identity/hash, replacement chain, implementation tier, and requirement link; unresolved or mutable unpinned semantic sources block conformance work, DER cannot rely on an unidentified X.690 edition, public-key validation cannot silently mix RFC, SEC, or NIST revisions or ignore published errata/update status, DN comparison cannot omit RFC 4518/4519 and the exact RFC 3454 mapping referenced by RFC 5280, HTTP/2 or HTTP/3 cannot be enabled without their complete pinned transport closure, DNAME/referral/glue discovery cannot cite obsolete DNS terminology, CT v2 cannot cite obsoleted RFC 6962 behavior as current, DNS Cookies cannot ignore RFC 9018's update to RFC 7873, DNSSEC tests cannot claim current algorithm policy from RFC 8624 alone, and TSIG cannot cite obsoleted RFC 2845 behavior as current. |
| `v0.3.5` | Reproducible SPDX SBOM generation with deterministic ordering, stable document identity, dependency/source checksums, features, targets, relationships, release commit binding, and verification of committed and packaged artifacts. | Two clean worktrees generate byte-identical SBOMs; timestamp, UUID, ordering, feature, target, source, checksum, relationship, and release-commit drift are detected. |
| `v0.3.6` | Core cross-target CI baseline for Linux x86-64/AArch64 and one big-endian architecture, Windows GNU/MSVC, macOS/iOS, Android, BSD, `wasm32-unknown-unknown`, bare-metal Thumb and RISC-V, with an explicit compile-only versus native-runtime evidence distinction. | Every current `no_std` crate and feature profile compiles on its declared matrix; unsupported cells fail during configuration, and missing target installation, endian assumptions, host-only dependencies, or silently skipped jobs fail CI. |
| `v0.3.7` | Conformance system: stable machine-readable requirement IDs with source hash and section/paragraph anchor, original/corrected text hashes, normative strength, actor, applicability, pre/postconditions, errata links, implementation symbol, evidence IDs, capability schema, fixture provenance, and generated reports separating recognized, implemented, enabled, interoperability-tested, formally checked, production-qualified, standard-unsupported, client-unsupported, provider-unsupported, and operator-disabled states. | Every normative client requirement maps to implementation/evidence ownership and every implementation/control maps back; stale hashes, unreflected errata, unclassified IANA values, orphan requirements/evidence, claims lacking positive/malformed/boundary/negative evidence, conflicting applicability, and unassigned implementation work fail validation. |
| `v0.3.8` | Upstream drift monitor for RFC status/full errata content, IANA ACME registries and resolved references, external profiles, and pinned draft revisions, producing a machine-readable difference report without mutating accepted baselines. | Offline fixtures prove additions, removals, text/status/reference/revision changes, errata correction changes, and draft replacement are detected; live checks remain advisory until human-reviewed and assigned to a version. |
| `v0.3.9` | Machine-readable roadmap and requirement-work graph with stable work IDs, public-version mapping, prerequisite edges, accountable owner, affected crate/boundary, typed requirement source, evidence obligations, completion state, and pentest boundary. Requirement sources explicitly include RFC/standard text, errata, registries, threat-model controls, architecture decisions, compatibility contracts, platform invariants, and reviewed security findings; non-normative hardening work never receives an artificial RFC citation. Generate the human-readable release tables, completeness summaries, release waves, and critical-path views from the reviewed graph while retaining every public version in this plan. | Schema, duplicate/orphan, unknown/invalid source, missing evidence/version, cycle, forward-order, scope-without-work, work-without-a-reviewed-requirement source, generated-drift, and hand-edited-output negatives fail closed; every row in this plan and every applicable requirement has exactly one graph identity and no tool can merge or skip pentest stops. |
| `v0.4.0` | Threat model: assets, adversaries, trust boundaries, abuse cases, security invariants, and non-guarantees. | Independent review finds no unowned trust boundary or secret lifecycle. |
| `v0.4.1` | Formal-method and trace-interchange contract for independently authored TLA+/Quint models: stable state/action IDs, bounded parameters, safety/liveness claim records, counterexample fixtures, deterministic trace serialization, and explicit model-transition-to-Rust-transition ownership. It defines the method but does not pretend one monolithic model proves every lifecycle. | Two deliberately different toy models and a hostile trace reader prove tool/version provenance, stable replay, invalid-transition rejection, counterexample retention, state/action coverage reporting, and failure when a Rust transition or security invariant lacks a model owner. |
| `v0.4.2` | Test and evidence architecture: vectors, properties, fuzzing, fault injection, integration tiers, coverage policy, and completeness register. | Every invariant has at least one assigned evidence technique and release owner. |
| `v0.4.3` | Immutable regression-evidence manifest for vectors, corpora, model traces, interoperability captures, expected semantic outcomes, provenance, and content hashes. | Tamper, omission, duplicate-ID, provenance, and accidental-baseline-replacement negatives pass; evidence remains addressable across releases. |
| `v0.4.4` | Longitudinal replay runner that executes every compatible historical evidence set against current code and emits a semantic difference report. | Seeded old/current fixture simulations detect missing behavior and changed results; an expected change cannot pass without a reviewed replacement vector, reason, requirement link, and release note. |
| `v0.4.5` | Backend-boundary contract: pure command/state/policy/event reducer, typed effects and bounded outcomes, positive evidence/receipt types, purpose-specific signer capabilities, fail-closed transport/storage/clock/entropy classifications, static-dispatch `no_alloc` APIs, and optional object-safe `alloc` executors selected only by explicit construction. | Architecture tests and compile-fail fixtures prove core cannot depend on runtimes/backends, features cannot auto-select implementations, ambiguous outcomes cannot fabricate success, storage cannot load-or-default, and DER versus JOSE signature formats or signing purposes cannot be confused. |
| `v0.4.6` | Crate and feature topology contract: default core-only `no_std` without allocation, additive `alloc` and `std` tiers, lightweight challenge crates, an approved independently auditable `vardheim-pkix` boundary, and separate outward adapter packages for heavy runtimes, crypto, DNS SDKs, databases, and TLS stacks rather than ecosystem features inside portable crates. | Dependency-direction and cycle tests prove protocol/core crates cannot depend on adapters; `--all-features` on a portable crate cannot pull unrelated ecosystems, and every public crate has a responsibility statement, unsafe/dependency policy, publication decision, and narrow export review. |
| `v0.4.7` | Build and API ergonomics gate: feature-power-set testing with `cargo hack` or equivalent, minimal/maximal and intentional multi-provider profiles, MSRV/current-stable coverage, public API surface snapshots plus semver compatibility comparison against the latest published crate from the first public API onward, compile-time and binary-size budgets, generated-source/corpus policy, and documentation examples for each supported integration profile. | Negative fixtures detect accidental default features, mutually enabled backend selection, dependency cycles, unexplained public API/semver drift, missing profile examples, and budget regressions; FIPS profiles prove non-FIPS implementations are unavailable rather than merely unselected, and an intentional pre-1.0 break requires migration notes and a retained old/new API report. |
| `v0.4.8` | Unpublished RustCrypto signer/verifier feasibility spike against the candidate `v0.4.5` effect and evidence boundary, covering message-versus-digest input, exact encoding, returned-output verification, borrowed buffers, provider identity, cancellation, and definite-versus-ambiguous dispatch. The spike is private evidence, not a public provider or crates.io support claim. | A pinned real API spike and intentionally faulty implementation exercise every candidate operation and failure class; the boundary is revised before freeze if safe Rust, bounded memory, exact-byte verification, or honest ambiguity cannot be expressed without provider leakage into core. |
| `v0.4.9` | Unpublished ring signer/verifier feasibility spike against the same candidate boundary, independently testing its algorithm, key-representation, message-input, verification, and error constraints rather than assuming the RustCrypto result generalizes. | Pinned ring API tests cover supported/unsupported algorithm cells, malformed and wrong-key output, immutable identity binding, cancellation boundaries, and prove missing capabilities remain typed unsupported with no cross-provider fallback. |
| `v0.4.10` | Unpublished rustls resolver and ACME transport feasibility spike, including explicit TLS roots/SNI/ALPN, bounded response envelopes, connection/session/resolver partitioning, early-data refusal, redirect/origin policy, cancellation, and definitely-not-dispatched versus may-have-dispatched evidence. | Loopback hostile-server tests and compile/API probes prove the candidate transport contract can represent real rustls behavior without exposing stream ownership to core, inventing delivery certainty, sharing trust domains, or permitting implicit retry. |
| `v0.4.11` | Unpublished executor-mode feasibility spike for blocking, async, and caller-buffer embedded clients over one inert effect protocol, with static no-allocation dispatch and an optional object-safe allocation tier. No runtime becomes a core dependency or implicit default. | Equivalent scripted traces, cancellation at every poll/effect boundary, executor drop, scratch exhaustion, reentrancy, and feature-graph tests prove all three modes preserve identical reducer decisions and ambiguity while the no-allocation graph remains runtime-free. |
| `v0.4.12` | Unpublished memory-store and SQL-like transaction feasibility spike for revisions, durable intent, outbox effects, observations, leases, fencing, and crash recovery. The SQL model uses a private representative schema/API and makes no production database support claim. | Deterministic transaction and fault schedules cover commit loss, duplicate execution, isolation conflicts, stale workers, ambiguous commit/result observation, and restart; any semantic mismatch refines the store/effect contract before public persistence APIs freeze. |
| `v0.4.13` | Machine-readable crate policy tiers: portable crates require `no_std`, safe Rust, dependency and allocation limits; native/FFI adapter crates may use `std` and only allow unsafe inside explicitly inventoried minimal modules with invariants, owner, platform evidence, and audit scope. Protocol and policy semantics remain safe Rust in every tier. | Validator fixtures reject an unclassified crate, portable `std`/unsafe use, unsafe outside an allowlisted module, missing rationale/owner/evidence, widened unsafe surface, adapter-to-core dependency inversion, and a native crate falsely included in portable package or target claims. |
| `v0.4.14` | Independent replay-nonce linear-authority formal model: acquisition, scope, queue bounds, reservation, destructive consumption, response harvesting, `badNonce`, cancellation, and restart without restoration. | Exhaustive bounded exploration and permanent counterexample traces prove no nonce is cloned, restored, cross-scoped, reused after any outcome, or coupled to HTTP success before response-nonce harvesting. |
| `v0.4.15` | Independent request-admission/sign/commit/dispatch formal model binding exact bytes, signer identity, admission, verified output, durable outbox intent, and dispatch knowledge. | Model checking and Rust-trace fixtures prove consumed authority cannot return, unverified output cannot commit, pre-dispatch invalidation prevents the effect, post-dispatch uncertainty cannot become definitely unsent, and retry creates wholly fresh authority. |
| `v0.4.16` | Independent challenge-presentation ownership formal model for prepare, externally present, self-check, trigger, cleanup, expiry, replacement, and competing workers across method revisions. | Counterexample replay proves only the owning generation/revision can trigger or clean up, stale workers cannot erase a replacement, and no receipt or state converts authority between DNS, HTTP, TLS, or later method revisions. |
| `v0.4.17` | Independent order/authorization aggregation formal model for identifier coverage, challenge status, invalidity, polling, finalization eligibility, cancellation, and terminal outcomes. | Bounded exhaustive traces prove a skipped/contradictory child state cannot fabricate readiness, terminal failure cannot regress to pending, and finalization requires the exact complete authorization set. |
| `v0.4.18` | Independent transactional-outbox ambiguity formal model for persisted intent, effect execution, result observation, reconciliation, definitely-unsent proof, and may-have-dispatched outcomes. | Crash at every transition plus delayed, lost, duplicated, and reordered observations proves blind resend and false success are unreachable and only positive evidence narrows dispatch knowledge. |
| `v0.4.19` | Independent lease, fencing, and once-only application formal model covering acquisition, expiry, renewal, theft, stale workers, revisions, idempotent observation, and obligation preservation. | Exhaustive schedules prove an old fence cannot commit, apply a result twice, clear cleanup/reconciliation obligations, or activate a superseded generation. |
| `v0.4.20` | Independent deployment formal model for staged generations, certificate/key/staple binding, activation, health verification, rollback, overlap, target acknowledgement, and retirement. | Crash and concurrency traces prove partial artifacts never activate, failed health restores the last good generation, stale deployment workers are fenced, and retirement/destruction is never inferred from absence. |
| `v0.4.21` | Independent renewal formal model for ARI/fallback scheduling, wall/monotonic clocks, jitter, attempt identity, issuer policy, key mode, failure backoff, restart, and emergency replacement. | Model checking proves deadlines do not cross clock domains, stale ARI cannot postpone safety limits, restart cannot duplicate an attempt or lose a due renewal, and issuer/key-mode changes require fresh authority. |
| `v0.4.22` | Semantic module-invariant and complexity gate supplementing the 500-line limit: every production module declares owned invariants and authority constructors; public API snapshots, dependency direction, stack/depth estimates for bounded paths, reducer/state complexity, compile/binary budgets, and focused compile-fail tests are reviewed together. | Checks reject arbitrary numbered splits, duplicated invariant ownership, public construction of sealed evidence, unbounded recursion/stack growth, unexplained reducer-state growth, architecture-direction drift, and file/API/build budget regressions. |
| `v0.4.23` | Security mutation-testing baseline for the first trusted boundaries, with independently authored mutations targeting removed verification, nonce restoration, duplicate-key acceptance, certificate-policy bypass, may-have-dispatched retry, cleanup weakening, trust-domain fallback, and forged evidence construction. | Each seeded security mutation is killed by a named non-generated test; survivors block release and become permanent requirements, while mutation tooling cannot rewrite or bless the expected corpus. |
| `v0.4.24` | Executable no-allocation and executor-contract proof: portable nominal `no_alloc` paths link on targets without a global allocator, run under a panic-on-allocation test allocator where a host harness is required, publish bounded peak-stack evidence keyed to exact target, compiler, optimization profile, feature set, and linker configuration, and specify `Send`/`Sync`, pinning, callback reentrancy, effect ownership, and caller-scratch lifetime/aliasing guarantees for static and allocation-backed executors. | Link-only freestanding targets, allocation traps, configuration-bound repeatable peak-stack thresholds, exact scratch exhaustion, nested callback/reentrancy, cancellation/drop, pin-movement compile-fail, lifetime/aliasing compile-fail, and thread-safety assertions prove the declared path neither allocates nor retains caller buffers; changing any measurement dimension invalidates the stack result, and every unsupported concurrency property is explicit. |
| `v0.4.25` | Unpublished safe `no_std` DER/PKIX feasibility and build-versus-adopt decision before public DER APIs: compare candidate ASN.1/X.509 components, their unsafe/dependency/MSRV/allocation surface, and at least one independent parser oracle against the proposed bounded caller-workspace architecture and exact X.690/RFC 5280 rules. No spike dependency or oracle becomes production trust by implication. | Pinned API/build probes plus canonical/malformed/differential corpora cover lengths, INTEGER/OID/BIT STRING/SET ordering, certificate envelopes, algorithm parameters, extensions, time, names, and workspace exhaustion; the reviewed decision records adopted/rejected components, unresolved semantic gaps, replacement cost, and forces boundary revision when safe bounded `no_std` behavior cannot be demonstrated. |
| `v0.4.26` | Generic executor authority confinement: blocking, async, polling, embedded, static, and allocation-backed executors are local-process drivers of transient capabilities and inert effects; the generic contract has no remote executor, serializable live authority, or generic `EffectTicket`. Remote signer, DNS, KMS, secret-manager, deployment, and agent integrations use later purpose-specific authenticated protocols whose endpoints return observations only and cannot mint local qualified evidence. | API/architecture, serde-negative, feature-graph, and fake-executor tests prove live admission/evidence cannot cross a process boundary, no generic effect schema is accidentally exposed, every local executor yields identical reducer semantics, and each later remote adapter must declare its own canonical bounded encoding, version/capability negotiation, endpoint/tenant/policy/effect/request/generation/deadline/recovery-epoch binding, replay/reordering/duplication/downgrade/session protection, and honest dispatch acknowledgement. |

## Phase 1 - Bounded Types, Encoding, And JOSE

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.5.0` | Core resource budgets for bytes, strings, collections, nesting, and cumulative allocation/work accounting. | Boundary, overflow, exhaustion, and exact-limit properties for every constructor and decrement path. |
| `v0.5.1` | Protocol-operation budgets for headers, identifiers, challenges, subproblems, links, redirects, retries, polling, and elapsed time. | Composed budgets cannot reset or bypass a parent ceiling across nested operations. |
| `v0.5.2` | Initial Kani proof harnesses for resource-budget arithmetic, bounded-length conversions, capacity-policy invariants, and cumulative work/allocation monotonicity, establishing counterexample-to-regression handling from the first security-critical primitives. | Harnesses pass at published bounds with assumptions recorded; overflow, truncation, sign-loss, budget reset, and exact-limit counterexamples become permanent Rust regressions. |
| `v0.6.0` | Strict URI, origin, directory, and resource URL newtypes with normalization and cross-type isolation. | Constructors reject user-info, fragments, unsafe schemes, ambiguity, and cross-domain substitutions. |
| `v0.6.1` | Account, order, authorization, challenge, certificate, request, effect, revision, tenant, key-handle, and private-constructor positive-evidence domains such as verified signature, durable commit, bounded transport response, and owned presentation receipt. Validation, signer-binding, request-admission, verification, and effect-authority capabilities are explicitly transient, non-serializable, non-deserializable, and distinct from persistable lifecycle, obligation, and audit records. | Cross-resource/evidence misuse is prevented by types and compile-fail tests; backend failure values cannot construct a valid transition capability, and serde/API tests prove no authority capability can enter snapshots or be reconstructed or upgraded from lifecycle state, obligations, or audit data. |
| `v0.6.2` | Identifier domains: DNS A-label normalization, IDNA 2008 policy, wildcard representation, canonical IP bytes, forward-compatible known/unknown bounded identifier tokens, email/TNAuthList/bundleEID/NfInstanceId extension shells, and sorted identifier sets. | Unicode, public-suffix-policy boundary, wildcard, equivalence, duplicate, canonicalization, known-recognized-but-disabled, and unknown-token corpora pass. |
| `v0.6.3` | Secret-bearing account tokens, key authorizations, EAB credentials, wall/monotonic time domains, deadlines, correlation IDs, redacted handles, fixed-size cryptographic values, and safe overwrite-on-drop where the storage contract permits it. Replay nonces are linear authority rather than confidential secret and are assigned separately to `v0.6.5`; optional stronger native memory assurance is assigned to `v0.6.7`. | Required secrets are non-Copy/non-Clone, always redact, cannot cross clock or purpose domains, and never claim guaranteed erasure, page locking, dump/swap exclusion, guarded allocation, or absence of copies where the platform/profile cannot provide and evidence it. |
| `v0.6.4` | Distinct trust-domain identities and handles for ACME HTTPS roots, issued-certificate trust anchors, CT log keys, DNSSEC trust anchors, and signer/provider trust, with tenant/profile/purpose separation. | Compile-fail and serialization tests prevent cross-domain substitution, cross-tenant reuse, implicit conversion, ambiguous display, or treating one trust snapshot hash as another domain. |
| `v0.6.5` | Linear replay-authority taxonomy separating `Secret`, `LinearToken`, `FreshNonce`, candidate `ObservedNonce`, and persistable non-authoritative `ConsumedNonceId`. A `FreshNonce` is locally directory/account/tenant/operation scoped, fixed-capacity or caller-backed, destructively moved into one request, and never cloned/restored. Transport parsing may yield only an `ObservedNonce`; the authenticated protocol admission rules that can promote it are assigned to `v0.14.3`. | Compile-fail, capacity, cancellation, `badNonce`, malformed-response, lost-response, restart, scope-substitution, queue-eviction, and redaction tests prove nonce authority is neither treated as a secret nor serializable/restorable; every dispatch outcome consumes it, an observation alone grants no authority, and local scope partitioning is documented as Vardheim isolation policy rather than a server guarantee. |
| `v0.6.6` | Sealed observation-to-evidence promotion pipeline for external effects: adapter observation -> structural/context validation -> policy/assurance check -> sealed evidence constructor -> reducer event. Distinct types such as `StoreAssertedCommit`, `QualifiedDurableCommit`, `PresentationObserved`, and `OwnedPresentation` bind adapter implementation/session, assurance profile, request/effect identity and digest, resource generation/ownership, and validation context. Rust privacy prevents accidental fabrication but never claims an in-process adapter independently proves physical durability or external publication. | Compile-fail and hostile-adapter tests reject direct evidence/event construction, stale sessions, cross-request/generation/backend/context substitution, assertion promoted without the configured qualification profile, missing durability/publication semantics, serialized authority, and structurally or policy-inconsistent overclaim. Each evidence type names the facade/policy component trusted to promote it; semantically false but perfectly formed assertions remain an explicit residual trusted-computing-base risk that tests cannot detect generically. |
| `v0.6.7` | Optional native `SecretMemory` assurance boundary outside portable core for page locking, dump exclusion, guarded allocations, swap policy, copy/provenance tracking, redacted handles, explicit move/borrow/export rules, and best-effort zeroization. Profiles publish exact operating-system primitives and limitations; unavailable privileges or semantics return typed unsupported and never imply universal erasure, absence of copies, or protection from a compromised kernel/hypervisor. | Native fault/privilege tests cover lock denial/limits, fork/core-dump behavior where controllable, guard-page faults, swap/dump configuration drift, allocation/move/copy/export paths, panic/drop, process crash, unsupported targets, and fallback refusal. Documentation and evidence distinguish guaranteed API behavior, best effort, operator configuration, and claims that cannot be proven. |
| `v0.7.0` | Stable protocol and server-problem errors, all registered problem types, bounded subproblems, identifier association, instance URI, and unknown problem retention. | Every problem mapping and bounded round trip is tested without flattening to strings. |
| `v0.7.1` | Retry/operator advice, error provenance, correlation, source-category conversion, and secret-safe Display/Debug policy. | Every error class has an explicit retry decision; snapshots prove no secret-bearing formatting. |
| `v0.7.2` | Layered stable error envelope separating `ProtocolError`, `SecurityRefusal`, `ResourceExhaustion`, `BackendFailure`, `ReconciliationNeeded`, and `Unsupported`. Bounded redacted provider diagnostics and internal reasons remain observational only; they never encode dispatch knowledge, construct authority, or drive retry from text/vendor codes. | Exhaustive conversion and faulty-adapter tests prove every internal reason maps to one stable outer category without flattening orthogonal state, diagnostics cannot alter transitions, unknown provider codes remain bounded, and no error constructs positive evidence or narrows ambiguity. |
| `v0.7.3` | Orthogonal effect-observation product shared by every backend boundary: support/capability classification is outside an admitted operation; `DispatchKnowledge` independently records not-started, sealed positive `DefinitelyUnsent`, committed-not-dispatched, `MayHaveDispatched`, or positively dispatched; `OperationOutcome<O>` records pending/unknown, succeeded, or rejected; and `ObservationStatus` records available, unavailable, or structurally corrupt. A backend can therefore be unavailable while an effect may have dispatched. | Exhaustive product, impossible-state, conversion, reducer, and faulty-adapter tests prove flat enums cannot erase simultaneous facts; unsupported never implies unsent, rejected never implies dispatch certainty, corrupt/unavailable never rewrites dispatch or outcome, adapter error codes cannot construct `DefinitelyUnsent`, and only sealed positive evidence narrows ambiguity. |
| `v0.8.0` | Borrowed-input bounded UTF-8 JSON tokenizer with caller scratch for escapes, scalar/number grammar, raw-span retention, depth/token/byte/work limits, and a persistent fuzz target introduced with the parser. | Differential lexical corpus and continuous fuzzing never panic, allocate implicitly, or exceed cumulative budgets; every finding becomes an immutable regression seed. |
| `v0.8.1` | Strict object/array decoder with known-field bitsets, duplicate-key rejection, null/missing/empty distinction, exact consumption, and two-phase validate/count then fill support for caller-provided storage. | Differential semantic comparison against independent parsers plus duplicate, recursive, trailing, ambiguous, scratch-exhaustion, and oversized cases fails deterministically; disagreements are resolved against the RFC. |
| `v0.8.2` | Typed borrowed and bounded-owned ACME JSON schema decoding with exact timestamps/numeric ranges, known-field validation, bounded unknown spans, and a small documented capacity-policy abstraction rather than pervasive const parameters. | Known fields cannot be overridden by extensions; borrowed/owned parity, custom-capacity, typed fixture, mutation, and allocation-exhaustion corpora pass. |
| `v0.9.0` | RFC 4648 base64url without padding, exact-length decoded buffers, and canonical rejection. | RFC vectors, exhaustive short-input tests, round trips, noncanonical padding/alphabet rejection, and fuzzing. |
| `v0.10.0` | RFC 7518 algorithm and capability models with explicit policy intersection and no server-selected local algorithm. | Algorithm/key/purpose mismatch and downgrade matrices fail closed. |
| `v0.10.1` | RFC 7517 bounded JWK models and canonical public-key member encoding for required key families, retaining exact fixed-width coordinate and integer semantics from verified errata. | Leading-zero, coordinate, exponent, curve, duplicate/private-member rejection, independent-parser differential fixtures, and RFC-resolved disagreement cases pass. |
| `v0.10.2` | Provider-neutral digest semantics with typed algorithms, fixed-size outputs where defined, bounded variable output, purpose binding, caller-provided buffers, and deterministic fake providers, without admitting a concrete crypto implementation. | SHA-256/thumbprint/key-authorization vectors, wrong length/algorithm/purpose, short-buffer, provider failure, and no-fallback tests pass; consumers cannot request arbitrary unbounded digest output. |
| `v0.10.3` | Provider-neutral signing and verification operation semantics with typed key handles, purpose-specific requests, exact DER/raw output formats, algorithm capabilities, untrusted provider-signature observations, positive `VerifiedSignature` evidence, and deterministic fake providers. The later `v0.10.15` commit boundary decides whether observed output may enter a protocol effect. | Compile-fail and behavior tests prevent account/CSR/revocation/challenge purpose confusion, raw/DER conversion confusion, arbitrary-byte signing, bool verification, backend/algorithm fallback, forged verification evidence, or direct promotion of provider-returned bytes into a protocol signature. |
| `v0.10.4` | Provider-neutral entropy semantics for cryptographic and scheduling purposes, explicit request sizes and provenance, injected deterministic test entropy, failure classification, and no hidden global RNG. | Exhaustion, repetition, short output, partial failure, purpose crossover, scheduling determinism, and fallback tests pass; callers cannot treat unavailable entropy as zeros or reuse previous bytes. |
| `v0.10.5` | Provider-neutral key-generation operation semantics with role, algorithm, exportability, persistence, attestation, origin, stable request/idempotency identities, and public-key observation requirements, reusing the early entropy contract and no concrete backend. Generation returns only typed creation observations for later `v0.10.13` onboarding and never an active key. | Unsupported role/algorithm/exportability/attestation combinations fail before generation; key-handle/public-key mismatch, entropy failure, definite-not-created/created/ambiguous/unavailable classification, partial creation, implicit reuse, and silent software fallback tests pass without promoting a returned handle to active use. |
| `v0.10.6` | RFC 7638 canonical JWK thumbprints and account-key authorization digest inputs built exclusively through the provider-neutral digest contract. | Published vectors and cross-implementation differential evidence pass byte-for-byte; wrong digest capability, length, purpose, JWK member set, and canonicalization fail closed. |
| `v0.10.7` | Narrow legacy-hash compatibility capability for standards-mandated verification-only uses, including OCSP `ResponderID` key hashes, DNSSEC DS validation, and NSEC3; SHA-1 is unavailable to JWS, CSR/certificate signatures, new certificate fingerprints, storage, or generic caller-selected hashing. | Type/API and capability matrices reject every cross-purpose request, generation use, downgrade, implicit fallback, and unapproved algorithm; official OCSP/DNSSEC/NSEC3 vectors prove the exact allowed byte domains. |
| `v0.10.8` | Provider-neutral `DnsUpdateMac` operation semantics with typed TSIG key name, algorithm, secret handle, request/response/chained-message purposes, exact MAC format, truncation policy input, and no EAB/JWS interchange. Provider output is only an observation for the later `v0.10.16` handle-backed MAC authority/evidence boundary. | Compile-fail and fake-provider tests reject EAB/TSIG key or purpose reuse, arbitrary-byte MAC requests, unapproved algorithms, silent truncation, backend fallback, secret formatting, bool-only verification, or direct promotion of provider-returned MAC bytes into TSIG evidence. |
| `v0.10.9` | Provider-neutral key-disposition request, observation, receipt, and reconciliation contract with distinct `Destroyed`, `Disabled`, `ScheduledForDeletion`, `RetainedByPolicy`, `ProviderCannotDestroy`, `NotFound`, `Ambiguous`, and `Unavailable` outcomes bound to tenant, provider, key identity/version, purpose, request ID, policy, time, and native provider evidence. | Type/API and fake-provider tests prove scheduled deletion or disablement cannot become physical destruction, `NotFound` cannot prove prior destruction, ambiguous/unavailable outcomes remain pending, receipts cannot cross tenant/provider/key/version/purpose/policy, and repeated reconciliation is idempotent without fabricated terminal evidence. |
| `v0.10.10` | Provider-neutral public-key validation contract with typed RSA, Weierstrass EC, and EdDSA public material, exact algorithm/parameter/curve identity, purpose and policy inputs, provider/session identity, and private-constructor transient `ValidatedPublicKey` evidence. Validation covers the structural and mathematical checks available from public material plus explicit policy; it does not claim unknowable RSA factorization properties. Unsupported and unavailable validation never fall back. | Type/API, compile-fail, and malicious fake-provider tests reject forged/deserialized/cross-session evidence, algorithm/curve/parameter substitution, material-hash or tenant/provider/purpose/policy crossover, stale provider capability, unavailable/unsupported fallback, and bool-only results; successful evidence expires with its validation session/context and cannot itself authorize a signer handle. |
| `v0.10.11` | Domain-separated signer-binding ceremony producing transient non-serializable `BoundSigner` capability from current `v0.10.10` evidence. `BoundSigner` binds provider/session, tenant, handle, validated public-material hash, algorithm and parameters, policy version, permitted roles, key-health generation, and expiry. A provider-native pairwise-consistency operation may establish the cryptographic handle/key relationship; otherwise a narrowly privileged `bind_signer` operation, explicitly exempt from ordinary consumer admission, signs only an exact fresh entropy-backed canonical `SignerBinding` transcript and cannot accept arbitrary bytes or any JWS/CSR/revocation/TLS-challenge/audit request. Local verification is mandatory and failure or ambiguity yields no binding. | Type/API, compile-fail, deterministic entropy, and malicious fake-provider tests reject arbitrary-byte use of `bind_signer`, protocol-purpose crossover, cached/unrelated signatures, transcript replay/substitution, wrong provider/session/tenant/handle/material/algorithm/policy/role/health/expiry, DER/raw confusion, entropy failure, partial or ambiguous signing, local-verification failure, forged native consistency, serialization/restoration, material-only promotion, and recursive attempts to require ordinary admission for the binding operation. |
| `v0.10.12` | Local request-specific signer-consumer admission contract over `&mut BoundSigner`. Minting requires a valid permitted role and exact request identity covering the canonical signing bytes, algorithm/parameters, output format, protocol role, request correlation, and replay nonce where applicable; it performs no provider signature. Private non-serializable `SignerConsumerAdmission` is single-use, consumed immediately before dispatch to the ordinary signing operation, never restored after success, failure, cancellation, or ambiguity, and bounded per signer/session with unique outstanding request identities. Provider/session/policy/key-health change or `BoundSigner` expiry invalidates the signer and every derived admission. Invalidation observed before dispatch prevents signing; invalidation racing with or following dispatch yields an ambiguous signing outcome unless positive provider evidence proves a narrower result, while admission remains consumed and replacement requires revalidation, a new `BoundSigner`, request identity, and admission. | Type/API, compile-fail, state-machine, Kani, and Loom tests reject minting without `BoundSigner`, wrong/unpermitted roles, request/bytes/algorithm/format/nonce substitution, duplicate identities, capacity overflow, admission reuse or serialization, use after signer invalidation/expiry, restoration on every signing outcome, recursive binding-operation admission, and every ordinary handle-backed signature effect lacking the exact current admission. Race tests cover invalidation immediately before, during, and after dispatch; absence or contradiction of positive provider evidence remains ambiguous and cannot reuse the old request identity. |
| `v0.10.13` | Provider-neutral transactional key-onboarding state model shared by generation, PKCS#8 import, legacy migration, and platform/provider key adoption. A stable creation/import request ID and provider idempotency token bind tenant, role, algorithm/parameters, provider, intended key version, origin, exportability, persistence, attestation, and policy. The durable key record additionally pins the observed immutable provider-native key identity/version and canonical public-key digest; mutable aliases, labels, names, or persistent references are discovery metadata only. Typed provider outcomes are `DefinitelyNotCreated`, `Created`, `Ambiguous`, and `Unavailable`. Persisted data is the product `KeyLifecycleState × KeyObligationSet`: lifecycle states such as `Requested`, `CreationUnknown`, `CreatedQuarantined`, `Active`, `RetainedQuarantined`, and `Retired` are inventory/policy facts only, while orthogonal obligations include reconciliation, validation, binding, revalidation, disposition, and operator decision. Historical validation/binding records may make a key lifecycle-eligible, but are never live capabilities; durable `Active` means eligible under lifecycle policy, not presently signable. Every failure or ambiguity preserves its obligations, and object absence or failed lookup never proves destruction. | Pure state-machine, Kani, crash/restart, and malicious fake-provider tests cover every lifecycle/obligation product transition, a created key with lost response, idempotent reconciliation, duplicate generation/import after restart, wrong or missing immutable identity/version/public-key digest, mutable-locator retargeting, cross-tenant/role/algorithm/provider substitution, validation failure, ambiguous binding, unreadable HSM attributes, delayed/eventually consistent KMS visibility, retained quarantine, operator decision, cleanup failure, disposition ambiguity, provider unavailability, and prove no transition or generic failure state can drop an obligation, no durable lifecycle value grants signing authority, and no orphan is silently deleted or declared destroyed. |
| `v0.10.14` | Live key-authority reconstruction boundary over `v0.10.10`-`v0.10.13`: persisted lifecycle state, validation/binding audit facts, and an `Active` eligibility record can request fresh authority but can never deserialize, rehydrate, or upgrade into `ValidatedPublicKey`, `BoundSigner`, `SignerConsumerAdmission`, or any effect-authority capability. Every process/provider session reconstructs fresh public-key validation and signer binding before signing. Mutable aliases, labels, names, and persistent references resolve to the durable record's exact immutable provider-native identity/version and canonical public-key digest and never become authority identities. Restart, provider-session or policy change, key-health epoch change, immutable identity/handle replacement, public-key change, KMS alias/version retargeting, or HSM object replacement invalidates live authority and records `RevalidationRequired`; unavailable rebinding leaves the key lifecycle-eligible but unusable. | Compile-fail, serde, state-machine, Kani, crash/restart, and fake-provider tests start from persisted historical validation, binding, and `Active` records and prove none can construct a signing effect; cover replayed audit records, unchanged mutable handles across provider-session rotation, KMS alias/version retargeting, HSM delete/recreate under one label, immutable identity/version/public-key substitution, policy/key-health changes, rebinding unavailable after restart, and cleanup/disposition obligations surviving every crash boundary. Concurrent reconstruction/reconciliation uses modeled fencing so stale workers cannot publish authority or clear obligations. |
| `v0.10.15` | Provider-neutral immutable-dispatch and verified-signature commit boundary closing binding-to-signing TOCTOU. `BoundSigner` pins an immutable provider-native key identity/version plus canonical public-key digest; aliases, labels, names, and persistent references are discovery-only. Admission and signer dispatch address that exact immutable identity. Provider output is private `UnverifiedSignature` bound to the consumed admission and cannot enter JWS, CSR, revocation, TLS-ALPN, audit, or any other protocol effect. Vardheim locally verifies the exact admitted bytes, algorithm, parameters, output encoding, immutable identity, and bound public key; only successful verification constructs `VerifiedSignature` usable by an effect. Unsupported, unavailable, malformed, wrong-key, or failed verification transmits nothing, restores no admission, invalidates the signer, records revalidation/quarantine obligations according to policy, and never selects another signer or verifier implicitly. | Type/API, compile-fail, state-machine, Kani, and deterministic race tests replace an alias/object after binding but before admission, after admission but before dispatch, during dispatch, and before returned-output verification; cover a valid signature from the wrong key, immutable-identity mismatch, malformed/noncanonical/DER-raw-confused output, algorithm/parameter substitution, verifier unavailability/failure, cancellation, response loss, and provider-session invalidation. No unverified bytes can construct an effect, every failure consumes admission, wrong-key output is rejected locally, and any new attempt requires the `v0.10.14` reconstruction path plus a new request identity and admission. |
| `v0.10.16` | Provider-neutral handle-backed MAC authority and positive-evidence boundary for EAB and `DnsUpdateMac`. Transient non-serializable `BoundMacKey` pins tenant, directory/zone scope, provider/session, immutable secret identity/version, algorithm, policy, permitted MAC purposes, health epoch, and expiry; mutable secret aliases/names are discovery-only. Single-use `MacConsumerAdmission` binds the exact canonical MAC input, EAB or TSIG purpose, request identity, output/truncation policy, and TSIG request/response/chaining state, and is consumed immediately before immutable-identity dispatch and never restored. Provider output is private `UnverifiedMac`. Local/exportable-secret implementations compare a recomputed MAC in constant time before constructing purpose-specific `VerifiedMac`. Ordinary opaque-provider verification yields only distinct `ProviderAssertedMac`; `CryptographicallyAttestedMac` is reserved for signed/native receipts binding provider/session, immutable secret identity/version, input/output digests, algorithm, purpose, request/admission identity, freshness/replay protection, and assurance-profile identity. Neither weaker type is promoted to `VerifiedMac`, and default EAB/TSIG policy rejects both unless an explicit provider-trust policy admits the exact evidence class/profile. Unsupported verification never exports a secret, switches provider, or yields raw usable MAC bytes. | Type/API, compile-fail, constant-time-comparison harnesses, state-machine, Kani, Loom, and deterministic barrier tests retarget a secret before admission, after admission/before dispatch, during dispatch, and before verification; cover cross-tenant/directory/zone/key-version/purpose substitution, wrong external-account credential, wrong TSIG chain state, valid MAC from another key, malformed/truncated/extended output, provider-session change, cancellation, ambiguity, verifier unavailability, assertion/attestation-profile downgrade, forged or replayed attestation fields, admission reuse/restoration, and fallback. EAB and TSIG constructors accept only matching `VerifiedMac` or explicitly policy-admitted purpose-specific assertion/attestation evidence, never `UnverifiedMac` or raw bytes. |
| `v0.10.17` | Provider-neutral transactional symmetric-secret onboarding and content-binding state model, distinct from asymmetric public-key onboarding. Stable create/import/adopt request and provider idempotency identities bind tenant, directory/zone, EAB external-account or TSIG key identity, algorithm, permitted purposes, provider, intended immutable secret version, origin, exportability, persistence, policy, and source-secret disposition. Typed outcomes are `DefinitelyNotCreated`, `Created`, `Ambiguous`, and `Unavailable`; every new object remains quarantined until an admitted `SecretBindingMode` proves intended content equivalence. Modes are `LocallyCompared` using a fresh domain-separated local/provider MAC and constant-time comparison, `PeerConfirmed` using an authenticated exact CA/DNS peer result, `CryptographicallyAttested` using a genuine signed/native receipt bound to the full transcript and freshness, `ProviderAsserted` as weaker provider-only success rejected by default, and `Unverified` as never usable. `PeerConfirmed` is a durable lifecycle fact produced only through the `v0.10.19`-`v0.10.21` bootstrap/effect/outcome boundary, never permission for a quarantined handle to construct ordinary EAB or TSIG effects. Durable `SecretLifecycleState × SecretObligationSet` separates requested/creation-unknown/created-quarantined/active-eligible/retained-quarantined/retired state from reconciliation, binding, revalidation, source-destruction, disposition, and operator obligations. No imported/adopted handle can construct `BoundMacKey` without a current policy-admitted binding mode. | Pure state-machine, Kani, crash/restart, and malicious-provider tests cover wrong-secret import, another valid EAB/TSIG secret, lost import/create response, duplicate creation, immutable identity/version mismatch, alias retargeting, restored/recreated provider objects, cross-tenant/directory/zone/external-account/purpose import, forged/replayed peer or attestation evidence, provider assertion upgraded to stronger binding, source-secret destruction before durable binding, binding failure/ambiguity, cleanup/disposition ambiguity, provider unavailability, and restart before every persist/import/bind/activate/destroy boundary. No generic failure drops an obligation, no quarantine or durable eligibility grants MAC authority, and object absence never proves destruction. |
| `v0.10.18` | Live symmetric-secret authority reconstruction boundary over `v0.10.16`-`v0.10.17`. Persisted secret lifecycle, binding mode/profile, peer/attestation receipts, and audit history are facts only and can never deserialize, rehydrate, or upgrade into `BoundMacKey`, `MacConsumerAdmission`, `VerifiedMac`, `ProviderAssertedMac`, `CryptographicallyAttestedMac`, or purpose-effect authority. Every process/provider session re-resolves the exact immutable secret identity/version, revalidates current lifecycle/policy/provider health, and uses a fresh non-mutating local comparison, current cryptographic attestation/provider assertion admitted by policy, or authenticated non-mutating peer probe before minting reusable MAC authority. Historical peer confirmation never authorizes runtime use. Restart never automatically repeats a mutating EAB account creation or DNS UPDATE ceremony; a provider/profile that offers only mutation for peer confirmation requires explicit reconciliation and operator policy. A one-time EAB secret whose successful account creation and account state are durably committed proceeds to its disposition obligations and is not reconstructed. Restart, session/policy/health change, restored or recreated object, alias/version retargeting, assurance-profile change, peer trust/configuration change, expiry, or source/target binding change records revalidation and leaves a reusable secret eligible but unusable until safe reconstruction succeeds. | Compile-fail, serde, state-machine, Kani, Loom, crash/restart, and deterministic barrier tests start from every persisted lifecycle/binding/assertion/attestation/audit form and prove none can create an EAB or TSIG effect. Replayed receipts, unchanged aliases across session rotation, object restore/recreation, cross-peer/profile substitution, unavailable non-mutating rebinding, mutating-only profiles, consumed one-time EAB secrets, stale fenced reconstruction, concurrent rotation, and every crash boundary preserve obligations; restart never repeats peer mutation, no stale worker publishes authority, and no worker clears binding/source-destruction/disposition work without durable evidence. |
| `v0.10.19` | Narrow symmetric-secret content-binding bootstrap authority that breaks the quarantine/`BoundMacKey` cycle without granting ordinary protocol authority. A private non-serializable single-use `SecretBindingAttempt` is constructible only from `&mut` state of the exact quarantined onboarding transaction and pins provider/session, immutable secret identity/version, tenant, directory/zone, peer, external-account or TSIG key identity, algorithm, purpose, policy, fresh transcript, onboarding request, binding mode, and stable attempt identity. The local variant permits only the canonical domain-separated binding transcript. The peer variant permits only construction of the exact typed protocol-specific confirmation effect selected by policy; it cannot MAC arbitrary bytes, construct a normal `MacConsumerAdmission`, or be reused for unrelated EAB/TSIG traffic. The attempt is consumed immediately before immutable provider dispatch and is never serialized, restored, cloned, retried, or returned after success, rejection, cancellation, ambiguity, or unavailability. Failure or ambiguity yields no binding evidence or `BoundMacKey`. | Type/API, compile-fail, state-machine, Kani, Loom, and malicious-provider tests reject construction outside a quarantined mutable transaction, arbitrary-byte/provider-operation access, ordinary EAB/TSIG admission, cross-provider/session/tenant/directory/zone/peer/external-account/key/version/algorithm/purpose/transcript/onboarding-request substitution, replay, cloning, serialization, restoration, fallback, and use after every terminal or nonterminal outcome. Local attempts accept only the exact domain-separated transcript; peer attempts can populate only the exact request-bound confirmation-effect envelope; no attempted or emitted MAC becomes runtime MAC authority. |
| `v0.10.20` | Provider-neutral peer-binding external-effect identity and observation contract over `v0.10.19`. Stable binding-attempt and effect identities bind the exact immutable request bytes/digest, peer, transport/security profile, mutation class, reconciliation key, expected authenticated success/rejection evidence, and affected account or DNS resource. Effects are typed as non-mutating probe or mutating ceremony. Authenticated request-bound peer evidence is required for any success, rejection, peer confirmation, or content-mismatch claim; unauthenticated errors cannot establish those facts, transport/provider unavailability cannot prove no dispatch, and sent-or-maybe-sent effects are never blindly retried. EAB account creation may jointly establish peer confirmation and the durable account result. TSIG prefers an authenticated non-mutating probe; a mutating DNS UPDATE must declare ownership, exact rollback/cleanup, duplicate semantics, and reconciliation. The orthogonal outcome product and recovery rules are completed by `v0.10.21`; historical observations remain facts, not live capabilities. | Pure reducer, type/API, property, Kani, and adversarial protocol tests cover authenticated and unauthenticated observations, connection loss before/during/after dispatch, lost EAB success, duplicate-account risk, ambiguous DNS UPDATE, unrelated or replayed peer evidence, wrong effect/attempt/peer/resource identity, mutation misclassification, blind retry, one-time EAB reuse, and conversion of historical observations into authority. Every inconclusive path retains the source secret and reconciliation/operator obligations. |
| `v0.10.21` | Orthogonal peer-effect outcome product and recovery matrix: `DispatchKnowledge × OperationOutcome × BindingEvidence × ObservationStatus` replaces any interchangeable flat result enum. Dispatch knowledge distinguishes not started, positively `DefinitelyUnsent`, committed-but-not-dispatched, may-have-dispatched, and positively dispatched. Operation outcome independently distinguishes pending/unknown, `Succeeded`, and `Rejected`. Binding evidence independently distinguishes none/inconclusive, purpose-specific `PeerConfirmed`, and purpose-specific proven content mismatch. Observation status distinguishes available from `Unavailable` without changing dispatch knowledge or operation outcome. An authenticated `badNonce`, rate limit, contact rejection, ToS rejection, DNS `REFUSED`, or other operation result affects only the axes it proves; it never implies secret mismatch. Only authenticated purpose-specific evidence establishes peer binding or mismatch. Summary labels such as ambiguous or unavailable are projections for reporting, never state transitions or retry authority. | Exhaustive product/reducer, impossible-state, Kani, property, and table-driven recovery tests cover every axis combination and prove unavailability cannot erase may-have-dispatched state, operation rejection cannot fabricate binding failure, and ambiguous transmission cannot become retryable. The normative recovery matrix proves: before provider MAC dispatch, close as unsent and mint an entirely fresh attempt; after MAC consumption but before any outbox envelope, prove peer dispatch was impossible, discard artifacts, and start fresh; after exact envelope commit but before dispatch, send only those committed bytes without rebuilding or re-signing; after dispatch starts or may have started, reconcile without blind resend; authenticated `badNonce` obtains a fresh nonce and rebuilds the complete EAB attempt with new bootstrap/request/admission identities; account success commits account, peer binding, and obligations before disposition. A fresh attempt after positive `DefinitelyUnsent` evidence is a new capability, never restoration or reconstruction of the consumed one. |
| `v0.10.22` | Strict signature-evidence and multi-axis verifier-assurance invariant. `UnverifiedSignature`, `ProviderAssertedSignature`, `AttestedSignature`, and `VerifiedSignature` are separate sealed types; `VerifiedSignature` always and only means exact cryptographic verification of returned bytes against the bound key and exact admitted input. It binds execution locality, signer/verifier trust-domain relationship, implementation same/diverse relationship, validated-module identity including exact FIPS boundary, key/input binding strength, verifier implementation/version/session, immutable signer/key identity, exact input/output digest, algorithm/parameters/encoding, and admission identity. A local software signer may use its own conforming implementation; diversity is a stronger optional profile, while remote self-verification or provider success is not `VerifiedSignature`. JWS, CSR, revocation, challenge certificates, and signed audit records require the exact verified type. Any future policy acceptance of assertion/attestation must produce a separately named `AdmittedSignature<Policy>` witness that cannot satisfy a `VerifiedSignature` consumer. | Compile-fail, assurance-product, FIPS-boundary, effect-constructor, and hostile-provider tests prevent assertion/attestation/unverified promotion, changing any bound axis, treating remote self-verification as cryptographic verification, requiring an out-of-boundary verifier in a FIPS-only profile, accepting another request/key/encoding, or falling back. Every consumer declares independent minimum assurance axes without collapsing them to one ordinal class, and no non-verified signature evidence reaches protocol bytes or durable signed records. |
| `v0.10.23` | Provider-neutral security-sensitive identity issuance contract for request, attempt, effect, admission, reconciliation, correlation, and provider-idempotency identities. It separates stable internal identity, ephemeral correlation, and caller-supplied idempotency keys; supports admitted injected entropy or transactionally allocated counters; domain-separates tenant, purpose, store, provider, node, process/incarnation, and recovery epoch; detects bounded collisions/exhaustion; and defines persistence-before-provider ordering. Caller strings are never trusted internal identities. | Deterministic, property, Kani, concurrent-store, fork/snapshot/restore, multi-node, and fault-injection tests cover repeated/failed entropy, counter rollover, lost allocation commit, collision, process fork, VM clone, database rollback, epoch change, node-ID reuse, caller-key substitution, crash before/after persistence and external dispatch, and reproducible test issuers. No identity reaches evidence, an outbox, admission, reconciliation, or provider until its required durable allocation/domain context is committed. |
| `v0.11.0` | RFC 7515 flattened JWS construction through a bounded sink/caller buffer with typed protected headers, exactly one of `jwk`/`kid`, exact signed URL, typed payloads, purpose-specific signing requests, explicit algorithm capabilities, exact raw JOSE signature formats, consumption of matching `v0.10.12` admission immediately before signer dispatch, and acceptance only of `v0.10.15` locally verified signature evidence. | Golden signing-input vectors, Kani header-exclusivity/length harnesses, and properties prove deterministic bytes, no arbitrary JSON signing, no DER/raw signature confusion, no purpose confusion, no admission/request substitution or restoration after signer outcomes, no provider-returned unverified bytes entering JWS, and no intermediate unbounded `String`. |
| `v0.12.0` | EAB nested JWS construction through `v0.10.16`-`v0.10.21`: HMAC purpose separation, transactional secret onboarding/content binding, narrow quarantined `SecretBindingAttempt`, fresh `BoundMacKey` reconstruction only after admitted binding, directory/external-account binding, exact-input single-use `MacConsumerAdmission`, immutable secret-version dispatch, purpose-specific positive MAC evidence, algorithm/assurance policy, and source/target secret destruction lifecycle. The bootstrap attempt can create only the exact request-bound EAB confirmation envelope and cannot become ordinary EAB authority. Independent `VerifiedMac` is the default runtime requirement; explicitly admitted provider assertion or cryptographic attestation remains visibly weaker and bound to the exact directory and external-account identity. | EAB vectors plus wrong-secret import, lost/duplicate onboarding, bootstrap-cycle and arbitrary-byte negatives, restart before every binding boundary, secret-alias retargeting at every attempt/admission/dispatch/verification barrier, another valid CA credential, cross-directory/tenant/external-account/version/purpose substitution, wrong URL/algorithm/input, malformed MAC, verifier unavailable, assertion/attestation downgrade, attempt/admission reuse after failure/cancellation/ambiguity, premature source destruction, retained secret, and raw/`UnverifiedMac` constructor negatives pass without binding an account to the wrong external account. |
| `v0.12.1` | Account-key rollover inner/outer JWS envelopes with old/new key and purpose separation. | Swapped inner/outer keys, URLs, accounts, algorithms, and payloads are rejected. |
| `v0.13.0` | Single-use replay nonce ownership integrated with `v0.10.12` request admission: per-directory bounded fixed/caller-storage queues, duplicate rejection, nonce and exact request identity binding before signing, destructive admission consumption immediately before signer dispatch, cancellation consumption, and bounded retry semantics. Successful, failed, cancelled, or ambiguous signing restores neither nonce nor admission; reconciliation after ambiguous network transmission cannot reuse either. | Property and Kani tests prove no consumed nonce or admission can be emitted, reinserted, or restored; request/nonce/admission substitution fails; signer and transmission ambiguity remain non-retryable without reconstruction; and every retry re-encodes with a fresh nonce and mints a fresh request-specific admission. |
| `v0.13.1` | Initial Loom model for concurrent nonce insertion/take/cancellation, directory isolation, `BoundSigner` admission mint/consume/invalidation, unique request identities, and bounded outstanding admissions, introduced with the first shared nonce/signing-attempt pools rather than deferred to final qualification. | All bounded interleavings pass without nonce/admission reuse, duplicate request identity, capacity escape, cross-directory or cross-signer leakage, deadlock, duplicate acceptance, lost wakeup, use after signer invalidation, or restoration after cancelled/failed/ambiguous signing or send. |

## Phase 2 - RFC 8555 Protocol Core

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.14.0` | Typed request/response envelopes, bounded ACME headers/body, status/content type, effective URL, and exact signed-request URL binding. | Header/body/content-type/status and mismatched URL adversarial matrix passes. |
| `v0.14.1` | Origin authorization and SSRF policy: HTTPS, directory identity, endpoint/link allowlists, IP classification, proxies, and no automatic signed-request redirects. | Cross-origin, redirect, user-info, DNS rebinding, local/link-local/private network, and normalization matrix passes. |
| `v0.14.2` | Public provider-neutral one-request transport contract before any live CA integration: authenticated response evidence, exact requested/effective URL binding, redirects structurally disabled, bounded headers/body, phase deadlines, cancellation, and definite-failure versus ambiguous-outcome classification. | Deterministic fake transports and compile/API tests reject fabricated authentication, URL substitution, hidden redirects, unbounded bodies, generic retry of ambiguous mutations, credential leakage, and fallback to another TLS/root/transport implementation. |
| `v0.14.3` | Authenticated replay-nonce admission and reservation policy over `v0.6.5`: promote exactly one grammatically valid `Replay-Nonce` observation only after authenticated TLS, admitted ACME origin and effective URL, redirect policy, strict HTTP framing, complete admitted `newNonce` or ACME POST response classification, directory-context binding, and local duplicate/non-reuse checking within the configured bounded queue plus consumed-ID window. The client never claims global server nonce uniqueness. A nonce from an authenticated `badNonce` response is reserved for that operation's complete rebuilt retry instead of entering the general queue. Directory/account/tenant/operation partitioning remains explicit Vardheim isolation policy, not an RFC claim about server nonce scope. | Hostile transport/header tests reject unauthenticated, wrong-origin/directory, redirected, framing-invalid, duplicate-header, empty, malformed, partial, unrelated-method, cross-context, locally queued duplicate, and consumed-window replay observations; authenticated `badNonce` reserves exactly one nonce for the fresh full request composition, queue pressure cannot evict it into general use, bounded-window eviction is deterministic, and response outcome parsing cannot occur before nonce admission/refusal is recorded. |
| `v0.15.0` | Directory discovery parsing for all base resources, metadata, CAA identities, EAB-required, and bounded unknown fields. | Missing, duplicate, malformed, unsupported, and hostile endpoint fixtures fail closed. |
| `v0.15.1` | Directory refresh/change detection, trust re-evaluation, ToS change workflow, cached identity, and capability invalidation. | Endpoint/origin/ToS changes require the correct policy or operator action and cannot reuse stale trust. |
| `v0.15.2` | Complete `newNonce` operation: directory-scoped HEAD acquisition, GET alternative, no-cache/request cache-control policy, strict Replay-Nonce parsing, bounded collection from successful and problem responses, duplicate/overflow policy, and fresh acquisition/re-signing after `badNonce`. A `badNonce` retry discards the old nonce, signing bytes, request identity, and consumed `v0.10.12` admission, then obtains a fresh nonce and mints a fresh exact-request admission. | HEAD/GET/status/cache/header/malformed/base64url/duplicate/directory-confusion fixtures pass; every applicable response harvests at most one valid scoped nonce, and `badNonce` tests prove the old admission is never reused even when signing, dispatch, response, or retry coordination was cancelled or ambiguous. |
| `v0.16.0` | Account creation with contacts, ToS agreement, Location binding, status, successful state persistence event, and a current `v0.10.10` validated account public key plus `v0.10.11` account-role `BoundSigner` and exact-request `v0.10.12` admission before the first signed effect. | Positive/negative flows, malformed account responses, invalid or substituted public material, missing/stale/wrong-role binding or admission, request substitution, signer-session change, and destructive-consumption tests pass; account creation cannot emit a JWS from an unvalidated or unbound handle. |
| `v0.16.1` | `onlyReturnExisting` account recovery and lost-account-creation response reconciliation using current `v0.10.10` validation, `v0.10.11` account-role `BoundSigner`, and exact-request `v0.10.12` admission rather than account URL, parsed JWK, or cached signature as ownership evidence. | Existing, absent, ambiguous, duplicated, wrong-Location, invalid-key, wrong-handle, expired binding, stale/replayed/wrong-request admission, provider-session change, and restart cases cannot corrupt or recover account identity without a current bound signer and fresh admission. |
| `v0.16.2` | EAB-required account creation integration using exact-directory/external-account `v0.10.16`-`v0.10.21` secret onboarding, narrow peer-binding bootstrap, orthogonal peer outcomes, fresh runtime binding reconstruction, and MAC evidence. `v0.16.4` closes inner/outer evidence composition; the account-creation effect uses the `v0.33.5`-`v0.33.6` durable peer-binding/EAB outbox once persistence exists. Authenticated account success can jointly establish `PeerConfirmed` and durable account state, may-have-dispatched requests enter account-specific reconciliation without blind resend, and one-time EAB material reaches deletion only after durable account/binding success and retained obligations. | Required/optional EAB, wrong-secret import, lost/duplicate onboarding, cross-directory/credential-version/effect substitution, provider-assertion/attestation policy refusal, consumed-attempt/admission retry reconstruction, lost successful account response, duplicate-account risk, authenticated operation rejection without binding-failure inflation, unauthenticated errors, ambiguous account response, restart without repeated creation, restart before every binding/account/destruction boundary, and crash-at-every-boundary cases prove EAB material is neither reused unsafely nor deleted before durable success. |
| `v0.16.3` | Explicit existing-account adoption from directory URL, account URL, and signer/key handle: strict directory/origin/account-URL binding, current `v0.10.10` public-key validation, `v0.10.11` account-role `BoundSigner`, exact-request `v0.10.12` admission, fresh signer-authenticated POST-as-GET ownership proof, account status/contact/orders/ToS reconciliation, imported provenance, and non-exportable HSM/KMS support. | Valid/idempotent re-import plus invalid public material, wrong key/handle/role, expired binding, stale/replayed/wrong-request admission, wrong/cross-directory URL, operator-only assertion, redirected/effective URL mismatch, deactivated/revoked account, stale contacts/ToS/orders, unavailable/unsupported validation or signer, provider-session change, conflicting local account, ambiguous response, restart, and non-exportable-key fixtures prove possession of an account URL, parsed public key, or unrelated signature alone never establishes ownership. |
| `v0.16.4` | Composed EAB account-creation typestate joining the inner EAB MAC and outer account JWS into one immutable `EabAccountCreationAttempt`. The attempt binds canonical account intent, contacts and ToS agreement; directory identity and exact `newAccount` URL; account JWK and immutable account-key/provider-session identity; EAB key ID, immutable secret identity/version, MAC algorithm and evidence profile; inner protected header, payload, exact signing input and positive MAC evidence; outer replay nonce, JWS signing input, request identity and signer admission; bootstrap-attempt/account-request/outbox-effect identities; and final exact HTTP bytes/digest. Consuming states are `Prepared -> InnerMacVerified -> OuterSignatureVerified -> OutboxCommitted -> Dispatched -> Reconciled`. Only purpose-matching positive MAC evidence constructs `InnerMacVerified`; only `VerifiedSignature` over the exact complete outer input constructs `OuterSignatureVerified`; and no executable peer-effect envelope exists until that exact outer request is locally verified and durably committed. No state, evidence, admission, inner JWS, nonce, or identity can be stitched across attempts. | Type/API, compile-fail, golden-wire, state-machine, Kani, and adversarial composition tests cover inner MAC success followed by signing failure, signer/session invalidation between inner and outer construction, crash after inner MAC but before signer admission or outbox commit, substituted inner JWS after signer admission, old MAC/bootstrap evidence combined with a fresh nonce, cross-account/directory/contact/ToS/JWK/key/EAB-secret/request/effect substitution, malformed or wrong-key provider outputs, and repeated state use. Authenticated `badNonce` consumes the completed attempt and requires a fresh nonce, bootstrap attempt, MAC, signer admission, request/effect identity, and full EAB assembly; an exact successful response can be applied only once. |
| `v0.17.0` | Account retrieval, contact update, status handling, and replay/stale response policy. | Every account state and update precondition is tested. |
| `v0.17.1` | Account order enumeration and pagination semantics with bounded link traversal. | Empty, multi-page, cyclic, cross-origin, repeated, and oversized collections pass. |
| `v0.17.2` | Account deactivation and recovery reconciliation. | Ambiguous response, retry, already-deactivated, stale key, and illegal transition cases pass. |
| `v0.18.0` | Account key rollover with nested JWS, recovery checkpoints, offline ceremony boundary, and distinct current `v0.10.10` validation plus `v0.10.11` old/new account-role `BoundSigner` capabilities and separate exact-request `v0.10.12` admissions before either nested signature effect is constructed. | Golden vectors, crash at every rollover step, rejection of swapped keys/URLs/roles/bindings/admissions, one valid side paired with an unvalidated or unbound signer, request/admission replay or substitution, binding expiry, provider-session change, and old/new recovery tests prove neither half of rollover can bypass binding or admission. |
| `v0.18.1` | Durable old account-key lifecycle after rollover or account deactivation: retain both keys until fresh signer-authenticated proof through currently validated and bound signer handles plus fresh request admissions establishes the effective account key, classify ambiguous rollover without disposing either key, invalidate expired `v0.10.11` bindings and consumed/stale `v0.10.12` admissions, apply explicit disable/retain/scheduled-delete/destroy policy through `v0.10.9`, and preserve legal-hold and audit obligations for software, HSM, and KMS keys. | Crash/restart at every rollover/deactivation/binding/admission/disposition boundary plus lost response, old-only/new-only/both/neither signer availability, stale/replayed/wrong-role or wrong-request admission, CA rollback or stale view, deactivated account, scheduled deletion/cancellation, legal hold, provider outage, idempotent reconciliation, and receipt-binding tests prove no key is destroyed before durable control evidence and no ambiguous account can resume mutation. |
| `v0.19.0` | Normalized certificate intent, stable intent digest, sorted identifiers, validity/profile/key/challenge/deployment policy, and local single-flight identity. | Intent equivalence, duplicate, tenant, ordering, and policy mutation properties pass. |
| `v0.19.1` | `newOrder` construction and response parsing with identifiers, validity, Location, expiry, error, extension fields, and authorization/finalize URLs. | Multi-identifier and malformed order fixtures pass. |
| `v0.19.2` | Unknown `newOrder` outcome reconciliation through account orders and normalized intent evidence. | Lost-response simulation never blindly duplicates an order without policy authorization. |
| `v0.19.3` | Multi-issuer semantic policy: typed directory/issuer identities, directory-scoped account keys and nonces, explicit issuer selection, per-issuer ToS/EAB/profile/trust/rate state, sharing policy, and a hard prohibition on silent CA failover. | Cross-directory credential/nonce/account substitution, implicit sharing, provider-name branching, and automatic alternate-issuer retry fail; a failover proposal requires a new explicit operator/policy decision and records migration/replacement intent. |
| `v0.20.0` | Two-level authorization state: bounded untrusted wire observations plus validated local states/capabilities for status, expiry, identifier, wildcard metadata, reuse, deactivation, and extensions. | Exhaustive runtime transition tables reject contradictory/unknown remote observations while compile-fail tests restrict operations to validated local capabilities. |
| `v0.20.1` | Challenge offer/status/error models with CA status separated from local presentation lifecycle, bounded opaque unknown methods, method markers and family version modules such as `dns_01`, selection inputs, and acknowledgement semantics. | Mixed known/unknown offers, future-method retention, status/presentation confusion, and impossible transitions fail; family calculations cannot advance authorization/order state directly. |
| `v0.20.2` | Multi-authorization aggregation, readiness, mixed terminal outcomes, and cleanup ownership assignment. | Permutation/property tests prove order-independent correct aggregation. |
| `v0.20.3` | Optional RFC 8555 `newAuthz` pre-authorization: directory capability detection, exactly one identifier, account/directory binding, wildcard rules, request/Location response handling, reuse/deactivation integration, and typed unsupported capability when omitted. | Missing endpoint, multiple identifiers, wrong account/directory, malformed Location, wildcard/preauthorization policy, replay, reuse, deactivation, ambiguous outcome, and unsupported-CA cases pass without weakening ordinary order authorization. |
| `v0.20.4` | Conflict-checked challenge-method registry and sealed revision authority with three non-interchangeable identities: the IANA method token (`dns-01`), the exact RFC/draft specification revision implementing that token, and Vardheim's internal evidence/schema revision. Core owns only method/status/effect authorization; family modules own specification-specific calculation and presentation evidence. Registry entries also bind identifier kinds, calculation profile, presentation family, compatibility state, and implementation/capability owner; a future `dns-02` is a distinct method token within `vardheim-challenge-dns`, not revision two of `dns-01`. | Duplicate method/specification/schema identity, overlapping authority, missing owner, cross-family or cross-identity conversion, recognized-only promoted to executable, and extension collision fixtures fail; synthetic new schema and specification revisions remain explicitly compatible/incompatible, while synthetic `dns-02` coexists in the family crate without a new crate or conversion from `dns-01` evidence. |
| `v0.21.0` | POST-as-GET request semantics and typed retrieval for account, order, authorization, challenge, and certificate resources. | Empty-payload/signature and resource-type confusion fixtures pass. |
| `v0.21.1` | Retry-After HTTP-date/delta parsing, wall/monotonic clock conversion, bounded backoff, jitter inputs, and clock-skew reporting. | Date/delta/overflow/skew vectors and deterministic injected-clock tests pass. |
| `v0.21.2` | Polling controller, status reconciliation, cancellation, deadlines, retry budgets, and endless/contradictory server behavior. | Cancellation races, endless polling, stale response, and contradictory state fault injection pass. |
| `v0.22.0` | CSR finalization request/response semantics, order-ready enforcement, CSR digest binding, processing polling, and certificate URL acceptance. | Impossible finalization transitions fail; lost finalization responses reconcile without blind unsafe retries. |
| `v0.23.0` | Certificate POST-as-GET retrieval, media-type policy, borrowed bounded RFC 7468 PEM tokenization, streaming base64 decode, and strict certificates-only bundles. | Private-key injection, malformed label, oversized bundle, wrong first object, non-certificate object, scratch exhaustion, fuzz, and independent-parser differential cases pass. |
| `v0.23.1` | RFC 8288 Link parsing and bounded alternate-chain discovery with origin policy and relation filtering. | Hostile, cyclic, cross-origin, malformed, duplicated, and excessive alternate links fail safely. |
| `v0.24.0` | Complete revocation: account-key and certificate-key signing, reason validation, already-revoked behavior, signer-purpose isolation, and current `v0.10.10` validation plus `v0.10.11` role-specific `BoundSigner` and exact-request `v0.10.12` admission for whichever handle constructs the revocation JWS, including a compromised certificate key only on the explicitly permitted certificate-revocation path. | Every RFC reason and signer path plus invalid/substituted public material, wrong key/certificate/role, missing/expired binding, stale/replayed/substituted admission, account-versus-certificate signer crossover, provider-session change, compromised-key use outside its narrow revocation purpose, replay, ambiguous outcome, and idempotency tests pass. |
| `v0.25.0` | Complete structured problem/subproblem decoding, registered/unknown extension problems, operator-action links, and sanitized server detail. | Complete problem fixture and recursive/resource-limit suite passes. |
| `v0.25.1` | RFC 8555 pagination and collection conformance closure across account orders and extension collections. | Cyclic, repeated, cross-origin, malformed, and budget-exhausting pagination tests pass. |
| `v0.25.2` | RFC 8555 section-by-section client conformance closure with no catch-all implementation work. | The machine-readable matrix reports every mandatory client requirement implemented and every optional behavior explicitly classified. |

## Phase 3 - Challenge And Certificate Foundation

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.26.0` | HTTP-01 core: exact token/path/key-authorization construction, DNS/IP identifier binding, wildcard prohibition, and expiry. | RFC vectors; traversal, ambiguous path, Unicode, host mismatch, stale token, and token-bound tests. |
| `v0.27.0` | HTTP challenge presentation trait, in-memory store, generic route helper, expiry, and `v0.6.6` observation-to-qualified-evidence promotion into private-constructor ownership receipts required for acknowledgement and cleanup. | Concurrent presentations never overwrite or expose unrelated data; exact lookup/expiry, hostile-adapter assertion/promotion properties, and an initial Loom model prove receipt ownership cannot be forged, overclaimed, or confused. |
| `v0.27.1` | HTTP-01 local/external self-check interface, IPv4/IPv6 policy, multi-vantage evidence, and cluster visibility barrier. | Resolver/routing disagreement, partial visibility, timeout, and false-positive simulations pass. |
| `v0.27.2` | Durable HTTP challenge cleanup obligations, compare-and-delete ownership, retry classification, and crash recovery. | Cleanup never removes another workflow's data and resumes after every injected crash. |
| `v0.27.3` | Axum and Actix HTTP-01 integration examples and tested adapter helpers built on the generic route/store API, without framework dependencies in core crates. | Framework integration suites prove exact routing, host binding, concurrency, expiry, response body, and cleanup behavior. |
| `v0.27.4` | Redis-backed distributed HTTP-01 presentation store with namespacing, TTL, compare-and-delete ownership, cluster visibility, and credential redaction. | Real Redis integration covers failover, stale TTL, duplicate token, lost acknowledgement, wrong owner, partition, and cleanup. |
| `v0.27.5` | Shared sealed challenge-presentation receipt envelope binding the exact IANA method token, specification revision, internal evidence/schema revision, identifier type/value, account/key authorization, directory/tenant/order/authorization/challenge IDs, presentation generation, expiry, backend identity, externally observed value digest, and cleanup ownership. Family crates construct their own private specification/schema payloads; core consumes only validated envelope capabilities. | Compile-fail, serialization, substitution, expiry, replacement, restart, stale-worker, schema-migration, specification-revision, and synthetic-future-method tests prove downstream adapters cannot forge receipts, one backend/generation cannot acknowledge or clean another, persisted audit facts cannot restore authority, and no DNS/HTTP/TLS, method-token, specification-revision, or evidence-schema conversion exists without an explicit reviewed compatibility rule. |
| `v0.28.0` | HTTP-01 filesystem webroot adapter with restrictive permissions, atomic creation, exact cleanup, Nginx/Apache integration examples, and no arbitrary CA-controlled path. | Platform filesystem tests, symlink/race/traversal attacks, crash cleanup, and permission verification. |
| `v0.28.1` | Introduce the dependency-light `vardheim-pkix` crate as the independently auditable `no_std` certificate boundary for DER, PEM, CSR, X.509, path policy, status evidence, CT evidence, and certificate binding; protocol types remain in core and concrete crypto/network adapters remain outside it. | Crate-direction, feature, packaging, documentation, and compile tests prove `vardheim-pkix` has no runtime/TLS/database/provider dependency, core does not depend on it, the facade can compose it explicitly, and all future PKIX milestones have assigned module ownership. |
| `v0.28.2` | Canonical bounded DER writer with checked tag/length arithmetic, deterministic SET/extension ordering, caller-provided sinks, and exact output sizing before CSR or certificate construction. | Published vectors, exhaustive short lengths, long-form boundaries, ordering, short-buffer, overflow, noncanonical encoding, and Kani length proofs pass without partial output. |
| `v0.28.3` | Borrowed bounded DER reader with depth/element/byte/work budgets, checked tag/length arithmetic, exact consumption, and caller-controlled storage before external CSR or certificate parsing. | Hostile DER corpus, mutation/differential fuzzing, truncation, indefinite/nonminimal length, overflow, trailing data, and Kani conversion tests pass. |
| `v0.28.4` | Shared ASN.1 algorithm-identifier and parameter model for keys, CSR, X.509, OCSP, and CRL, including absent/NULL equivalence rules, curve identifiers, RSA PKCS#1, and exact RSA-PSS parameter validation. | Cross-context vectors reject unknown/duplicate/mismatched parameters, unsafe defaults, DER/raw confusion, PSS hash/MGF/salt/trailer mismatch, and inconsistent SPKI/signature identifiers. |
| `v0.28.5` | Private test-only reference crypto primitives implementing real SHA-2, HMAC including HS256, entropy, and key generation through the provider-neutral contracts, with no production-support or crates.io claim. | Official vectors, entropy/key-generation failures, wrong MAC/digest purpose, dependency admission, feature isolation, and package tests prove genuine bytes are available to early interoperability while the adapter remains outside production graphs. |
| `v0.28.6` | Private test-only reference signing/verification adapter for ES256, RSA PKCS#1, RSA-PSS verification, CSR/X.509 signatures, and TLS-ALPN challenge-certificate signing, building on `v0.28.2`-`v0.28.5`. | Real CSR/JWS/certificate/path/TLS fixtures interoperate with independent implementations; purpose/algorithm/key/parameter negatives pass, and no public API or production capability claim exposes the adapter. |
| `v0.28.7` | Private test-only PKIX evidence verifier extension for genuine OCSP/CRL/SCT signatures, delegated responder chains, SCT log algorithms, and narrowly purpose-bound OCSP SHA-1 before production providers arrive. | Official and independent status/CT matrices prove genuine signatures and negative cases; unsupported algorithms remain typed, purpose isolation holds, and feature/package checks keep implementation dependencies outside production and crates.io graphs. |
| `v0.28.8` | Private test-only DNSSEC verifier extension covering the pinned mandatory/recommended RSA, ECDSA, EdDSA, DS-digest, and narrowly purpose-bound SHA-1/NSEC3 capabilities before production providers arrive. | Current registry/RFC vectors prove genuine DNSSEC signatures, DS digests, NSEC3 hashes, and negative cases; unsupported values remain typed and all implementation dependencies remain outside production and crates.io graphs. |
| `v0.28.9` | Private test-only `DnsUpdateMac` implementation for RFC 8945 HMAC-SHA-256 and permitted SHA-384/SHA-512 TSIG fixtures, isolated from EAB and all production/crates.io graphs. | RFC 8945 vectors, wrong key/algorithm/purpose, request-response chaining, truncation, BADKEY/BADSIG/BADTIME, secret-retention, feature-isolation, and package tests provide genuine TSIG bytes before production crypto. |
| `v0.28.10` | Strict bounded ASN.1/PKIX time parser and checked wall-clock conversion shared by certificates, CRLs, OCSP, and CMS/CT: RFC 5280 UTCTime 1950-2049 interpretation, GeneralizedTime selection, mandatory seconds/Zulu, no offsets or fractional seconds for PKIX profile fields, calendar validation, explicit leap-second policy, and ordered interval construction. | Official/independent and differential fixtures cover 1949/1950/1999/2000/2049/2050/9999 boundaries, wrong time type, missing seconds/Z, offsets, fractions, invalid month/day/leap year/time, leap second, `notBefore > notAfter`, integer overflow/range loss, locale/system-parser differences, short buffers, and exact checked conversion into Vardheim wall time. |
| `v0.28.11` | RFC 5280 distinguished-name equality and normalization with ordered RDN sequence, unordered attributes within an RDN, exact attribute OIDs, UTF8String/PrintableString support, RFC 4518 LDAP StringPrep including RFC 3454 case folding and insignificant-space handling, caseIgnoreMatch, domainComponent rules, and explicit typed handling of optional/unknown matching rules. | RFC-derived and independent matrices cover encoding differences, Unicode normalization/case folding, leading/trailing/repeated spaces, multi-valued RDN reordering, RDN-order changes, OID substitution, domainComponent case, embedded NUL/prohibited characters, unsupported Teletex/BMP/Universal strings, unknown equality rules, and collision/non-transitivity attempts without locale-dependent comparison. |
| `v0.28.12` | Strict DER primitive/constructed conformance layer over `v0.28.3`: minimal signed INTEGER and ENUMERATED encodings, minimal base-128 OID arcs, BIT STRING unused-bit rules, canonical BOOLEAN, exact zero-length NULL, primitive-versus-constructed tag legality, canonical SET ordering hooks, and shortest definite-length enforcement before any PKIX model accepts a value. | X.690/RFC-derived vectors, independent-parser differential tests, exhaustive short encodings, high-tag/long-length boundaries, canonically encoded zero/positive/negative INTEGER and ENUMERATED acceptance, nonminimal signed encodings, nonzero-length NULL, unterminated/overflow/nonminimal OIDs, invalid unused bits, noncanonical TRUE, constructed primitive types, primitive constructed types, reordered SETs, short buffers, and Kani arithmetic proofs pass or fail at the generic DER boundary as specified; context parsers separately enforce positivity, ranges, and zero restrictions. |
| `v0.28.13` | X.509 certificate-envelope structural prevalidation before path processing: version-gated serial/signature/issuer/validity/subject/SPKI/issuerUniqueID/subjectUniqueID/extensions fields, positive bounded serial policy, normalized inner/outer `signatureAlgorithm` and parameters equivalence through `v0.28.4` rather than raw-byte equality, duplicate extension-OID rejection, unknown-critical-extension rejection unless the active profile has an assigned handler, and empty-subject/critical-SAN requirements. | RFC 5280 and independent malformed-certificate corpora cover absent/default/illegal versions, v1/v2 extension or unique-ID misuse, canonical zero/negative/oversized serials, nonminimal serial encoding rejected earlier, normalized-equivalent and mismatched algorithm parameters, duplicate extensions, unknown critical/noncritical extensions, empty subject with absent/noncritical SAN, trailing fields, and prove every structural defect is rejected before issuer search or path validation. |
| `v0.28.14` | JWK/SPKI/CSR/certificate public-key validation integration through `v0.10.10`: RSA modulus positivity/oddness/minimum and maximum size plus exponent positivity/oddness/range/policy; Weierstrass coordinate range, decoding, exact named-curve binding, on-curve, non-infinity, and subgroup/cofactor policy; Ed25519/Ed448 exact encoding/length/decoding and algorithm-profile weak/small-order policy; and JWK/SPKI algorithm/parameter consistency. Handle-backed consumers additionally require current `v0.10.11` `BoundSigner` plus exact-request `v0.10.12` admission; issued leaf keys are validated even when never used to verify a signature. | RFC 5480/8017/8032/8410, exact pinned SEC/NIST, independent-provider, and malformed-key matrices cover zero/even/weak/oversized RSA values and exponents, invalid/infinite/off-curve/wrong-curve EC points, malformed/noncanonical/weak-policy EdDSA encodings, absent-versus-forbidden parameters, JWK/SPKI substitution, CSR/leaf key mismatch, signer-handle/public-key mismatch, provider/session/policy changes, unsupported/unavailable validation, no fallback, and prove no CSR acceptance, account operation, certificate binding, deployment, or handle-backed signing effect can use unvalidated, unbound, or unadmitted evidence. |
| `v0.29.0` | Certificate key policy, key-handle role binding, algorithm/export/reuse/storage/attestation choices, account-key separation, and typed renewal rotation mode selection. | API/type tests prevent account/leaf reuse and silent algorithm/exportability fallback; renewal cannot proceed without an explicit rotation mode. |
| `v0.29.1` | RFC 2986 CSR construction with exact SANs, wildcard/subject/KU/EKU/profile policy, optional RFC 7633 TLS Feature request values, deterministic extension ordering, and a signature boundary requiring current `v0.10.10` validation, `v0.10.11` CSR-role `BoundSigner`, exact-request `v0.10.12` admission, immutable-identity dispatch, and locally constructed `v0.10.15` `VerifiedSignature`. No CSR bytes become usable before verification. | Published/generated vectors, supported/unknown/duplicate TLS Feature values, criticality policy, exact order/CSR identifier-set equality, invalid/substituted public material, mutable identity replacement before/during dispatch/verification, valid wrong-key signature, malformed/DER-raw-confused output, verifier unavailable, wrong handle/role, expired binding, stale/replayed/substituted admission, provider-session change, and compile/API tests prove unverified output cannot construct or emit a CSR. |
| `v0.29.2` | External CSR bounded parsing, signature verification, `v0.28.14` provider-bound public-key validation evidence, key capability checks, and policy validation before finalization. | Invalid or substituted public material, duplicate SAN, unauthorized subject, wrong signature/key/algorithm/provider session, unavailable/unsupported validation, malformed DER, profile mismatch, fuzzing, and independent-parser/provider differential fixtures pass with RFC-reviewed disagreement decisions and no fallback. |
| `v0.29.3` | Provider-neutral immutable issued-certificate trust snapshot model with anchor DER/fingerprints, snapshot identity/hash, explicit additions/removals/distrust, purpose/profile/name/policy constraints, provenance, tenant binding, and no ACME-root/CT/DNSSEC reuse. | Construction and mutation tests reject duplicates, conflicting trust/distrust, wrong tenant/domain/profile, mutable backing data, hash ambiguity, missing provenance, constraint loss, and load-as-empty behavior. |
| `v0.29.4` | `CertificateTrustProvider` load/reload contract returning versioned immutable snapshots with unavailable/corrupt/unsupported/unchanged/changed outcomes and no load-or-empty fallback; snapshot changes create explicit verification invalidation inputs. | Fake-provider conformance covers root add/remove/distrust, failed/partial reload, rollback, stale version, hash collision simulation, tenant crossover, concurrent readers, and mandatory revalidation signals. |
| `v0.29.5` | Durable certificate-key renewal/rotation policy with explicit `AlwaysRotate`, bounded reuse-count/time, provider-managed rotation, and forced rotation after compromise, algorithm/profile/provider-policy change, attestation loss, or key-health failure; the selected key generation is bound into renewal intent and audit state. | Restart and policy-transition properties cover reuse limits, expiry edges, concurrent renewals, provider rotation races, unavailable replacement generation, compromise override, algorithm/profile changes, attestation loss, rollback, and prove neither implicit indefinite reuse nor unnecessary rotation can occur outside the recorded mode. |
| `v0.30.0` | X.509 leaf/intermediate structural models for SPKI, names, validity, SAN, KU/EKU, basic/name constraints, critical extensions including RFC 7633 TLS Feature, signatures, AKI/SKI, path length, and shared algorithm identifiers, consuming only `v0.28.13`-validated envelopes. Structural relationships include CA/basicConstraints/keyCertSign, path-length legality, nameConstraints only on CA certificates, EKU/KU consistency, and extension criticality rules. | Certificate parsing corpus plus continuous differential fuzzing covers malformed or contradictory basic constraints, KU/EKU/name constraints/path length, TLS Feature malformed/duplicate/unknown/critical values, and proves structural failures occur before issuer search or path-trust decisions. |
| `v0.30.1` | Issued-leaf binding: current `v0.28.14` validation evidence for the returned public key even when it verifies no signature, equality with the CSR key, exact authorized identifiers, requested profile/TLS Features, validity bounds, required/prohibited extensions, and no extra SANs or unrequested Must-Staple requirement. | Invalid/weak-policy/substituted leaf key, stale/cross-session evidence, validation unavailable/unsupported, key/SAN/profile/validity/extension/TLS-Feature substitution, requested-but-omitted, returned-but-unrequested, duplicate, and unsupported feature-value cases fail closed without backend fallback. |
| `v0.30.2` | RFC 5280 path construction and base validation with an explicit issued-certificate trust snapshot, AKI/SKI selection, every `GeneralName` name-constraint form, distinguished-name comparison, self-issued handling, trust-anchor constraints, basic/path-length constraints, signature/parameter equivalence including RSA-PSS, depth/time budgets, caller-provided candidate/visited storage, and no network fetching. | Official/independent valid and broken path fixtures plus differential evidence cover all name forms, DN canonical/equality edge cases, self-issued rollover, constrained anchors, loops, ambiguous issuers, parameter mismatch, every base constraint, exact workspace exhaustion, and deterministic no-heap behavior. |
| `v0.30.3` | Complete RFC 5280 certificate-policy processing: valid-policy tree, `certificatePolicies`, `policyMappings`, `policyConstraints`, `inhibitAnyPolicy`, explicit-policy and anyPolicy behavior, caller-provided policy-tree arenas, and every intentionally unsupported non-client feature classified. | RFC-derived and independent policy-path matrices exercise mapping, inhibition, pruning, wrap-up, malformed qualifiers, critical-extension handling, and exact arena exhaustion without silently reducing to a Web PKI subset. |
| `v0.30.4` | RFC 6125 service identity policy for DNS/IP/email/profile-specific identities, wildcard rules, IDNA binding, and caller-selected service context. | Identity, wildcard, Unicode, IP-type, and cross-service confusion corpus passes. |
| `v0.30.5` | Certificate-status evidence policy for locally verified OCSP and CRL results, freshness/failure modes, and explicit offline/soft/hard-fail policy without hidden network I/O or adapter-created proof. | Revoked, stale, unknown, unavailable, issuer mismatch, and policy-mode fixtures prove only PKIX verification can construct status evidence. |
| `v0.30.6` | Version-aware Certificate Transparency evidence interface and operator policy for public Web PKI deployments, with distinct RFC 6962 v1 and RFC 9162 v2 evidence/profile types, without claiming CT-log operation in core or accepting adapter assertions as proof. | Valid/invalid/unknown-log/stale-list, unsupported-version, mixed-version, and disabled-policy behavior is explicit; compile-fail tests prevent external construction or cross-version conversion of verified CT evidence. |
| `v0.30.7` | Bounded OCSP request encoding and response decoding for `OCSPResponse`, `BasicOCSPResponse`, `ResponseData`, `SingleResponse`, `CertID`, `ResponderID`, nonce, and critical extensions using caller-provided extension/response workspaces. | RFC 6960 and independent fixtures cover GET/POST request bytes, malformed/duplicate/unknown critical extensions, trailing data, response-status combinations, exact capacity exhaustion, and parser differential fuzzing. |
| `v0.30.8` | Local OCSP cryptographic and semantic verification: response signature, issuer/delegated-responder chain and EKU, responder identity, `CertID`, producedAt/thisUpdate/nextUpdate, absent-`nextUpdate` maximum-age policy, nonce policy, clock-rollback detection, revocation time/reason, and recursion limits. | Wrong issuer/responder/key/hash/signature/EKU/time/nonce/cert ID, delegated-responder revocation, replay, absent/far-future `nextUpdate`, maximum age, clock rollback, and multi-response ambiguity fail closed; only successful local verification yields status evidence. |
| `v0.30.9` | Bounded CRL and `TBSCertList` parsing with CRL/entry extensions, CRL number, delta indicator, issuing distribution point, authority key identifier, reason, invalidity date, and certificate issuer using caller-provided extension/entry storage. | RFC 5280 and independent full/delta/indirect fixtures cover malformed time/name/serial/extensions, duplicates, unknown critical extensions, oversized lists, exact workspace exhaustion, and differential fuzzing. |
| `v0.30.10` | Local CRL signature, issuer/scope, distribution-point, indirect-entry, reason-coverage, freshness, base/delta pairing, CRL-number, and revocation-state verification with bounded merge work. | Wrong signer/issuer/scope/base/delta/number/time/reason, removed-from-CRL, duplicate/conflicting entries, stale evidence, and merge-budget cases fail closed; only successful local verification yields status evidence. |
| `v0.30.11` | Bounded RFC 6962 CT v1 SCT-list parsing from X.509 certificate extensions, OCSP extensions, and TLS sources with exact source/provenance binding, v1 log-ID and digitally-signed fields, caller-provided list storage, and a persistent differential fuzz target. | RFC 6962 v1 and deployed independent fixtures cover malformed lengths, duplicate SCTs, mixed sources, unknown versions/algorithms/logs, trailing bytes, exact capacity exhaustion, and every fuzz finding as an immutable regression seed. |
| `v0.30.12` | Local RFC 6962 CT v1 SCT signature-input construction and verification against a v1 log-list snapshot, including certificate/precertificate form, issuer-key hash, poison-extension removal, timestamp, log lifetime/disqualification, and operator diversity policy. | Wrong v1 log/key/source/issuer hash/entry type/timestamp/signature, stale or rolled-back lists, duplicate operators, malformed precertificate reconstruction, retained poison extension, and v2 evidence fail closed; adapters cannot fabricate verified CT evidence. |
| `v0.30.13` | Provider-neutral `PublicPkiFetch` contract distinct from ACME transport: explicit HTTP/HTTPS and unsupported-scheme policy, no cookies or ACME/proxy/ambient credentials, address-class/SSRF controls, bounded redirects/bodies/deadlines/caches, request-purpose isolation, and untrusted-byte-only results. | Conformance adapters detect credential/header leakage, redirects to forbidden addresses, DNS rebinding, proxy/ambient authentication, cache-key confusion, oversized bodies, unsupported LDAP, fabricated evidence, and any path that bypasses local cryptographic verification. |
| `v0.30.14` | Optional OCSP acquisition over `PublicPkiFetch`: responder discovery, bounded GET/POST selection, request construction, cache key/freshness/nonce policy, offline mode, and refresh scheduling; downloaded bytes always enter `v0.30.7`-`v0.30.8`. | Mock responder tests cover malicious URLs, wrong content, replay, stale/future responses, cache poisoning, outage, ambiguous network results, GET/POST limits, soft/hard/offline policy, and no hidden fetch from `no_std` validation. |
| `v0.30.15` | Optional CRL acquisition over `PublicPkiFetch`: distribution-point policy, bounded download, cache/provenance, offline mode, and refresh scheduling; downloaded bytes always enter `v0.30.9`-`v0.30.10`. | Malicious/unsupported URLs, redirects, wrong content, cache poisoning, outage, oversized lists, stale data, and policy-mode fixtures pass without unbounded, credentialed, or implicit network access. |
| `v0.30.16` | Optional RFC 6962 CT v1 acquisition: signed/pinned v1 log-list source policy, bounded list update/cache/rollback, SCT collection from certificate/TLS/OCSP sources, offline mode, and refresh scheduling; all objects enter `v0.30.11`-`v0.30.12`. | Tampered/downgraded/stale v1 log lists, v2 substitution, split sources, outage, rollback, oversized evidence, and disabled/offline policy tests pass without adapter-created trust, version fallback, or CT-log operation claims. |
| `v0.30.17` | Optional AIA intermediate acquisition over `PublicPkiFetch`, disabled by default, with explicit allowlists, cache/provenance, recursion/depth/work budgets, and offline behavior; every certificate is parsed and validated locally. | Private/link-local/cross-origin URLs, redirect loops, wrong content/issuer, cycles, cache poisoning, outage, oversized chains, and disabled-policy cases cannot influence path validation without explicit authorization and local verification. |
| `v0.30.18` | Bounded RFC 9162 CT v2 `TransItem`, `TimestampedCertificateEntryDataV2`, inclusion-promise, signed-tree-head, consistency-proof, and inclusion-proof parsing with v2 OID log IDs, version-specific signature schemes, caller-provided storage, and persistent differential fuzzing. | RFC 9162 and independent fixtures cover every supported `TransItem` type, malformed vectors/lengths/OIDs/extensions, unknown types/algorithms, duplicate/mixed items, exact capacity exhaustion, and rejection of RFC 6962 wire objects as v2. |
| `v0.30.19` | Local RFC 9162 CT v2 signature-input and evidence verification against a v2 log-list capability, including CMS precertificate reconstruction, issuer binding, timestamp/log lifetime/disqualification, and explicit no-conversion/no-fallback separation from RFC 6962 v1. | Wrong log OID/key/scheme/item type/issuer/CMS input/timestamp/signature, stale or rolled-back v2 lists, v1 log/evidence substitution, and attempted v1-v2 conversion fail closed; version-specific verified evidence remains non-interchangeable. |
| `v0.30.20` | Complete RFC 6960 OCSP extension and delegated-responder recursion policy: parse and classify `id-pkix-ocsp-nocheck`, archive cutoff, extended revoke, acceptable-response, CRL-reference, service-locator, and preferred-signature extensions with explicit criticality and bounded recursion semantics. | Official/independent fixtures cover absent/NULL/malformed/duplicate/critical extensions, unauthorized `nocheck`, archive-cutoff misuse, extended-revoke status rules, response-type mismatch, responder loops/depth exhaustion, and every policy branch without treating an uncheckable responder as trusted. |
| `v0.30.21` | Chain-wide certificate-status policy with separately configurable leaf/intermediate requirements, responder-certificate status outcomes, recursion termination, source/freshness binding, and deterministic aggregation into good/revoked/unknown/unavailable/indeterminate. | Leaf-only, every-intermediate, exempt-anchor, delegated-responder, `nocheck`, unavailable-responder-status, mixed OCSP/CRL, soft/hard/offline, loop, and partial-chain matrices prove no required certificate silently escapes status evaluation. |
| `v0.30.22` | OCSP acquisition privacy policy over `PublicPkiFetch`: operator-selectable POST-only mode, bounded GET eligibility, request-body-sensitive cache identity, tenant/purpose/policy isolation, nonce privacy rules, and no cross-tenant responder sharing unless explicitly authorized. | Mock responders and cache probes prove POST-only never emits certificate identifiers in URLs, GET limits are exact, URL/method/body/tenant/purpose/policy changes miss cache, nonce-bearing responses are not broadly shared, and privacy policy cannot be weakened by adapter defaults. |
| `v0.30.23` | Public-PKI HTTP representation and framing policy: decompression disabled by default or guarded by independent encoded/decoded/work budgets; conflicting or invalid `Content-Length`/`Transfer-Encoding`, excessive chunks/trailers/informational responses, premature EOF, and partial bodies are errors. | RFC 9110/9112 hostile-server fixtures cover compression bombs, CL/TE ambiguity, duplicate lengths, invalid/final transfer codings, chunk extensions/count/size, trailer limits, 1xx floods, close-delimited/incomplete bodies, timeout, and exact budget exhaustion without yielding a successful PKI object. |
| `v0.30.24` | Purpose-specific public-PKI representation and cache contract: validate configured media types, bind cache keys to normalized URL, method, request-body digest, tenant, purpose, policy version, trust/privacy context, and response provenance, and never cache a partial or framing-invalid object. | OCSP/CRL/AIA/CT media-type and cache-partition matrices reject type confusion, body-equivalent URL collisions, GET/POST mixing, tenant/purpose/policy crossover, redirect-origin ambiguity, stale privacy policy, partial entries, and adapter-selected cache weakening. |
| `v0.30.25` | Optional RFC 9162 CT v2 acquisition and policy integration: signed/pinned v2 log-list capabilities, bounded `TransItem` collection/update/cache/rollback, offline mode, refresh scheduling, and explicit coexistence with but no fallback to CT v1. | Tampered/downgraded/stale v2 log lists, v1 substitution, mixed-version sources, wrong OID/operator metadata, outage, rollback, oversized evidence, disabled/offline policy, and v1-only deployments classify precisely without adapter-created trust or evidence conversion. |
| `v0.30.26` | RFC 6962 CT v1 Merkle audit primitives: leaf/node hashing, signed-tree-head input/signature verification, bounded inclusion-path verification, and append-only consistency-proof verification using v1 log capabilities and caller-provided workspaces. | RFC 6962/independent vectors cover empty/single/non-power-of-two trees, wrong leaf/index/tree size/root/STH key/signature/time, short/long/reordered paths, overflow, stale/disqualified logs, inconsistent roots, exact workspace exhaustion, and rejection of v2 objects without producing inclusion or consistency evidence. |
| `v0.30.27` | RFC 9162 CT v2 Merkle audit primitives over versioned `TransItem` objects: leaf hashing, STH signature verification, bounded inclusion and consistency verification, final-STH/log-shutdown policy inputs, and no reuse of v1 hash/signature identities. | RFC 9162/independent vectors cover every tree boundary, wrong OID/type/hash algorithm/root/STH signature/time/frequency/final-STH state, malformed/reordered proofs, overflow, disqualified/closed logs, exact workspace exhaustion, and v1 substitution without producing v2 audit evidence. |
| `v0.30.28` | Provider-neutral version-aware CT audit acquisition contract for STH, inclusion, consistency, and combined proof retrieval with exact log/base-URL identity, privacy policy, bounded responses/deadlines/retries, untrusted-byte results, and no direct-log fetch hidden inside PKIX verification. | Fake-log conformance covers wrong log/version/tree size/leaf hash/effective URL/media type, rollback, split endpoints, stale/future STHs, MMD skew, privacy-disabled direct fetch, partial/oversized responses, ambiguous timeout, and proves adapters cannot construct verified inclusion/consistency evidence. |
| `v0.31.0` | Alternate-chain candidate parsing and bounded evaluation against completed PKIX policy. | Broken, looping, ambiguous, expired, and cross-signed candidate fixtures pass. |
| `v0.31.1` | Explicit chain selection policy: server default, root fingerprint, issuer, shortest valid, compatibility metadata, and bounded custom selector. | Deterministic selection/property tests prove no hardcoded CA names or order-dependent result. |
| `v0.31.2` | Core-to-PKIX evidence bridge: `vardheim-pkix` exclusively constructs private-field `VerifiedLeaf`, `VerifiedPath`, and `CertificateBinding`; the facade validates and translates them through the `v0.6.6` promotion path into crate-private reducer events, while public custom adapters return bounded observations only. | Compile-fail/architecture tests prevent public verified-event construction, core-to-PKIX dependency reversal, observation-to-success shortcuts, and evidence forgery; documentation marks configured backend implementations as trusted-computing-base components that types cannot make honest if malicious. |
| `v0.31.3` | Validation-session-bound PKIX capabilities that borrow or brand their input/session and bind leaf/chain hashes, certificate intent and CSR, policy version, issued-trust snapshot hash, verification instant/validity deadline/clock epoch, status/CT evidence versions, and crypto-provider capability identity. Persistable verification records include stable invalidation reason codes but are audit facts only, never transition capabilities. | Compile-fail, lifetime, serde, mutation, and replay tests prove capabilities cannot outlive or mismatch their session, survive restart, be restored from audit data, or remain usable after trust, policy, the time window/clock epoch, status, CT logs, algorithms, provider capabilities, or input bytes change; every invalidation path records a stable reason. |

## Phase 4 - Deterministic And Durable Lifecycle

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.32.0` | Deterministic account/directory/order command-state-policy-event reducer through order creation and recovery, emitting typed effects and obligations without async, I/O, time, entropy, signing, or storage calls. | Exhaustive transitions, Kani legality harnesses, and replay of the `v0.4.1` valid/invalid traces pass with no backend dependency or fabricated success path. |
| `v0.32.1` | Authorization/challenge presentation, validation, polling, cleanup, retry, and operator-action reducer slices with cleanup as an orthogonal durable obligation. | Every challenge lifecycle, CA/local-status combination, terminal transition, crash/retry trace, and retained cleanup receipt is covered by generated/model traces. |
| `v0.32.2` | CSR/finalization/certificate download/verification state slices including unknown outcomes. | Certificate cannot advance to verified without all prior evidence; reconciliation traces pass. |
| `v0.32.3` | Deployment/renewal/completion, cancellation, recoverable/terminal failure, and cleanup-required state slices. | Terminal-state and cancellation/cleanup precedence properties pass. |
| `v0.33.0` | Canonical serializable snapshots, revisions, schema identifiers, forward migration contract, unknown-field policy, and an explicit prohibition on serializing validation, signer/secret binding, binding-attempt, signer/MAC request admission, signature/MAC verification, provider assertion/attestation, or effect-authority capabilities. In particular `SecretBindingAttempt`, `BoundMacKey`, `MacConsumerAdmission`, `VerifiedMac`, `ProviderAssertedMac`, and `CryptographicallyAttestedMac` are never snapshot fields. Snapshots may contain bounded lifecycle/inventory state, obligation sets, redacted audit records, binding-profile identifiers/receipt digests, durable peer-effect state, and invalidation inputs only. | Round-trip, version skew, corrupt snapshot, forbidden-capability serialization, symmetric/asymmetric lifecycle-obligation products, binding-attempt/assertion/attestation capability injection, audit-record substitution, active-record authority escalation, peer-effect state substitution, and migration fixture suites pass. |
| `v0.33.1` | Effect IDs and persist-before-effect transactional outbox with once-only result application. | Crash/restart after every persist/execute/result boundary is safe. |
| `v0.33.2` | Operation-specific unknown-outcome reconciliation and policy decision records. | Lost response simulations never convert uncertainty into false success or blind retry. |
| `v0.33.3` | Cross-version snapshot compatibility and migration harness using retained snapshots from every released schema revision. | Current readers replay every supported historical snapshot; incompatible, corrupt, downgrade, forward-version, and migration-chain cases fail according to the published policy. |
| `v0.33.4` | Snapshot restoration and capability-reconstruction boundary: restart restores lifecycle/inventory facts, audit/history, stable invalidation reasons, binding-profile/receipt metadata, durable peer-effect facts, and every pending obligation, never live validation, signer/secret binding, `SecretBindingAttempt`, signer/MAC admission, signature/MAC verification, provider assertion/attestation, or effect authority. Deployment, signing, EAB, and TSIG reconstruct their respective capabilities whenever the process, provider session, policy/issued-trust/status/CT/algorithm/key-or-secret health epoch, verification validity window/clock epoch, handle/public key, immutable secret identity/version, alias/version, peer trust, assurance profile, or bound input changes; reconstruction cannot automatically replay a mutating peer effect. | Crash-at-every-boundary and retained-snapshot tests start from persisted validation, binding, `Active`, `SecretBindingAttempt`/`BoundMacKey`-shaped hostile data, assertion/attestation receipts, peer-effect records, and deployment records; prove none authorize signing, EAB, TSIG, repeated peer mutation, or deployment effects; reject replayed audit/receipt fields and alias/handle/secret/effect substitution; preserve asymmetric and symmetric obligations through migration/replay; classify unavailable revalidation/rebinding without false activation; and require deterministic safe reconstruction even when durable context appears unchanged. |
| `v0.33.5` | Durable peer-binding effect orchestration and recovery for `v0.10.19`-`v0.10.21`. The store commits a stable binding-attempt intent before privileged provider MAC dispatch, then commits the exact immutable peer-effect envelope to the transactional outbox before network dispatch. Attempt consumption, outbox state, `DispatchKnowledge × OperationOutcome × BindingEvidence × ObservationStatus`, authenticated peer evidence, account/DNS mutation state, secret binding, source-secret retention/destruction, and lifecycle/obligation transitions are explicitly ordered and fenced. Recovery preserves every orthogonal axis, invokes operation-specific reconciliation, never reconstructs `SecretBindingAttempt`, never blindly resends a may-have-dispatched effect, and never repeats a mutating peer ceremony merely to rebuild runtime authority. Successful EAB account creation atomically or transactionally commits durable account state and peer binding before one-time secret disposition. A mutating TSIG confirmation records ownership, exact update identity, rollback/cleanup responsibility, and authoritative reconciliation; non-mutating authenticated probes are preferred. | Crash/restart, store-fault, outbox, fencing, Kani, Loom, exhaustive outcome-product, and hostile-peer tests stop before provider dispatch, after attempt consumption, before/after outbox commit, before/during/after network dispatch, after remote success or rejection, before local result/account/DNS/binding commit, and before source destruction. Fixtures cover positive definitely-unsent recovery into a wholly new attempt, lost EAB success, duplicate-account prevention, ambiguous DNS UPDATE, authenticated operation rejection without fabricated binding failure, unauthenticated errors, observation/reconciler unavailability without dispatch-state loss, stale workers, concurrent reconciliation, restart without repeated peer mutation, and one-time EAB disposition; no crash loses an obligation, source secret, remote-effect ownership record, outcome axis, or reconciliation path. |
| `v0.33.6` | Durable composed EAB account-creation execution and recovery for `v0.16.4`. Persist only redacted phase facts/digests and, after `OuterSignatureVerified`, the complete exact HTTP request bytes in the transactional outbox; transient MAC/signature evidence, binding attempts, and admissions are never serialized. Before provider MAC dispatch, positive unsent evidence permits closure and a wholly fresh attempt. After MAC consumption but before an outbox envelope, the architecture proves network dispatch was impossible, discards non-restorable artifacts, and starts fresh. An `OutboxCommitted` attempt dispatches only its stored bytes without rebuilding or re-signing. Once dispatch starts or may have started, recovery preserves ambiguity and reconciles the exact request/effect identity without resend. Authenticated `badNonce` records operation rejection but no secret mismatch, consumes the attempt, and rebuilds the entire composition with a fresh nonce, bootstrap attempt, inner MAC, signer admission, request/effect identities, and outbox record. Authenticated account success commits account state, `PeerConfirmed`, obligations, and once-only result application before EAB disposition. | Crash/fault injection at every typestate, provider, signing, durable-write, outbox, dispatch, response, reconciliation, account/binding commit, and secret-disposition boundary covers inner-MAC success/outer-sign failure, session invalidation, absent outbox proof, committed-unsent dispatch, lost account success, repeated response application, duplicate-account risk, authenticated `badNonce`, rate/contact/ToS rejection without content-mismatch inflation, unavailable observation/reconciliation, stale fencing, and restart. Tests prove no partially composed request is executable, no committed request is rebuilt, no may-have-dispatched request is blindly resent, no evidence is stitched, and no success or disposition transition is applied twice. |
| `v0.33.7` | System-wide rollback discontinuity and restored-store recovery model extending the `v0.4.18` outbox and `v0.4.19` lease/fencing models plus the snapshot contract. Every durable store advertises `RollbackProtected`, `RollbackDetecting`, or `RollbackUnprotected`; persisted `StoreEpoch`/`RecoveryEpoch` plus explicit `RestoreDetected` or operator `RestoreDeclared` inputs invalidate leases, fences, provider sessions, caches, and live authority, quarantine restored outbox entries, and require reconciliation/revalidation of trust, signers, secrets, challenge ownership/cleanup, key eligibility, deployment generations, and retirement state. An in-store epoch alone is not rollback detection because it rolls back with the store; external witness semantics are assigned to `v0.33.8`. Platforms without such evidence require operator-declared restoration or report detection unsupported. | Whole-database rollback fixtures and formal/Rust trace replay restore snapshots before/after every outbox dispatch, lease/fence, key retirement, challenge replacement/cleanup, deployment activation/retirement, and trust/provider update. Tests prove stale epochs cannot execute or clear obligations after a detected/declared discontinuity, may-have-dispatched entries are never blindly resent, restored eligibility/generations remain quarantined, every authority is reconstructed, in-store epochs never overclaim detection, unsupported detection is honest, and operator declaration deterministically enters the same recovery path. |
| `v0.33.8` | Injected external `RollbackWitness` protocol outside the durable store's failure domain. Versioned profiles bind witness implementation/identity/session, authenticated store identity and state-head digest, monotonic counter or journal semantics, update batching/detection window, witness-key rotation, and the exact two-system commit/recovery protocol. Startup policy rejects `RollbackUnprotected` for high assurance unless an operator explicitly selects a named reduced-assurance profile; operator-declared restore is recovery input, not protection against unnoticed rollback. | Deterministic model, two-system crash, split-brain, clone, and hostile-witness tests cover store commit before witness advance, witness ahead/store ahead, lost/reset/rolled-back/cloned witness, concurrent writers, partition, bounded batching windows, state-head substitution, store/witness identity mismatch, key rotation at every boundary, unavailable witness, and reduced-assurance opt-in. Recovery never invents atomicity, high assurance never starts without admitted external evidence, and every uncertain relation quarantines effects/authority for reconciliation. |
| `v0.34.0` | Store transaction/load/commit/compare-and-swap traits returning bounded assertions/observations, with distinct `NotFound`, unavailable, corrupt, conflict, and `StoreAssertedCommit` outcomes plus an in-memory reference implementation. Only the `v0.6.6` facade/policy assurance path can promote a matching assertion into `QualifiedDurableCommit`; this qualifies the configured adapter contract and does not independently prove physical media synchronization. | Load-or-default is impossible; stale/replayed writes, unavailable-versus-absent confusion, cross-session/request/digest substitution, assertion-to-qualified-receipt overclaim, receipt forgery, malicious adapters, and atomicity model tests pass with the residual store TCB assumption published. |
| `v0.34.1` | Lease, fencing-token, challenge-ownership, asymmetric-key and symmetric-secret onboarding/reconstruction/reconciliation ownership, and single-flight store traits. | Lease theft, expiry, stale owner, duplicate key/secret reconciliation, concurrent signer/secret revalidation and rebinding, source-secret destruction races, obligation clearing, and fencing interleavings fail safely in property tests and Loom models introduced with the shared primitives. |
| `v0.34.2` | Encrypted-field/key-reference boundary and tenant/intent/account namespace isolation. | Ciphertext/key-reference substitution and cross-namespace access tests pass. |
| `v0.35.0` | Complete in-memory store, outbox, leases, cleanup queue, deterministic clock behavior, and fault hooks. | Reference-store conformance suite covers every store trait and fault. |
| `v0.35.1` | Durable filesystem snapshot/journal store with restrictive permissions and symlink/reparse-point-safe paths. | Cross-platform path/race/permission and corrupt/truncated journal tests pass. |
| `v0.35.2` | Filesystem store fsync/atomicity, crash recovery, backup/restore, and schema migration. | Power-loss injection at every write/rename/fsync/migration stage passes. |
| `v0.36.0` | Filesystem deployment staging with bounded files, ownership/mode/ACL policy, safe paths, fsync, and atomic certificate/key generation layout. | No crash/race exposes partial or mismatched active material. |
| `v0.36.1` | Deployment generation activation, stale-worker fencing, previous-generation retention, and deterministic rollback. | Stale activation and rollback-after-failure matrices pass. |
| `v0.36.2` | Service reload callback/control-channel interface and post-activation certificate/key/TLS health evidence. | Failed/hung/replayed reload and health checks trigger correct rollback without losing the serving generation. |
| `v0.36.3` | Optional OCSP-staple deployment artifact contract: certificate, key, chain, verified staple bytes, staple expiry, and status-evidence identity are staged and activated as one fenced generation; deployers receive bytes but cannot construct status proof. | Crash/race/partial-write/wrong-certificate/wrong-evidence/stale-generation tests prove no certificate is activated with a mismatched, unverified, expired, or separately committed staple and rollback restores a complete prior generation. |
| `v0.37.0` | Stable low-level workflow loop, typed effects/events/obligations, local-process static executor contracts, optional `alloc` object-safe adapters, and checkpoint contract; async remains entirely in local executors and `v0.4.26` forbids a generic remote executor or serialized live authority. | Fake blocking, async, polling, and embedded executors cover every effect/result, reject unauthorized or wrong-purpose evidence, produce identical reducer semantics, and compile/serde negatives prove only later purpose-specific remote protocols cross a process boundary and return observations rather than local evidence. |
| `v0.37.1` | High-level `ensure_certificate`, begin/resume/status/cancel, idempotent intent, and result APIs. | End-to-end fake platform covers success, recovery, cancellation, and every typed terminal outcome. |
| `v0.37.2` | Manual challenge workflow, operator instructions/evidence, confirmation, cleanup, and resume. | Manual-flow misuse, stale confirmation, restart, and cleanup tests pass. |
| `v0.37.3` | High-level account/contact/rollover/deactivation, revoke, renew-now, and ARI inspection API shells over completed protocol operations. | Compile/API misuse and behavior tests cover every public method without placeholder success. |
| `v0.37.4` | Reusable adapter conformance framework and semantic fault catalog for transport, signer, store, clock, entropy, identity issuer, challenge presenter, DNS provider, deployer, and `v0.4.26` purpose-specific remote protocol boundaries; every later adapter must register and pass its applicable suite. | Reference good/bad adapters prove the suites detect fabricated responses, redirect/effective-URL mismatch, signature-format/purpose errors, identity collision/use-before-persist, load-or-default, nondurable commits, clock/entropy fallback, forged ownership, stale generations, ambiguous-outcome misclassification, remote schema/capability downgrade, replay/reordering/duplication, cross-session/context substitution, false dispatch acknowledgement, and remote evidence minting. |
| `v0.37.5` | Reusable composed-authenticated-effect conformance for `v0.10.21`, `v0.16.4`, and `v0.33.6`, mandatory for every signer, MAC provider, ACME transport, outbox/store, and executor combination that advertises EAB account creation. It standardizes consumed EAB typestates, exact cross-layer identity/digest binding, positive inner-MAC and outer-signature evidence, orthogonal dispatch/operation/binding/observation outcomes, committed-byte dispatch, complete `badNonce` reconstruction, reconciliation, fencing, once-only result application, redaction, and capability-free persistence. Unsupported combinations fail before onboarding and never splice another provider or transport. | Generated good/bad adapter compositions substitute every account intent/contact/ToS/directory/URL/JWK/key session/EAB secret/algorithm/inner JWS/nonce/admission/request/effect/final-byte field; fail between every typestate and persistence boundary; return wrong-key or unverified MAC/signature evidence; lose, duplicate, reorder, or replay transport results; fabricate binding mismatch from rate/contact/ToS/DNS rejection; erase may-have-dispatched state during unavailability; rebuild committed bytes; reuse old MAC with a fresh nonce; double-apply success; or select fallback. The shared suite detects every fault and retains immutable regression fixtures for future adapters. |
| `v0.38.0` | Minimal deterministic in-house ACME test server implementing successful RFC 8555 directory/account/order/HTTP-01/finalization/retrieval/revocation paths. | Full local happy-path lifecycle is reproducible with no external service. |
| `v0.38.1` | Deterministic hostile ACME response catalog: malformed/bounded HTTP/JSON/PEM/DER, stale/reused nonces, contradictory/skipped states, wrong/effective/cross-origin URLs, content-type/header faults, incorrect certificates/chains, clock faults, and oversized bodies. | Every catalog fault has a client assertion, budget expectation, typed failure/ambiguity classification, and cannot panic or silently weaken policy. |
| `v0.38.2` | Ambiguous-outcome/crash/distributed fault server: lost responses, delayed/reordered effects, repeated operations, worker death, lease theft, and failure at every persistence/effect/result/cleanup boundary. | Restart after every transition/effect and every scripted persistence failure produces the modeled safe outcome without nonce restoration, false success, duplicate activation, or lost cleanup. |
| `v0.38.3` | Minimal test-only `std` HTTP/TLS executor implementing the `v0.14.2` transport contract for loopback hostile-CA and Pebble suites, explicitly not a production transport claim. | The shared transport conformance suite plus loopback TLS/root/name/timeout/cancellation/redirect/body-limit tests pass; feature and packaging checks prove it is absent from portable/default production graphs. |
| `v0.38.4` | Production `std` wall/monotonic clock adapter with explicit construction, conversion provenance, suspend/resume and backward-clock detection, monotonic-deadline behavior, and platform-unavailable failure. | Injected/native tests cover wall jumps, monotonic continuity, suspend, restart, overflow, skew thresholds, unavailable clocks, and prove no retry/deadline logic silently substitutes one clock domain for another. |
| `v0.38.5` | Private thin real-boundary vertical slice connecting directory discovery, response-nonce harvesting, account intent, exact JWS admission/signature verification, durable outbox commit, the test-only rustls executor, hostile local ACME HTTP, lost-response reconciliation, and capability-free restart. It validates architecture and exact bytes before production transports or public provider claims. | One deterministic account creation succeeds through real HTTP/TLS and real test crypto; fault injection before and after every reducer/store/sign/dispatch/response boundary proves exact committed bytes are reused only when authorized, may-have-dispatched is reconciled without blind resend, response nonces are independently harvested, and restart reconstructs rather than deserializes authority. |
| `v0.39.0` | Pinned Pebble integration for directory, account, order, HTTP-01, finalization, retrieval, revocation, and pagination. | Zero skipped mandatory RFC 8555 happy-path cases supported by Pebble. |
| `v0.39.1` | Pebble randomized-state, badNonce, alternate behavior, retry, and order-list robustness suite. | Repeated seeded runs are reproducible and all expected variants pass. |
| `v0.39.2` | Prior-release differential protocol harness comparing normalized requests, responses, state transitions, errors, and security decisions with retained Vardheim release evidence. | Every unexplained wire or semantic difference blocks release; approved corrections preserve both the old regression case and the newly required result. |
| `v0.39.3` | Existing certificate adoption: import and locally verify certificate, private-key/public-key binding, chain, identifiers, profile, validity, issuer/directory/account association, deployment target, and provenance; classify as renewable, replacement-required, or unmanaged. | Mismatched/missing key, wrong chain/SAN/profile/issuer/account, expired/not-yet-valid, unknown provenance, unsupported algorithm, cross-tenant, and already-managed cases fail or classify explicitly without issuing or deploying new material. |
| `v0.39.4` | Renewal bootstrap from adopted material: derive durable intent and schedule, use ARI later when available, avoid unnecessary immediate issuance, preserve current serving generation, and require policy for unknown account/issuer or near-expiry emergency replacement. | Clock/restart/idempotency tests prove stable scheduling, no duplicate immediate order, correct emergency threshold, no credential guessing, and serving continuity for valid adopted certificates. |
| `v0.39.5` | Durable multi-issuer orchestration and migration: persist explicit issuer choice, isolate accounts/nonces/ToS/EAB/trust/rates, propose rather than perform failover, and model approved migration/replacement without credential crossover. | Crash/retry and policy tests prove no silent CA switch, alternate-issuer order, account/key reuse, or trust/profile downgrade; approved migration retains audit provenance and serving continuity. |

## Phase 5 - Renewal, DNS, And TLS-ALPN

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.40.0` | RFC 9773 ARI identifiers, unauthenticated renewal-info GET, suggested-window validation, explanation URL handling without auto-fetch, Retry-After, and replacement links. | RFC vectors and malformed AKI/serial/window/URL/server-response tests. |
| `v0.41.0` | Durable ARI renewal state and cryptographically random time selection persisted within a validated suggested window. | Injectable RNG/clock properties prove selected times remain stable across restart and within the window. |
| `v0.41.1` | Non-ARI fallback scheduling, jitter, emergency threshold, clock-skew detection, retry checks, replacement/expiry/revocation stop conditions. | Failure/skew/restart properties prove no missed emergency renewal or renewal storm. |
| `v0.41.2` | Certificate-key compromise response: mark the key unusable, preserve the currently serving certificate only according to explicit emergency policy, generate/select a non-compromised replacement key, prioritize replacement issuance, order revocation safely, and retain operator/audit evidence. | Compromised-key reuse is impossible for CSR, deployment, renewal, or revocation except the explicitly allowed RFC revocation path; outage/crash/failed-replacement tests preserve serving continuity without declaring the compromised material safe. |
| `v0.41.3` | Account-key compromise or loss response: suspend new mutations, distinguish suspected compromise from unavailable/lost key, perform standards-compliant rollover only when old/new authorization is possible, refuse fabricated recovery, support new-account/issuer migration policy, and record operator evidence. | Wrong/lost/compromised signer, partial rollover, CA outage, revoked account, new-account migration, and restart tests prove no automatic fallback, key reuse, identity takeover, or false recovery claim. |
| `v0.41.4` | Certificate generation retirement state machine: Active to Superseded to Retired/Revoked/Destroyed, explicit overlap/grace policy, deployment-target removal acknowledgements, and serving-generation fencing during replacement. | Crash/retry/partial-target tests prove replacement overlap is bounded, removal is acknowledged per target, active material is never removed early, and terminal states cannot reactivate. |
| `v0.41.5` | Superseded key and artifact retention policy consuming the `v0.10.9` disposition contract: archive versus destruction decisions, provider-specific disable/scheduled-delete/retention outcomes, expired certificate/chain cleanup, backup implications, legal hold, pending reconciliation, and failure recovery. | Wrong-generation/tenant/key request, partial or scheduled deletion, disable-only provider, retained-by-policy, not-found without prior receipt, ambiguous/unavailable HSM/KMS, restore, duplicate cleanup, and retention-expiry tests prove active or held material cannot be disposed and only `Destroyed` evidence can report physical destruction. |
| `v0.41.6` | Certificate inventory reconciliation and export API across managed, adopted, unmanaged, active, superseded, retired, revoked, expired, orphaned key, orphaned certificate, and orphaned deployment states. | Store/deployment scan permutations, missing/corrupt records, duplicates, cross-tenant artifacts, stale targets, redacted human/machine exports, and repair-plan idempotency tests pass without automatic destructive reconciliation. |
| `v0.41.7` | Post-deployment revocation/status monitoring and replacement response using configured OCSP/CRL evidence, freshness schedules, policy escalation, emergency replacement, target continuity, and audit/operator notifications. | Revoked/unknown/stale/outage/soft-hard-fail transitions, restart, false responder data, replacement failure, and multi-target tests never silently keep a policy-forbidden certificate or remove the last serving generation. |
| `v0.41.8` | RFC 7633 TLS Feature/Must-Staple lifecycle policy: requested/returned feature binding, local server-capability check, and mandatory fresh locally verified OCSP evidence before activation when `status_request` is required. | Unsupported/critical/changed features, missing/stale/unknown/revoked OCSP, wrong certificate/responder, clock rollback, restart, and alternate-chain cases fail closed without removing the last valid serving generation. |
| `v0.41.9` | Durable OCSP staple refresh state machine with refresh-before-expiry scheduling, acquisition/verification obligations, retry/backoff, hard-fail policy, activation fencing, rollback, and last-serving-generation continuity. | Crash at every fetch/verify/stage/activate boundary, responder outage, absent `nextUpdate`, maximum-age expiry, clock jump, stale worker, repeated response, and replacement failure cannot serve an expired required staple or falsely report refresh success. |
| `v0.41.10` | High-level lifecycle facade methods for retire, unmanage, inventory/reconciliation, deployment-target removal, artifact disposition, and Must-Staple/status inspection over the completed reducer/state boundaries. | Compile/API and end-to-end fake-platform tests cover authorization, idempotency, partial targets, active-generation protection, dry-run repair plans, unavailable status, and every typed terminal outcome without bypassing evidence or cleanup obligations. |
| `v0.41.11` | Durable CT inclusion-monitoring state machine for activated certificates and every accepted v1/v2 SCT: MMD-derived due times, STH/inclusion/consistency acquisition obligations, verified audit evidence, retry/backoff, log outage/disqualification/closure policy, operator notification, and long-term checkpoint retention. | Crash at every schedule/fetch/verify/checkpoint boundary, missing-after-MMD inclusion, stale/future/rolled-back STH, inconsistent tree, log outage/disqualification/closure, certificate replacement/retirement, clock jump, duplicate SCT, v1/v2 coexistence, and stale worker tests cannot falsely report inclusion or lose a pending audit obligation. |
| `v0.41.12` | Optional independent CT witness/gossip evidence and split-view policy: typed witness identity/trust, signed checkpoint observations, quorum/diversity/freshness requirements, cross-view consistency comparison, privacy/redaction, unsupported/unavailable classification, and escalation without treating witnesses as log-signature substitutes. | Fake independent witnesses cover matching and conflicting checkpoints, equivocation, stale/replayed/forged observations, shared-operator diversity failure, quorum loss, partition, witness compromise/removal, v1/v2 mismatch, privacy policy, restart, and prove gossip evidence cannot fabricate SCT, STH, inclusion, or consistency verification. |
| `v0.42.0` | DNS-01 calculation and provider trait with exact TXT value, normalized owner, record handle/lease, concurrent-value preservation, and exact cleanup. | RFC vectors and properties proving concurrent workflows cannot remove unrelated TXT values. |
| `v0.42.1` | Provider-neutral bounded DNS query contract before authoritative discovery: typed names/classes/types, per-attempt unpredictable transaction IDs, UDP/TCP result evidence, deadlines/cancellation, response ID/question/source/transport/server binding, DNSSEC record preservation, and definite/ambiguous/unavailable classifications. | Deterministic fake query/entropy tests reject reused or predictable IDs, mismatched ID/question/source/server/transport, malformed compression, oversized sections, spoofed answers, fallback resolver changes, and unbounded retry; no concrete DNS library enters core/challenge crates. |
| `v0.42.2` | Deterministic DNS-query simulator and shared conformance suite for authoritative discovery, propagation, DNSSEC, CAA, and provider self-check consumers. | Scripted delegation, split authority, IPv4/IPv6, truncation, timeout, reordering, stale cache, bogus signatures, and resolver disagreement scenarios produce stable typed evidence and catch intentionally faulty query adapters. |
| `v0.42.3` | Bounded EDNS(0) OPT construction/parsing with version, extended RCODE, DO flag, bounded advertised UDP payload, unknown option retention, malformed/duplicate OPT rejection, and persistent differential fuzzing. | RFC 3225/6891/8914 vectors plus malformed owner/type/class/version/flags/options, BADVERS, extended RCODE, duplicate OPT, short-buffer, exact-capacity, and parser-differential cases pass. |
| `v0.42.4` | DNS/EDNS query-attempt policy: DO for local DNSSEC material, explicit DNS-header CD/AD semantics with AD never trusted alone, fragmentation-avoidance payload selection, mandatory TCP retry on TC/truncation, attempt-scoped IDs, and bounded server-specific fallback only when required semantics remain available. | UDP/TCP/EDNS-intolerant simulations prove no truncated answer is accepted, DNSSEC-required queries never silently lose DO/OPT, AD cannot construct evidence, fallback cannot change server or trust semantics, oversized UDP is not advertised, and retries use fresh IDs within one parent budget. |
| `v0.42.5` | UDP DNS source-port and complete response-correlation policy: unpredictable source-port selection or bounded socket rotation, explicit local-address/port capture, and binding to local tuple, remote tuple, transaction ID, question, transport, attempt, and selected server. | Fake socket/entropy and loopback tests cover fixed/low-entropy ports, port reuse beyond policy, NAT-observed tuple handling, wrong local/remote address or port, delayed prior-attempt packets, server changes, ID collisions, and source-port exhaustion without accepting a partially matched response. |
| `v0.42.6` | Bounded DNS-over-TCP codec and correlation: fresh retry IDs, two-octet length framing, partial prefix/body reads, multiple and out-of-order frames, connection identity, in-flight ID/question uniqueness, EOF/timeout handling, and bounded reuse/pipelining policy. | RFC 7766 simulations cover split/coalesced frames, zero/oversized lengths, slow partial frames, premature EOF, extra/trailing messages, reordered responses, reused in-flight IDs, wrong question/connection/server, UDP-ID replay over TCP, cancellation, and connection-budget exhaustion. |
| `v0.42.7` | Optional RFC 7873/9018 DNS Cookies profile with typed client/server cookies, secret rotation, server-specific state, BADCOOKIE handling, bounded retry, privacy policy, and no substitution for transaction/tuple/DNSSEC/TSIG validation. | Official/independent and simulated fixtures cover initial negotiation, valid/invalid/missing/malformed cookies, BADCOOKIE retry, anycast/server change, secret overlap/retirement, replay, downgrade, privacy-disabled mode, and prove cookies cannot relax response correlation or construct DNSSEC/update evidence. |
| `v0.43.0` | Authoritative zone and server discovery with label walking, CNAME/NS delegation representation, and bounded loop detection. | Delegation loops, lame/missing zones, split authority, and malformed response fixtures pass. |
| `v0.43.1` | DNS-01 propagation checks against every authoritative server over IPv4/IPv6 with explicit partial/complete visibility evidence. | Partial propagation, inconsistent answers, timeout, truncation/TCP fallback, and unreachable-family matrix passes. |
| `v0.43.2` | TTL, negative-caching, bounded wait/backoff, recursive comparison, and self-check deadline policy. | NXDOMAIN/NODATA cache and stale/early visibility simulations cannot report false global readiness. |
| `v0.43.3` | Complete bounded authoritative discovery semantics for DNS self-check: distinguish authoritative answers, downward referrals, partial answers/referrals, and lame/upward responses using AA/sections/zone cuts/SOA ownership; process CNAME and RFC 6672 DNAME with synthesized CNAMEs; validate in-domain, sibling, and out-of-bailiwick glue; resolve missing nameserver A/AAAA iteratively without ambient system resolution; and retain per-server provenance. | RFC 1034/1035/2181/2308/6672/9471/9499 simulations cover AA misuse, wrong-zone SOA, parent/child NS disagreement, missing/extra/inconsistent glue, glue poisoning, cyclic/out-of-bailiwick nameserver dependencies, bounded A/AAAA lookup, CNAME/DNAME/referral loops, synthesized-name overflow, lame delegation, unreachable authority, split IPv4/IPv6, and exact work/depth/server limits without accepting recursive-resolver assertions as authority. |
| `v0.44.0` | DNSSEC validation model and resource policy: typed DNSKEY/DS/RRSIG/NSEC/NSEC3 records, secure/insecure/bogus/indeterminate outcomes, current IANA algorithm/digest policy, and caller-provided RRset/chain/proof workspaces. | Registry and capacity tests reject unknown/prohibited/downgraded algorithms, unbounded record sets, cross-zone/type confusion, and any secure result created without cryptographic validation or explicitly authenticated resolver evidence. |
| `v0.44.1` | Canonical DNSSEC owner-name and RRset serialization for signature verification, including wildcard-label reconstruction, canonical ordering, original TTL, exact caller-buffer sizing, and a persistent differential fuzz target. | RFC 4034 vectors, mixed-case/compression/order/TTL/wildcard boundaries, short buffers, overflow, independent implementation comparisons, and retained fuzz regressions pass byte-for-byte. |
| `v0.44.2` | Bounded DNSKEY, DS, and RRSIG parsing plus key-tag/digest input construction with exact wire-field validation and unknown-value retention. | RFC vectors and mutation/differential corpora cover malformed keys/signatures/digests, labels, signer names, duplicates, unsupported algorithms, truncation, and exact workspace exhaustion. |
| `v0.44.3` | Local DS-to-DNSKEY digest and key-tag verification through purpose-bound provider capabilities and current IANA validation policy, including legacy SHA-1 only where mandated. | Valid/broken DS matrices, key-tag collision, digest mismatch, prohibited/unknown digest, downgrade, wrong owner/key flags, and wrong-purpose SHA-1 tests fail closed. |
| `v0.44.4` | Local RRSIG cryptographic verification over canonical RRsets through purpose-bound RSA/ECDSA/EdDSA capabilities and current IANA validation policy. | Valid/broken algorithm matrices, malformed keys/signatures, signer/labels/type/class/TTL mismatch, revoked/zone-key flags, format confusion, downgrade, and cross-purpose tests fail closed. |
| `v0.44.5` | Root-to-leaf DNSSEC chain construction across delegations, secure/insecure transitions, signed DNSKEY/DS RRsets, islands of trust, loops, and bounded caller-provided chain/visited storage. | Root, signed delegation, unsigned delegation, missing DS/DNSKEY, algorithm rollover, island, loop, contradictory authority, and exact-capacity fixtures produce deterministic classifications. |
| `v0.44.6` | DNSSEC signature inception/expiration and serial-number time arithmetic with explicit wall-clock provenance, skew policy, TTL/evidence lifetime, and no monotonic/wall substitution. | Wraparound, future/expired signatures, clock jumps, skew bounds, TTL expiry, restart, and ambiguous-time fixtures cannot accept stale or not-yet-valid evidence. |
| `v0.44.7` | RFC 4035 NSEC authenticated-denial validation for NXDOMAIN/NODATA, closest encloser, next-closer, wildcard denial/existence, delegation, and bitmap semantics using bounded proof workspaces. | Positive/negative/wildcard/delegation corpora reject incomplete, contradictory, cyclic, wrong-zone, malformed-bitmap, and opt-out-confused proofs. |
| `v0.44.8` | RFC 5155 NSEC3 authenticated denial: hashed-owner construction, closest-encloser/next-closer/wildcard proofs, opt-out semantics, salt handling, and RFC 9276 iteration/CPU budgets using purpose-bound SHA-1 only. | Official/independent NSEC3 vectors, empty/nonempty salts, opt-out delegations, excessive iterations, hash collisions, malformed chains, incomplete proofs, and exact work-budget exhaustion fail deterministically. |
| `v0.44.9` | DNSSEC trust-anchor provisioning, identity/version hashing, manual updates, and optional RFC 5011 add/revoke/remove hold-down rollover state with explicit operator policy and durable audit. | Initial trust, add/remove/revoke, clock/restart, key disappearance, compromised/all-lost anchors, rollback, substitution, and hold-down boundary tests never silently trust an unproven anchor. |
| `v0.44.10` | Validating-resolver evidence policy: local validation is default; authenticated, identity-bound resolver evidence may be accepted only by explicit policy, while unauthenticated DNS responses and the AD bit alone are never trust evidence. | Spoofed AD, downgraded transport, wrong resolver identity, stale/cache-substituted evidence, policy changes, split resolvers, unavailable validation, and local-versus-remote disagreement classify without false secure results. |
| `v0.44.11` | Integrated DNSSEC self-check engine connecting query evidence, chain/signature/time/denial validation, trust-anchor state, and operator policy into bounded secure/insecure/bogus/indeterminate results. | End-to-end signed/unsigned/broken-chain/rollover/NSEC/NSEC3/wildcard/downgrade/clock/budget fixtures cover every result and prove no partial validator stage can report readiness. |
| `v0.44.12` | Isolated production `std` DNS-query adapter implementing `v0.42.1`-`v0.42.7` and `v0.43.3` with explicit resolver/socket/runtime construction, entropy-backed IDs/source ports, exact tuple binding, EDNS(0), DO/CD policy, bounded UDP payload, complete TCP framing/correlation, optional DNS Cookies, iterative authoritative/referral/glue/DNAME discovery, DNSSEC record preservation, and no system-resolver semantic fallback. | Shared DNS/EDNS/TCP/Cookie/discovery conformance plus loopback authoritative tests cover ID/port reuse and spoofing, tuple mismatch, referral/glue poisoning, DNAME synthesis, lame/unreachable authority, extended RCODE/BADVERS/BADCOOKIE, EDNS/cookie intolerance, truncation/partial TCP frames, reordering, fragmentation policy, timeout, IPv4/IPv6, cancellation, unsupported targets, and dependency admission. |
| `v0.44.13` | Pinned Pebble/challtestsrv DNS-01 integration over completed DNS presentation, concrete query, propagation, and local DNSSEC validation boundaries. | Complete DNS-01 issuance, concurrent TXT, delegation, cleanup, NSEC/NSEC3/DNSSEC/query failures, and restart paths pass using genuine cryptography. |
| `v0.45.0` | Provider-neutral RFC 2136 UPDATE wire/state model with zone section, prerequisites, update operations, response/RCODE handling, exact record ownership, retry/ambiguity classification, and no concrete network or MAC backend claim. | RFC vectors and deterministic authoritative simulations cover malformed sections, wrong zone/class/name/type/value, prerequisite races, duplicate/partial updates, ambiguous response loss, replay, cleanup ownership, budgets, and continuous parser fuzzing. |
| `v0.45.1` | RFC 8945 TSIG record and canonical MAC-input construction for requests, responses, errors, and multi-message chains through `v0.10.16`-`v0.10.21`: typed key name/algorithm/purpose, transactionally onboarded immutable secret identity/version, narrow quarantined `SecretBindingAttempt` for an exact peer-confirmation probe or update, fresh runtime `BoundMacKey` only after admitted binding, signed time/fudge, original ID, exact request identity, prior-MAC/chaining state, output/truncation policy, single-use admission, and purpose-specific positive MAC evidence. | RFC 8945 and independent vectors plus continuous differential fuzzing cover wrong-secret import, bootstrap-cycle and arbitrary-byte negatives, restart/rebinding, compression/canonical-name errors, secret-name/version retargeting before attempt/admission/dispatch/verification, cross-zone/tenant/purpose key substitution, valid wrong-key MAC, prior-MAC chaining, wrong original ID/time, truncation and malformed output, verifier unavailable, assertion/attestation-policy downgrade, attempt/admission reuse, short buffers, duplicate/misplaced TSIG, and EAB-purpose confusion; raw or unverified MAC bytes cannot form a TSIG record. |
| `v0.45.2` | TSIG response verification policy through the same freshly reconstructed `BoundMacKey` and exact request/chain evidence: authenticate the request MAC before response use, bind response verification to immutable secret identity/version, admitted content-binding mode, request MAC, server, time, algorithm, truncation policy, and multi-message state, and accept no unsigned or merely unverified success. | Wrong/missing/truncated MAC, secret retargeting between request and response, restored/recreated secret object, wrong binding mode/key/version/algorithm/time/fudge/request MAC/server/chain, signed/unsigned error permutations, replay, provider-session change, verifier unavailable, weaker assertion/attestation policy refusal, crash/retry, and admission/evidence substitution fail closed without treating an unauthenticated update as success. |
| `v0.45.3` | TSIG secret lifecycle and rotation integrated with `v0.10.16`-`v0.10.21`: transactional create/import/adopt, typed content binding, immutable versioned identities, discovery aliases, narrow binding attempts, authenticated non-mutating peer probes where supported, explicit ownership/rollback/reconciliation for mutating confirmation UPDATEs, activation/overlap/retirement windows, live `BoundMacKey` invalidation/reconstruction, consumed-attempt/admission handling, source/target minimum retention, destruction evidence, redacted audit, and crash-safe update-worker adoption. Durable execution uses `v0.33.5` before production activation. | Wrong-secret/zone/key/version selection, lost/duplicate onboarding, alias retargeting, premature source or target deletion, excessive overlap, stale binding attempt/bound key/admission/worker use, rollback, partial rotation, authenticated probe rejection, unauthenticated errors, ambiguous confirmation UPDATE, missing ownership or reconciliation, lost destruction receipt, restart without repeated mutation, backup/restore/object resurrection, and secret-formatting tests pass without silently reusing old MAC authority. |
| `v0.46.0` | Cloudflare DNS adapter with narrowly scoped credentials, pagination, exact record IDs, rate limits, and redacted errors. | Mock and controlled integration tests for duplicate values, partial failures, revoked credentials, quotas, and cleanup. |
| `v0.47.0` | AWS Route 53 adapter isolated from core, including hosted-zone resolution, change polling, credential-provider boundary, and exact cleanup. | Local/mocked AWS protocol suite and opt-in controlled integration; dependency and SDK admission report. |
| `v0.48.0` | Azure DNS adapter isolated from core, including zone/resource IDs, identity boundary, ETags/concurrency, and exact cleanup. | Mock Azure protocol suite and opt-in controlled integration; dependency and SDK admission report. |
| `v0.49.0` | Remote least-privilege DNS validation agent wire protocol with authenticated capability requests, restricted names/values, expiry, replay defense, and audit receipts. | Protocol properties prove requests cannot authorize account access, issuance, or unrelated DNS edits. |
| `v0.49.1` | Reference purpose-specific DNS agent protocol/executor through `v0.4.26`: canonical bounded effect/observation encoding, schema/capability negotiation, mutual authentication, endpoint/tenant/policy/effect/request/generation/deadline/recovery-epoch binding, dispatch acknowledgement, enrollment/credential rotation, provider mapping, and recovery; the agent never receives serialized local authority or constructs qualified local evidence. | Compromise scenarios prove least privilege; malformed/version downgrade, disconnect, replay/reordering/duplication, cross-session/context substitution, false acknowledgement, expiry, recovery-epoch change, observation overclaim, and cleanup recovery pass. |
| `v0.50.0` | TLS-ALPN-01 identity construction per RFC 8737: dedicated ephemeral key handle, one SAN, critical `acmeIdentifier`, SHA-256 digest, exact certificate profile, current `v0.10.10` validation, `v0.10.11` TLS-challenge-role `BoundSigner`, single-use exact-request `v0.10.12` admission, immutable-identity dispatch, and locally constructed `v0.10.15` `VerifiedSignature`. No challenge-certificate bytes become presentable before verification. | RFC vectors plus wrong SAN/extension/criticality/digest/key, unvalidated or substituted public material, ephemeral identity replacement before/during dispatch/verification, valid wrong-key signature, malformed/DER-raw-confused output, verifier unavailable, wrong handle/role, cached/unrelated proof, expired binding, stale/replayed/substituted admission, provider-session change, and compile/API negatives pass without producing a usable challenge identity or permitting normal-certificate/account authority. |
| `v0.50.1` | TLS challenge identity store, expiry, presentation ownership, self-verification, and durable cleanup. | Concurrent/expired/stale-owner/crash cleanup cases cannot affect normal certificate identities. |
| `v0.51.0` | `vardheim-rustls` exact `acme-tls/1` SNI/ALPN challenge resolver with no normal-certificate fallback. | Handshake, concurrency, SNI confusion, ALPN downgrade, expired identity, and normal-traffic isolation tests pass. |
| `v0.51.1` | Pinned Pebble/challtestsrv TLS-ALPN-01 end-to-end integration through `vardheim-rustls`. | Successful issuance, invalid identity, cleanup, restart, and concurrent normal TLS traffic pass. |
| `v0.51.2` | rustls live certificate generation/reload consumer interface, SNI table update, activation health evidence, and rollback integration. | First issuance and renewal activate without process restart; failure leaves the prior SNI certificate serving. |
| `v0.51.3` | OpenSSL TLS-ALPN presentation adapter behind the same challenge identity contract, isolated from core. | OpenSSL handshake/SNI/ALPN/cleanup and normal-certificate isolation suite passes. |
| `v0.51.4` | rustls live OCSP staple consumer for fenced certificate generations, exposing verified staple bytes/expiry to the resolver without exposing status-evidence constructors. | Handshake and reload tests cover Must-Staple success, missing/expired/mismatched staple, concurrent SNI generations, refresh activation, stale worker, rollback, and normal non-stapled certificates. |
| `v0.51.5` | OpenSSL live OCSP staple deployment/refresh adapter behind the same fenced artifact contract. | OpenSSL handshake/reload tests cover callback lifetime, wrong certificate/staple, expiry, refresh race, process restart, failed activation, rollback, and no fabricated-status path. |

## Phase 6 - Transport And Cryptographic Backends

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.52.0` | Production transport adapter admission and configuration layer over the `v0.14.2` contract: connector/resolver/proxy/root-provider capabilities, bounded connection reuse, phase telemetry, credential isolation, runtime ownership, and uniform conformance requirements without selecting an implementation. | Reference adapter configurations and the shared transport suite reject hidden global runtimes, implicit roots/proxies/resolvers, cross-origin credential reuse, pool state crossing directory identities, configuration-time unsupported targets, and semantic drift from the early contract. |
| `v0.52.1` | HTTPS/TLS trust policy, custom roots, platform roots, minimum protocol, certificate/SPKI pinning, and peer identity evidence. | Wrong root/name/pin/version and trust-store failure matrices pass. |
| `v0.52.2` | Proxy authentication boundary, origin authorization enforcement, disabled bounded decompression policy, redirect prohibition, correlation, and credential-leak prevention. | Malicious proxy, redirect, decompression bomb, SSRF, cross-origin, and proxy credential tests pass. |
| `v0.52.3` | Production `std` `PublicPkiFetch` adapter implementing the isolation/SSRF portion of `v0.30.13` independently from authenticated ACME transport, with dedicated pools/configuration, HTTP/HTTPS policy, no ambient or proxy credentials, address-class filtering, DNS-rebinding defense, bounded redirects/bodies/deadlines, and purpose-keyed caches. | Shared fetch conformance and loopback tests prove ACME cookies/headers/tokens and proxy credentials never cross the boundary; HTTP delivery alone cannot authenticate OCSP/CRL/AIA/CT objects, and every returned body remains untrusted until local verification. |
| `v0.52.4` | Production custom issued-certificate trust provider for pinned DER/PEM bundles and explicit distrust overlays, with atomic reload, immutable snapshot hashing, tenant/profile constraints, and no coupling to ACME HTTPS roots. | File/memory source tests cover malformed/duplicate roots, partial writes, permissions, symlink/reparse races, distrust precedence, reload failure, rollback, concurrent readers, and no load-or-empty fallback. |
| `v0.52.5` | Production platform issued-certificate trust provider for supported operating-system root stores, normalized into immutable purpose-bound snapshots only when platform constraints, distrust, scope, and policy semantics are representable; otherwise return a typed unsupported result rather than claiming native-equivalent trust from flattened DER roots. | Native platform tests cover store unavailability/change, machine/user scope, distrust/removal, unrepresentable constraints, duplicate anchors, permission/service identity, snapshot drift, tenant policy, and prove platform ACME/TLS roots are not implicitly reused or native-equivalence claimed without explicit construction and preserved semantics. |
| `v0.52.6` | Production `PublicPkiFetch` HTTP framing, representation, privacy, and cache implementation for `v0.30.22`-`v0.30.24`, including POST-only OCSP mode, decompression policy, bounded chunks/trailers/1xx, strict CL/TE handling, media types, complete-body proof, and full cache partition keys. | Shared hostile HTTP and multi-tenant integration proves compression/framing/request-smuggling ambiguity, partial bodies, wrong media types, GET privacy leakage, and URL/method/body/tenant/purpose/policy cache mixing fail before bytes enter PKIX parsers. |
| `v0.52.7` | Production version-aware CT audit HTTP adapter implementing `v0.30.28` for v1/v2 STH, inclusion, consistency, and combined proof endpoints with dedicated pools, privacy controls, exact log/base-URL binding, bounded parsing handoff, checkpoint caching, and no ambient credentials. | Shared CT-fetch conformance plus hostile loopback logs cover endpoint/version substitution, redirect/rebinding, stale/future/rolled-back checkpoints, split-view responses, malformed/partial/oversized proofs, timeout/cancellation, cache crossover, privacy-disabled direct fetch, and prove returned bytes require local `v0.30.26`/`v0.30.27` verification. |
| `v0.52.8` | Transport protocol-version and replay-safety contract: explicit HTTP/1.1, HTTP/2, or HTTP/3 selection; TLS/QUIC early data disabled for every ACME mutation and by default for all ACME traffic; no implicit middleware/adapter retry; definitely-unsent versus possibly-processed outcomes for connect/write/GOAWAY/REFUSED_STREAM/reset/cancellation; and connection, TLS/QUIC session, DNS, proxy, trust, directory, tenant, and policy partitioning with origin coalescing prohibited unless separately proven. | Deterministic transport state models and faulty-adapter tests cover partial writes, lost responses, cancellation races, GOAWAY last-stream boundaries, REFUSED_STREAM misuse, reset after processing, TLS 0-RTT/HTTP 425, QUIC retry/migration, session resumption, resolver change, proxy/trust/policy rotation, cross-origin certificate coverage, hidden retry layers, and prove only protocol evidence can classify definitely-unsent while all other transmissions reconcile as ambiguous. |
| `v0.53.0` | Tokio + hyper + rustls asynchronous HTTP/1.1 transport with explicit executor construction, cancellation semantics, `v0.52.8` partitioning, early data disabled, and no automatic retry or protocol upgrade. | Loopback and hostile-server integration on Linux, Windows, BSD, macOS, Android, and iOS-capable CI tiers covers partial send/response loss/cancellation and proves HTTP/2 is not library-selected. |
| `v0.53.1` | Reqwest asynchronous HTTP/1.1 transport adapter with strict redirect/decompression/proxy policy parity, explicit rustls/native TLS construction, early data disabled, and hidden retry/upgrade layers disabled. | Differential transport suite proves policy-equivalent requests, errors, limits, cancellation, origin enforcement, transmission classification, and HTTP-version selection against hyper. |
| `v0.53.2` | Optional explicit RFC 9113 HTTP/2 ACME transport profile over admitted async adapters with bounded multiplexing, HPACK limits, disabled server push, no cross-origin coalescing by default, stream/connection identity, and exact GOAWAY/REFUSED_STREAM/RST_STREAM transmission classification under `v0.52.8`. | Hostile HTTP/2 integration covers header compression/resource attacks, stream-ID exhaustion, GOAWAY boundaries, forged/misused REFUSED_STREAM, reset/cancel races, connection error, origin/SAN overlap, tenant/directory/trust partitioning, concurrent requests, and differential HTTP/1.1 semantics without replaying possibly processed JWS requests. |
| `v0.53.3` | Optional explicit RFC 9114 HTTP/3 ACME transport profile over QUIC with pinned RFC 9000/9001/9002/9204 behavior, bounded QPACK and stream state, connection-ID/path-migration policy, TLS early data disabled, no cross-origin coalescing, and `v0.52.8` ambiguity semantics. | Hostile QUIC/HTTP/3 integration covers retry/version negotiation, migration/NAT rebinding, stateless reset, connection close, stream cancellation, QPACK blocking/decompression limits, session resumption, 0-RTT refusal, resolver/path change, partition crossover, and response loss without automatic replay of possibly processed requests. |
| `v0.54.0` | Blocking ureq-compatible HTTP/1.1 transport with strict policy parity, explicit rustls construction, early data disabled, and no implicit retries/upgrades. | Blocking timeout/cancellation/resource/redirect/origin, partial-send ambiguity, partitioning, and protocol-selection tests pass. |
| `v0.54.1` | Native TLS blocking HTTP/1.1 transport option isolated outside core, with platform roots, identity-policy parity, early data disabled, and no implicit retries/upgrades. | Native-root/hostname/proxy/timeout, partial-send ambiguity, partitioning, and version-selection behavior passes across supported desktop operating systems. |
| `v0.55.0` | Embedded/custom transport adapter and allocation-bounded response handoff for kernels, firmware, and future custom operating-system targets, with explicit protocol/version/replay capabilities and mandatory `v0.52.8` conformance. | Host-free compile profiles, caller-buffer exhaustion tests, deterministic fake transport, definite/ambiguous transmission cases, disabled early data, and no hidden runtime/retry/protocol assumptions pass. |
| `v0.56.0` | Explicit and separate capability sets for account JWS, EAB MAC, rollover, CSR signing, revocation, X.509 verification, TLS challenge-certificate signing, authority-token, audit, deployment, storage encryption, key generation, and ephemeral challenge roles with exact input/output formats. Every ordinary handle-backed signature-effect constructor requires `v0.10.11`-`v0.10.15` binding/admission/immutable-dispatch/`VerifiedSignature`; every handle-backed EAB or TSIG MAC effect requires `v0.10.16`-`v0.10.18` transactionally onboarded and freshly reconstructed `BoundMacKey`, single-use admission, immutable dispatch, and purpose-specific positive MAC evidence. Only the canonical `bind_signer` proof operation is exempt from ordinary signer admission. Verification-only and non-signature/non-MAC capabilities cannot satisfy either boundary. | Type/API tests prevent role/key/secret reuse, signer or MAC admission bypass/reuse, use of quarantined/unbound symmetric secrets, misuse of the narrow signer-binding exemption, mutable-locator dispatch, unverified provider-output promotion, provider assertion or cryptographic attestation silently becoming independently verified, EAB/TSIG crossover, MAC/signature or signing/verification confusion, DER/raw confusion, or claiming an algorithm for an unsupported purpose; new handle-backed effects cannot compile without their shared positive-evidence boundary. |
| `v0.56.1` | Purpose-aware capability negotiation across local provider, CA support, operator policy, certificate profile, signature/MAC/hash parameters, key generation, and verification needs. | Per-purpose matrices prove a shared algorithm name cannot imply support for another operation, and server-driven choice, parameter downgrade, cross-backend fallback, or partial capability claims fail closed. |
| `v0.56.2` | Operating-system and caller-supplied entropy adapters implementing the `v0.10.4` semantics, including platform availability/health evidence, fork/snapshot considerations, failure provenance, and explicit construction. | Native platform, injected failure, short-read, fork/snapshot/repetition, unsupported target, and no-fallback tests pass through the entropy conformance suite without hidden global RNG assumptions. |
| `v0.56.3` | Bounded SPKI/PKCS#8 import/export policy, key/algorithm verification, redacted errors, format-version handling, and mandatory `v0.10.13` quarantined onboarding for every imported private key before validation, binding, or activation. | Malformed/wrong algorithm/private-public mismatch/unsupported-format corpus plus duplicate import, lost import result, wrong returned handle/public key, validation/binding failure, restart, cleanup obligation, and no-premature-use tests pass. |
| `v0.56.4` | Encrypted software-key envelope, password/key-provider boundary, atomic account-key persistence, key versioning/rotation, backup, and recovery. | Wrong password/purpose/version, crash, permission, rollback, backup, and key substitution cases pass. |
| `v0.56.5` | Optional default-off migration import for bounded legacy PKCS#1 RSA and SEC1 EC private keys, with exact algorithm/curve/parameter/public-key validation and conversion into the provider's PKCS#8 or opaque-key lifecycle through `v0.10.13` quarantine; no legacy export or activation before validation and binding. | OpenSSL/independent fixtures cover malformed/encrypted/concatenated objects, weak RSA, invalid EC scalars/points, explicit or wrong curves, trailing data, key/public mismatch, unsupported providers, duplicate/lost import outcomes, restart, validation or binding failure, cleanup ambiguity, zeroization, redacted errors, and prove callers receive typed conversion-required/unsupported results rather than format guessing or a prematurely active key. |
| `v0.56.6` | Reusable key-disposition provider conformance and reconciliation framework for `v0.10.9`, including capability publication, native-state mapping, request idempotency, polling/backoff, terminal evidence, redaction, retention/legal-hold interaction, and mandatory application by every later software, HSM, TPM, KMS, remote, and platform key provider. | Intentionally faulty adapters are caught for destroyed-vs-disabled/scheduled/not-found confusion, cross-key receipts, forged terminal state, lost request IDs, rollback/resurrection, indefinite ambiguity, unavailable polling, secret leakage, and unsupported-operation fallback; every provider has an explicit supported-outcome table. |
| `v0.56.7` | Reusable public-key validation, `BoundSigner`, request-admission, and provider conformance framework for `v0.10.10`-`v0.10.12` and `v0.28.14`: algorithm-family rule tables; native pairwise-consistency or canonical `bind_signer` proof; local transcript verification; immutable provider-native identity/version and public-key-digest binding; bounded signer/session lifetime and permitted roles; local exact-request admission minting with bounded outstanding identities; destructive pre-dispatch consumption; provider/session/policy/key-health invalidation including in-flight dispatch races; redaction and capability publication; and mandatory application by every later software, HSM, TPM, KMS, remote, and platform provider. | Intentionally faulty providers are caught for invalid keys, wrong RSA/EC/EdDSA rules, mutable alias/label/name/reference promoted to authority, material-only evidence promoted to binding, arbitrary-byte or protocol use of `bind_signer`, recursive binding admission, cached/unrelated/cross-purpose proof signatures, forged native consistency, stale binding, request/role/nonce/format substitution, duplicate or excessive outstanding admissions, cross-session/tenant/handle/immutable-identity evidence, fallback, false success on unavailable validation or ambiguous binding, admission restoration after success/failure/cancellation/ambiguity, serialization/restoration, and failure to validate an issued leaf key that never verifies a signature. Deterministic dispatch barriers prove pre-dispatch invalidation prevents signing, racing/post-dispatch invalidation is ambiguous absent positive provider evidence, admission remains consumed, and replacement uses a revalidated key, new binding, request identity, and admission. |
| `v0.56.8` | Reusable transactional key-onboarding and live-authority-reconstruction provider/import conformance framework for `v0.10.13`-`v0.10.14`, mandatory for all later software, ring, aws-lc/FIPS, PKCS#11, TPM, KMS, remote, and platform key providers plus PKCS#8/legacy/PKCS#12 import paths. It standardizes idempotency-token capability, creation/import reconciliation, eventual-visibility polling, lifecycle-state/obligation separation, quarantine, fresh per-session validation/binding, activation eligibility, durable cleanup/disposition obligations, fencing, redaction, and supported outcome tables without treating stored history as live authority or lookup failure as destruction. | Intentionally faulty adapters are caught for duplicate creation/import, lost request IDs or idempotency tokens, `Ambiguous` promoted to `Created`, lifecycle failure dropping an obligation, wrong handle/public key/version/tenant/role/algorithm/provider, persisted validation/binding/`Active` records upgraded into capabilities, eventual-consistency timeout misclassification, KMS alias/version retargeting, HSM object replacement under one label, replayed audit records, rebinding unavailable after restart, stale fenced reconciliation clearing obligations, unreadable attributes, cleanup/disposition loss, object absence reported as destruction, provider fallback, and lifecycle-eligible or quarantined keys accepted without fresh authority. |
| `v0.56.9` | Reusable immutable-dispatch and returned-signature verification conformance framework for `v0.10.15`, mandatory for every later software, ring, aws-lc/FIPS, PKCS#11, TPM, KMS, remote, agent, and platform signer. It requires discovery locators to resolve to an immutable provider-native identity/version and canonical public-key digest, dispatch to use that exact identity, provider bytes to remain `UnverifiedSignature`, and a pinned explicit verifier to validate admitted bytes, algorithm/parameters, encoding, and bound key before any effect receives `VerifiedSignature`. Unsupported/unavailable verification is a typed fail-closed result with no fallback. | Shared deterministic barrier tests retarget or replace aliases, labels, handles, persistent references, remote mappings, and provider versions after binding/before admission, after admission/before dispatch, during dispatch, and before verification. Faulty adapters returning a valid wrong-key signature, malformed/noncanonical output, wrong DER/raw form, wrong algorithm/parameters, forged identity metadata, or success while the verifier is unavailable are caught; admission remains consumed, no bytes are transmitted, signer invalidation/quarantine obligations are retained, and no implicit signer/verifier fallback occurs. |
| `v0.56.10` | Reusable handle-backed MAC authority/evidence conformance framework for `v0.10.16`, mandatory for every later software, HSM, KMS/secret-manager, remote, and platform MAC provider. It standardizes immutable secret identity/version resolution, `BoundMacKey`, exact-input/purpose/truncation/TSIG-chain admission, destructive consumption, `UnverifiedMac`, constant-time independent `VerifiedMac`, separately typed `ProviderAssertedMac` and `CryptographicallyAttestedMac`, assurance policy, provider-session invalidation, capability publication, redaction, and no secret export or fallback. Every provider publishes per-purpose EAB/TSIG support and whether output evidence is independently verified, provider asserted, cryptographically attested, unsupported, or unavailable; “attested” requires a signed/native replay-protected receipt bound to the complete operation transcript. | Shared deterministic barriers retarget aliases/versions before admission, before/during dispatch, and before verification; intentionally faulty adapters use another tenant/directory/zone/external-account/key version/purpose, wrong TSIG chain state, valid wrong-key MAC, malformed/truncated output, non-constant-time comparison, forged assertion metadata, missing/forged/replayed attestation transcript fields, assertion promoted to attestation or independent verification, restored admission after any outcome, provider-session change, or signer/verifier fallback. Conformance proves only matching purpose-specific evidence reaches EAB/TSIG and opaque providers cannot overclaim assurance. |
| `v0.56.11` | Reusable transactional symmetric-secret onboarding, content-binding, and live-authority reconstruction conformance for `v0.10.17`-`v0.10.18`, mandatory for every later software, ring, aws-lc/FIPS, PKCS#11, TPM, KMS/secret-manager, remote, and platform MAC provider plus every secret import/adoption path. It standardizes stable request/idempotency identities, typed create/import/adopt outcomes, `SecretLifecycleState × SecretObligationSet`, quarantine, five binding modes and their assurance ordering, authenticated peer/attestation transcript validation, source-secret retention/destruction, reconciliation/fencing, safe per-session reconstruction through non-mutating evidence, capability-free snapshots, redaction, and published support tables. Historical peer confirmation is never runtime authority, and mutating peer ceremonies are governed by `v0.56.12`. Providers that cannot implement an admitted binding mode return typed unsupported and never create `BoundMacKey`. | Intentionally faulty adapters are caught for wrong-secret import, duplicate/lost create/import, `Ambiguous` promoted to `Created`, quarantined or `Unverified` objects activated, `ProviderAsserted` promoted to peer/cryptographic/local binding, forged/replayed peer or attestation receipts, cross-tenant/directory/zone/external-account/purpose/version binding, source destruction before durable binding, lifecycle failure dropping obligations, restored objects and replayed snapshot/audit capabilities, restart without safe reconstruction, mutating ceremony replay, stale fenced workers clearing obligations, cleanup/disposition ambiguity, lookup absence as destruction, secret export, and provider fallback. |
| `v0.56.12` | Reusable secret-binding bootstrap and peer-effect conformance for `v0.10.19`-`v0.10.21` and `v0.33.5`, mandatory for every provider/import path that advertises `LocallyCompared` or `PeerConfirmed`. It standardizes private single-use `SecretBindingAttempt`, exact quarantine/onboarding/transcript binding, local domain separation, operation-specific peer-effect construction, stable attempt/effect IDs, non-mutating-versus-mutating classification, orthogonal dispatch/operation/binding/observation state, persist-before-effect ordering, authenticated purpose-specific evidence, ambiguous-effect reconciliation, source retention, one-time EAB account/result coupling, TSIG probe preference and UPDATE ownership/rollback, restart rules, and capability/support publication. Profiles that cannot satisfy the exact ceremony return typed unsupported; they never substitute an ordinary `BoundMacKey`, `MacConsumerAdmission`, exported secret, provider assertion, or fallback provider. | Shared compile-fail, deterministic barrier, crash/restart, outbox, outcome-product, hostile-peer, and faulty-adapter suites cover construction outside quarantine, arbitrary-byte or ordinary protocol use, attempt reuse/restoration, cross-provider/session/tenant/peer/resource/transcript/effect substitution, unauthenticated success/rejection, operation rejection inflated into binding mismatch, observation unavailability erasing dispatch knowledge, lost EAB success, duplicate-account risk, ambiguous DNS UPDATE, blind retry, missing ownership/rollback/reconciliation, source destruction before durable resolution, stale fencing, mutating ceremony replay on restart, one-time EAB reconstruction, secret export, and fallback. Each provider proves its published local/peer mode and mutation/reconciliation cells or remains unavailable. |
| `v0.56.13` | Reusable strict signature-evidence and multi-axis verifier-assurance conformance for `v0.10.22`, mandatory for every software, ring, aws-lc/FIPS, hardware, KMS, secret-manager, remote, and platform signer. Providers publish locality, trust-domain and implementation relationship, validated-module identity, and key/input binding separately for exact cryptographic verification; provider assertions and attestations use distinct evidence types and never construct `VerifiedSignature`. Baseline production is exact local verification, same-implementation local software verification is valid, optional diversity is stronger, and remote self-verification remains insufficient. | Shared faulty-adapter, assurance-product, FIPS-boundary, effect-constructor, and trust-topology tests replace signer/verifier/key/session/module identities, promote assertions/attestations, make a remote signer self-verify, forge or collapse axes, return a valid wrong-key signature, force an out-of-boundary verifier into a FIPS-only profile, disable required verification, and attempt fallback. Capability tables and runtime evidence agree without ranking incomparable profiles as one scalar class; only exact `VerifiedSignature` reaches JWS, CSR, revocation, challenge certificates, or signed audit records. |
| `v0.57.0` | RustCrypto baseline ES256 account/CSR backend with entropy, key import/generation through `v0.10.13`-`v0.10.15`/`v0.56.8`-`v0.56.9`, immutable software-key identity/digest binding, mandatory returned-signature verification, JOSE raw signature conversion, and zeroization integration. | Official ES256/JWK/JWS/CSR vectors, malformed signature/key rejection, replaced key files at every binding/admission/dispatch/verification barrier, wrong-key valid signatures, verifier failure, creation/import lost-response and restart reconciliation, quarantine/activation gating, cleanup obligation, leakage review, and dependency admission pass. |
| `v0.57.1` | RustCrypto RSA/RS256 backend and bounded key-size/exponent policy. | RSA vectors, weak/oversized key, padding, algorithm-confusion, and resource tests pass. |
| `v0.57.2` | RustCrypto Ed25519/EdDSA backend where supported by ACME provider policy. | Official/differential vectors and unsupported-combination tests pass. |
| `v0.57.3` | RustCrypto P-384/ES384 backend where supported by ACME provider policy. | Official/differential vectors and coordinate/signature conversion tests pass. |
| `v0.57.4` | RustCrypto P-521/ES512 backend where supported by ACME provider policy. | Official/differential vectors, unusual bit-width, and conversion tests pass. |
| `v0.57.5` | RustCrypto EAB HMAC operation backend implementing `v0.10.16`/`v0.56.10` with immutable software-secret identity/version, `BoundMacKey`, exact-input single-use admission, constant-time independent returned-MAC verification, purpose-specific `VerifiedMac`, HS256 required and HS384/HS512 policy support, secret lifecycle, and no signature/MAC-provider substitution. Public create/import/adopt activation additionally requires `v0.57.13`. | RFC/JOSE vectors plus alias/version retargeting at every barrier, another valid EAB credential, cross-tenant/directory/external-account/purpose substitution, wrong hash/key length, malformed/truncated/extended MAC, admission reuse, verifier failure injection, secret retention, algorithm/assurance downgrade, and cross-backend fallback tests pass. |
| `v0.57.6` | RustCrypto RSA-PSS support for X.509 verification and PS256/PS384/PS512 JWS/CSR purposes where standards/provider policy permit, with exact MGF/hash/salt/trailer parameters. | Published/independent vectors and parameter-confusion, implicit-default, salt-length, wrong-purpose, malformed signature, and cross-provider differential tests pass. |
| `v0.57.7` | RustCrypto implementation of the `v0.10.7` compatibility-hash capability, including SHA-1 only for pinned OCSP/DNSSEC/NSEC3 verification purposes and no general digest constructor. | Official vectors and compile/API tests prove exact purpose/input binding, zero JWS/certificate-signing/generation exposure, no downgrade fallback, and capability refusal outside the pinned standards uses. |
| `v0.57.8` | RustCrypto DNSSEC verifier capabilities for every pinned mandatory/recommended validation algorithm and digest, with explicit typed treatment of MAY, unknown, prohibited, and unavailable algorithms and no signing requirement implied. | Current IANA/RFC 9904-derived RSA/ECDSA/EdDSA/DS matrices, malformed-key/signature, format, key-size, downgrade, cross-purpose, and independent-provider differential tests pass on the supported target matrix. |
| `v0.57.9` | RustCrypto `DnsUpdateMac` operation backend implementing RFC 8945 and `v0.10.16`/`v0.56.10` with immutable secret identity/version, `BoundMacKey`, exact TSIG request/response/chaining admission, constant-time independent MAC verification, purpose-specific `VerifiedMac`, HMAC-SHA-256 and policy-permitted SHA-384/SHA-512, zeroization integration, and strict EAB separation. Public create/import/adopt activation additionally requires `v0.57.13`. | Official/independent vectors plus alias/version retargeting at every barrier, cross-zone/tenant/purpose key rejection, valid wrong-key MAC, wrong chain state, truncation/malformed output, admission reuse after failure/cancellation/ambiguity, verifier injection failure, rotation/destruction, provider-session change, and no-fallback tests pass. |
| `v0.57.10` | Production RFC 2136 DNS provider adapter combining completed UPDATE, EDNS/TCP query, TSIG, `v0.10.16` `DnsUpdateMac`, and `v0.10.20`-`v0.10.21` peer-effect/outcome boundaries with zone restrictions, prerequisites, exact update/delete, positive request/response MAC evidence, non-mutating authenticated binding probes where available, mutating-effect ownership, and reconciliation. It is not production-activatable with a created/imported/adopted TSIG secret until `v0.57.13` completes onboarding/bootstrap/reconstruction. | Real authoritative-server integration covers authenticated probe and update success, secret rotation/retargeting between request and response, provider-assertion/attestation policy refusal, BADKEY/BADSIG/BADTIME, wrong zone/privilege, partial/lost response, operation rejection without content-mismatch inflation, observation unavailability without dispatch-state loss, unauthenticated errors, ambiguous confirmation update without blind retry, restart without repeated mutation, replay, concurrent TXT preservation, TCP fallback, crash cleanup/rollback/reconciliation, and proves no update success before exact request-bound TSIG response authentication. |
| `v0.57.11` | RustCrypto/software-key disposition implementation over encrypted envelopes and provider-managed files/memory, with atomic deletion/tombstones, backup/retention awareness, zeroization-limited claims, and explicit distinction between logical deletion and evidenced storage destruction. | Filesystem/memory fault tests cover open handles, partial unlink, snapshots/backups, permissions, read-only media, crash, restore/resurrection, not-found, repeated request, retained policy, and prove memory zeroization or unlink alone is not overclaimed as physical media destruction. |
| `v0.57.12` | RustCrypto public-key validation, software-signer binding, immutable dispatch, and returned-signature verification implementation for every admitted RSA, P-256/P-384/P-521, Ed25519, and Ed448 capability, using exact `v0.28.14` rules and `v0.10.11`-`v0.10.15` through `v0.56.7`-`v0.56.9` without exposing private material. | Official/pinned/independent vectors and cross-provider differential tests cover all valid/invalid boundary classes, wrong signer handle, replaced key files before every authority/dispatch/verification boundary, corrupted envelopes, valid wrong-key signatures, malformed encodings, unsupported curves/algorithms or verifier, provider health/session invalidation, entropy failure, cached/unrelated/cross-purpose proof rejection, transcript replay/expiry/role substitution, duplicate request identities, capacity bounds, destructive admission consumption across every signer outcome, and prove only locally verified output becomes effect authority. |
| `v0.57.13` | RustCrypto/software symmetric-secret create/import/adopt onboarding and fresh MAC-authority reconstruction through `v0.10.17`-`v0.10.21`/`v0.56.11`-`v0.56.12`, using encrypted versioned envelopes, stable idempotency, quarantine, private single-use `SecretBindingAttempt`, `LocallyCompared` binding by default, optional authenticated `PeerConfirmed` with durable orthogonal effect/reconciliation semantics, explicit source-secret disposition, and no restoration of live MAC capabilities. | Wrong-secret import, duplicate/lost writes, envelope/version substitution, arbitrary-byte bootstrap refusal, restart before every attempt/effect/binding/activation/source-destruction boundary, backup rollback/resurrection, cross-directory/zone/peer/purpose, constant-time comparison failure injection, forged/unauthenticated peer confirmation, operation/binding outcome confusion, observation unavailability, lost EAB success, ambiguous DNS UPDATE, restart without repeated mutation, stale worker, cleanup/disposition ambiguity, and snapshot capability-injection tests pass without premature activation or secret loss. |
| `v0.58.0` | ring backend in its own implementation boundary with explicit selection and no feature-unification ambiguity. | Cross-backend JWK/JWS/CSR vectors, unsupported algorithm fail-closed behavior, platform matrix, and dependency review. |
| `v0.58.1` | Published ring per-purpose capability, public-key-validation, signer-binding, asymmetric transactional onboarding, immutable signature dispatch/verification, handle-backed MAC operation authority/evidence, and key-disposition table with typed negotiation for JOSE/EAB/account rollover, CSR, X.509 path signatures, OCSP, CRL, CT v1/v2, TLS-ALPN, DNSSEC, TSIG, key import/generation, legacy verification hashes, and in-memory/software disposition; algorithm presence never implies another purpose or assurance level. Symmetric-secret activation additionally requires `v0.58.2`. | Generated compile/runtime matrices compare every purpose/algorithm/parameter/key-format/validation/binding/onboarding/signature-verification/MAC-evidence/disposition cell with `v0.56.6`-`v0.56.10` and independent vectors; replaced keys/secrets, wrong-key valid signatures/MACs, malformed output, verifier unavailability, and assurance downgrade fail before effects, unsupported cells are typed, and no cross-provider fallback or inferred support occurs. |
| `v0.58.2` | ring symmetric-secret onboarding/content-binding profile through `v0.56.11`-`v0.56.12`, publishing exact EAB/TSIG create/import/adopt, binding-attempt, peer-effect, mutation/reconciliation, and binding-mode support and using fresh local comparison where secret material is admitted. | Capability and differential tests cover wrong-secret import, unsupported opaque adoption, lost/duplicate onboarding, bootstrap confinement, restart reconstruction without repeated peer mutation, source destruction ordering, cross-purpose/version substitution, and prove unsupported cells do not fall back to another crypto provider. |
| `v0.59.0` | aws-lc-rs non-FIPS backend with explicit provider health, approved local policy set, and no silent fallback. | Cross-backend vectors, platform/native build matrix, provider failure tests, SBOM/native dependency evidence, and audit. |
| `v0.59.1` | Published aws-lc-rs non-FIPS per-purpose capability, public-key-validation, signer-binding, asymmetric transactional onboarding, immutable signature dispatch/verification, handle-backed MAC operation authority/evidence, and key-disposition table with typed negotiation for JOSE/EAB/account rollover, CSR, X.509, OCSP, CRL, CT v1/v2, TLS-ALPN, DNSSEC, TSIG, key import/generation, legacy verification hashes, and software disposition. Symmetric-secret activation additionally requires `v0.59.2`. | Generated native/portable matrices and cross-provider vectors prove every supported validation/binding/onboarding/signature-verification/MAC-evidence/disposition cell independently; replaced identities, valid wrong-key signatures/MACs, malformed output, verifier loss, and assurance downgrade fail before effects, every unavailable operation is typed, and overlap cannot trigger fallback or overclaim. |
| `v0.59.2` | aws-lc-rs non-FIPS symmetric-secret onboarding/content-binding profile through `v0.56.11`-`v0.56.12`, with stable import/create identities, encrypted persistence integration, quarantine, narrow binding attempts, local constant-time comparison, optional durable peer confirmation, safe reconstruction, and published mutation/reconciliation and assertion/attestation limitations. | Native/portable fault tests cover wrong-secret import, duplicate/lost result, bootstrap misuse, lost/ambiguous peer effect, restart/rollback without mutation replay, cross-tenant/purpose/version, source destruction races, binding downgrade, stale sessions, cleanup ambiguity, and cross-backend fallback. |
| `v0.60.0` | Separate AWS-LC FIPS deployment package/profile with exact module line, runtime `fips_status`, compliance manifest, per-purpose approved capability, public-key-validation/signer-binding, asymmetric transactional onboarding, immutable signature dispatch/verification, handle-backed MAC operation authority/evidence, and key-disposition table, supported environments, and fail-closed startup; non-approved or unavailable signature/MAC purposes and assurance profiles remain typed unsupported. Symmetric-secret activation additionally requires `v0.60.1`. | Dedicated supported-target CI proves provider status and every approved validation/binding/onboarding/signature-verification/MAC-evidence/disposition cell, rejects mutable-reference replacement, wrong-key/malformed/unverified signature or MAC output, assurance inflation, premature activation, stale evidence, non-approved cells, and negative fallback, and passes independent FIPS configuration/capability-claim review. |
| `v0.60.1` | AWS-LC FIPS symmetric-secret onboarding/content-binding qualification through `v0.56.11`-`v0.56.12`, documenting which create/import/adopt, binding-attempt, local comparison, peer-effect, reconciliation, cryptographic attestation, storage, and destruction operations remain inside the exact validated boundary. Unsupported modes fail startup/profile selection rather than leaving quarantine or using non-FIPS fallback. | Supported-target evidence covers wrong-secret import, boundary escape, arbitrary bootstrap use, non-approved comparison/hash/MAC, ambiguous peer effect, restart reconstruction without mutation replay, source destruction ordering, provider status loss, assurance overclaim, and negative fallback; compliance review binds the capability table to the validated module/configuration. |
| `v0.61.0` | PKCS#11 module/session/object discovery, key-handle identity, PIN/credential policy, and mechanism capabilities. | SoftHSM wrong module/slot/object/PIN and mechanism confusion tests pass. |
| `v0.61.1` | PKCS#11 transactional key creation/import onboarding, fresh-session authority reconstruction, immutable object-identity dispatch, mandatory returned-signature verification, public-key validation/object binding, concurrency/session pooling, unavailable-token recovery, non-exportability, lifecycle integration, and native disposition mapping through `v0.56.6`-`v0.56.9`. | Session loss/login races/token removal, created object with lost response, duplicate label/ID, deletion and recreation under the same label/ID before admission/dispatch/verification, object-handle reuse, valid wrong-object signatures, malformed output, verifier unavailability, unreadable/delayed attributes, persisted active/binding records, restart with rebinding unavailable, fenced reconciliation, cleanup/disposition failures, no fallback, and no-false-destruction integration pass with admission always consumed. |
| `v0.61.2` | PKCS#11 handle-backed EAB/TSIG HMAC operation provider through `v0.10.16`/`v0.56.10`: label discovery resolves to immutable token/slot/object identity and secret version, `BoundMacKey` plus single-use admission, exact mechanism/truncation/chaining policy, and explicit independent-versus-token-asserted assurance without extracting non-exportable secrets. Cryptographic attestation is claimed only for a verified signed/native receipt over the complete transcript; production activation of created/imported/adopted secrets additionally requires `v0.61.3`. | SoftHSM/native-token tests replace or recreate objects under one label before admission/dispatch/evidence validation; cover session/token loss, object-handle reuse, cross-tenant/purpose/version, valid wrong-object MAC, malformed output, C_Verify/unavailable assurance, assertion/attestation downgrade, cancellation/ambiguity, admission reuse, secret leakage, no fallback, and EAB/TSIG policy refusal of unapproved weaker evidence. |
| `v0.61.3` | PKCS#11 symmetric-secret create/import/adopt onboarding and fresh binding reconstruction through `v0.56.11`-`v0.56.12`. Token object identity/version remains quarantined until a narrow local/peer binding attempt, genuine token attestation, or explicitly policy-admitted provider assertion succeeds; labels never prove content. The profile publishes peer mutation/reconciliation support, and source-secret destruction and token disposition remain separate durable obligations. | SoftHSM/native-token tests import the wrong value, lose create/import or peer-effect responses, duplicate labels/IDs, misuse/replay a binding attempt, recreate/restore objects, restart sessions without repeating peer mutation, forge attributes/peer evidence/attestation, substitute slot/token/tenant/directory/zone/purpose, destroy the source early, and exercise cleanup/not-found ambiguity without producing `BoundMacKey` from quarantine. |
| `v0.62.0` | TPM 2.0 persistent handle, key creation/loading, policy session, signing, and non-exportability. | Software TPM wrong handle/policy/algorithm/lockout tests pass. |
| `v0.62.1` | TPM reset/recovery, transactional persistent/transient key onboarding, fresh-session authority reconstruction, immutable TPM Name/public-area/handle dispatch, mandatory returned-signature verification, attestation evidence boundary, interrupted operations, key rotation, lifecycle integration, and eviction/destruction semantics through `v0.56.6`-`v0.56.9`. | Reset/lockout/attestation/recovery plus persistent-handle replacement or reset before admission/dispatch/verification, changed TPM Name/public area, valid wrong-key signature, malformed output, verifier loss, created key with lost response, duplicate handle, delayed/unreadable public area, wrong session, quarantine/rebinding failure, admission consumption, hierarchy unavailability, cleanup/not-found ambiguity, restart, no fallback, and no-false-physical-destruction integration pass. |
| `v0.62.2` | TPM 2.0 keyed-hash/HMAC operation provider through `v0.10.16`/`v0.56.10` with immutable TPM Name/public-area identity, purpose-limited `BoundMacKey`, single-use admission, request/chain binding, non-exportability, and explicitly TPM-asserted rather than independently verified evidence unless a separate admitted verifier exists. `CryptographicallyAttestedMac` requires a verified TPM-native signed receipt over the complete operation transcript; production activation additionally requires `v0.62.3`. | Software/native TPM tests cover reset, persistent-handle replacement, Name/public-area change, wrong hierarchy/session/tenant/purpose/version, valid wrong-key MAC, malformed/truncated output, policy-session failure, verifier/assertion/attestation unavailability, assurance downgrade, cancellation/ambiguity, admission reuse, no fallback, and default EAB/TSIG rejection of unapproved weaker evidence. |
| `v0.62.3` | TPM keyed-hash secret creation/import/adoption onboarding and reconstruction through `v0.56.11`-`v0.56.12`, binding TPM Name/public area, hierarchy/policy, immutable version, intended content, narrow binding-attempt/effect identity, and admitted assurance mode. Ordinary TPM success is `ProviderAsserted`; `CryptographicallyAttested` requires a verified native signed receipt over the complete binding transcript. Peer-confirmation mutation/reconciliation support is explicit or typed unsupported. | Software/native TPM tests cover wrong sensitive data, lost/duplicate creation, bootstrap misuse, persistent-handle replacement, reset/restore, wrong Name/hierarchy/policy/tenant/purpose, forged or replayed peer/attestation evidence, ambiguous peer effect, restart/session loss without mutation replay, source destruction races, quarantine escape, eviction ambiguity, and no fallback. |
| `v0.63.0` | AWS KMS signer/key provider with transactional onboarding, fresh-session authority reconstruction, immutable key-ID/version dispatch, and mandatory returned-signature verification through `v0.10.13`-`v0.10.15`/`v0.56.8`-`v0.56.9`; aliases are discovery-only. It includes public-key validation, `BoundSigner`, local admission, grant/identity policy, retry/reconciliation, native disposition semantics, and no private export. | Mock and controlled AWS integration retargets aliases after binding/before admission, before/during dispatch, and before verification; covers valid wrong-version signatures, malformed output, verifier loss, wrong key ID/version/account/region, persisted records, rebinding unavailable after restart, request substitution, stale fencing, throttling, revoked grants, quarantine, cleanup, pending deletion, and proves no unverified output is transmitted and absence/scheduled deletion is never destruction. |
| `v0.63.1` | AWS KMS HMAC operation provider through `v0.10.16`/`v0.56.10` with aliases as discovery-only, immutable HMAC key ID/version, `BoundMacKey`, exact EAB/TSIG admission, GenerateMac/VerifyMac evidence classified as `ProviderAssertedMac` unless independently verified, grant/identity policy, retry classification, and no secret export. Cryptographic attestation is not inferred from authenticated API transport; production activation additionally requires `v0.63.2`. | Mock and controlled integration retargets aliases at every barrier; covers cross-account/region/key/version/purpose, wrong external-account or TSIG chain, valid wrong-key MAC, malformed output, VerifyMac unavailability, throttling, revoked grants, ambiguity, assurance downgrade, admission reuse, no fallback, and explicit policy acceptance/refusal of KMS assertions or genuine attestation. |
| `v0.63.2` | AWS KMS HMAC key create/import/adopt onboarding and reconstruction through `v0.56.11`-`v0.56.12`, with immutable key ID/version, stable client/idempotency identity where supported, quarantine, exact grant/account/region policy, typed binding-attempt/mode and peer-effect capability, source-key-material retention/destruction, eventual-consistency and ambiguous-effect reconciliation, and aliases as metadata only. GenerateMac/VerifyMac is `ProviderAsserted` unless separate cryptographic attestation is available. | Mock/controlled tests cover wrong imported material, alias retargeting, lost/duplicate create/import, bootstrap misuse, expired import tokens/material, cross-account/region/purpose, restored/pending-deletion keys, eventual visibility, forged peer/assertion/attestation metadata, ambiguous effect, restart without repeated mutation, premature source destruction, cleanup ambiguity, and no activation or fallback without admitted binding. |
| `v0.64.0` | Azure Key Vault signer/key provider with transactional onboarding, fresh-session authority reconstruction, immutable versioned-key-ID dispatch, and mandatory returned-signature verification through `v0.10.13`-`v0.10.15`/`v0.56.8`-`v0.56.9`; mutable aliases/names are discovery-only. It includes public-key validation/version binding, `BoundSigner`, admission, retry/reconciliation, soft-delete/purge/disable/retention semantics, and no private export. | Mock and controlled Azure integration covers alias/version retargeting at every binding-to-verification barrier, restored or recreated versions, valid wrong-version signatures, malformed output, verifier loss, wrong tenant/version, restart with unavailable rebinding, request substitution, stale fencing, throttling, revoked identity, quarantine, cleanup, recovery/purge protection, ambiguity, no fallback, and no false destruction. |
| `v0.64.1` | Azure Key Vault/Managed HSM handle-backed MAC operation capability closure through `v0.10.16`/`v0.56.10`: each admitted service/profile publishes exact immutable version identity, EAB/TSIG algorithm and verify support, assertion/attestation classification, and either a conforming non-exporting adapter or typed unsupported; secret export or software-provider substitution cannot emulate missing native semantics. Symmetric-secret activation additionally requires `v0.64.2`. | Pinned-profile mock/controlled tests cover alias/version retargeting, restored versions, cross-tenant/purpose, wrong-key MAC, malformed output, native verification unavailable, assurance downgrade, throttling/identity loss, and prove unsupported profiles fail explicitly without secret exposure or fallback. |
| `v0.64.2` | Azure Key Vault/Managed HSM symmetric-secret onboarding capability closure through `v0.56.11`-`v0.56.12`: each pinned profile publishes create/import/adopt, idempotency/reconciliation, immutable version identity, binding-attempt/modes, peer-effect mutation and reconciliation, source-material disposition, restore/soft-delete behavior, and safe reconstruction support, implementing the full boundary or returning typed unsupported. | Mock/controlled profile tests cover wrong-secret import, lost/duplicate operations, bootstrap misuse, restored/recreated versions, cross-tenant/purpose, forged peer/assertion/attestation evidence, ambiguous peer effects, restart without mutation replay, source destruction races, soft-delete/purge ambiguity, and prove missing native semantics are never emulated by exporting material or switching providers. |
| `v0.65.0` | OpenBao-compatible remote signer/key provider with authenticated transport, transactional onboarding, fresh-session authority reconstruction, immutable mount/key/version dispatch, and mandatory returned-signature verification through `v0.10.13`-`v0.10.15`/`v0.56.8`-`v0.56.9`; mutable key names resolve only during discovery. It includes validation, `BoundSigner`, admission, capability policy, secret-safe errors, and disposition mapping. | Dev-server integration changes key-version/name mappings before admission/dispatch/verification and covers agent/server restart, restored versions, valid wrong-version signatures, malformed output, verifier loss, lost create/import response, duplicate restart requests, wrong mount/version, binding ambiguity, token expiry, network ambiguity, quarantine, cleanup, retained versions, undelete, not-found, no fallback, and no overclaimed destruction. |
| `v0.65.1` | OpenBao-compatible transit HMAC operation provider through `v0.10.16`/`v0.56.10` with immutable mount/key/version, purpose-limited `BoundMacKey`, EAB/TSIG admission, authenticated transport, `ProviderAssertedMac` verify evidence unless an independent verifier or genuine signed/native attestation exists, capability policy, and secret-safe errors. Production activation additionally requires `v0.65.2`. | Dev-server integration changes key names/versions at every barrier and covers restored versions, cross-mount/tenant/purpose, valid wrong-version MAC, malformed output, verify endpoint unavailable, token expiry, network ambiguity, assurance downgrade, admission reuse, no fallback, and explicit EAB/TSIG policy handling of assertions versus cryptographic attestation. |
| `v0.65.2` | OpenBao-compatible transit HMAC create/import/adopt onboarding and reconstruction through `v0.56.11`-`v0.56.12`, with immutable mount/key/version, stable request and binding-attempt/effect identity, quarantine, version/restore and ambiguous-effect reconciliation, typed binding modes, authenticated transport, explicit peer-mutation support, source-secret disposition, and provider assertions kept distinct from cryptographic attestation. | Dev-server tests cover wrong-secret import, lost/duplicate request, bootstrap misuse, restored/undeleted versions, cross-mount/tenant/purpose, forged/replayed peer/assertion/attestation receipts, token expiry, network ambiguity, restart without peer mutation replay, source destruction races, quarantine escape, cleanup/retention ambiguity, and no fallback. |
| `v0.66.0` | Purpose-specific authenticated remote signer protocol through `v0.4.26` with canonical bounded request/observation encoding, schema/capability negotiation, mutual endpoint identity, tenant/policy/effect/request-digest/generation/deadline/recovery-epoch binding, explicit dispatch acknowledgement, replay/reordering/duplication/downgrade/cross-session defense, request-purpose authorization, transactional onboarding/reconciliation, fresh-session authority reconstruction, immutable remote key-instance/version dispatch, untrusted returned-signature transport, mandatory local verification, signed audit receipts, local admission enforcement, and native disposition forwarding through `v0.10.13`-`v0.10.15`/`v0.56.8`-`v0.56.9`. No live admission/evidence is serialized and the endpoint returns observations only. | Compromised-client/server scenarios remap remote names or restart the agent at each binding-to-verification barrier; negotiate stale/unsupported schemas; forge identity metadata, creation/binding evidence, or dispatch acknowledgements; replay/reorder/duplicate/cross-session substitute requests; return valid wrong-key, malformed, or replayed signatures; remove the verifier; substitute request context; lose responses; and prove quarantined keys, unverified output, remote evidence minting, stale sessions, cleanup loss, unsupported/unavailable operations, fallback, and semantic inflation all fail closed. |
| `v0.66.1` | Reference remote signer agent with enrollment/rotation/revocation, transactional onboarding/reconciliation, fresh-session authority reconstruction, immutable agent key-instance/version dispatch, mandatory client-side returned-signature verification, least-privilege deployment, validation/`BoundSigner`, admission enforcement, and disposition forwarding through `v0.10.13`-`v0.10.15`/`v0.56.8`-`v0.56.9`. | Hostile-network and compromised-agent/client integration restarts or rotates the agent and remaps key references before admission/dispatch/verification; covers valid wrong-key and malformed signatures, verifier loss, forged identity evidence, created key with lost reply, delayed attributes, wrong handle/version/request/role, rebinding ambiguity, replay, quarantine, cleanup/disposition failure, partial execution, no fallback, and no-semantic-inflation behavior. |
| `v0.66.2` | Offline account-key rollover ceremony package, human-verifiable transcript, dual authorization, resume/abort, and recovery. | Interrupted/swapped/replayed/partially approved ceremony fixtures fail safely. |
| `v0.66.3` | Generic authenticated remote MAC operation protocol extending `v0.66.0` with immutable secret-instance/version identity, EAB/TSIG purpose authorization, exact-input/truncation/chaining admission forwarding, untrusted MAC transport, independently verified versus `ProviderAssertedMac` versus genuinely `CryptographicallyAttestedMac` evidence, replay defense, and no remote secret export. Production activation of remotely created/imported/adopted secrets additionally requires `v0.66.5`. | Compromised-client/server tests remap secret names/versions, substitute tenant/directory/zone/external-account/purpose/chain state, return valid wrong-key or malformed MACs, forge assurance metadata, omit/forge/replay attestation fields, replay admissions/evidence, disconnect ambiguously, remove verification, and prove no raw/unapproved MAC reaches EAB/TSIG or triggers fallback. |
| `v0.66.4` | Reference remote MAC operation agent with enrollment/rotation/revocation, immutable secret mapping, restart-safe `BoundMacKey` invalidation, admission enforcement, native assertion/attestation reporting, rate limits, least privilege, and EAB/TSIG capability policy through `v0.56.10`. Transactional secret activation additionally requires `v0.66.5`. | Hostile-network and compromised-agent integration rotates/remaps secrets before admission/dispatch/verification; covers restart, wrong key/version/purpose, valid wrong-key MAC, malformed output, verifier unavailable, forged assurance, replay, cancellation/ambiguity, no fallback, and policy refusal of weaker evidence. |
| `v0.66.5` | Generic remote MAC protocol/agent transactional secret onboarding and fresh authority reconstruction through `v0.56.11`-`v0.56.12`: authenticated stable create/import/adopt and binding-attempt/effect IDs, immutable secret-instance/version mapping, quarantine, narrow local/peer binding operations, typed binding-mode receipts, peer mutation/reconciliation classification, replay protection, source-secret transfer/destruction policy, safe restart reconciliation, fencing, and assertion-versus-cryptographic-attestation separation. | Compromised client/agent/server tests import the wrong secret, lose/duplicate onboarding or peer effects, remap or restore instances, misuse/replay bootstrap authority, forge/replay peer/assertion/attestation receipts, substitute tenant/directory/zone/purpose/version, restart at every boundary without repeating mutation, destroy the source early, bypass quarantine, clear obligations from a stale worker, or claim absence as destruction; no live `BoundMacKey` or fallback results. |

## Phase 7 - Web PKI Extensions And Provider Profiles

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.67.0` | RFC 8657 account-URI and validation-method parameter syntax, normalization, account binding, and policy model. | Published vectors and malformed/duplicate/unknown parameter tests pass. |
| `v0.67.1` | RFC 8659 advisory CAA preflight for `issue`/`issuewild`, aliases, DNSSEC states, and CA advertised identities. | Deny/allow/indeterminate vectors pass without claiming CA-equivalent validation. |
| `v0.68.0` | RFC 8738 IP identifier orders for IPv4/IPv6 with HTTP-01 and TLS-ALPN-01, canonical bytes, CSR/certificate binding, and DNS-01 prohibition. | RFC vectors and cross-family, textual alias, SAN type, wildcard, forbidden challenge, and route-policy tests. |
| `v0.69.0` | Advertised capability extraction and versioned signed provider-profile schema for documented product behavior. | Schema fuzzing/signature/version and no-provider-name-branching checks pass. |
| `v0.69.1` | Expiring observed-capability cache with provenance, confidence, invalidation, and no security-policy override. | Stale/conflicting/spoofed observations cannot weaken policy. |
| `v0.69.2` | Operator capability/product policy precedence and typed unsupported taxonomy across standard/client/crypto/CA/operator/product causes. | Full precedence matrix returns stable exact reasons. |
| `v0.69.3` | Provider-observation record/replay corpus for directory metadata, advertised/observed capabilities, quirks, rate behavior, and redacted failure shapes. | Current code replays every retained provider observation; drift reports distinguish provider change, standard change, deliberate client correction, and regression. |
| `v0.70.0` | Let's Encrypt staging profile and conformance: directory changes, challenge behavior, ARI, rate-limit handling, alternate chains, and controlled production opt-in smoke. | Pinned staging suite, account/order lifecycle, all supported challenges, ARI, revocation, and no default production traffic. |
| `v0.71.0` | EAB-required commercial-CA profile and controlled conformance for product restrictions, quotas/payment/operator actions, challenge availability, and wildcard policy. | Controlled integration evidence, profile-vs-advertised conflict tests, and no embedded provider special cases. |
| `v0.71.1` | Production web-server adoption contract and reference migration from an existing `instant-acme`-style client: signer-proven `v0.16.3` issuer/account adoption, EAB, HTTP-01/TLS-ALPN-01/DNS-01 selection, renewal queue/status, atomic install, live reload, rollback, and redacted metrics. | A Fluxheim integration fixture replaces its client boundary and proves imported account ownership by fresh POST-as-GET, first issuance, due renewal, account rollover, revocation, failure continuity, non-exportable signer migration, and zero-restart SNI activation. |
| `v0.71.2` | Regulated web-server profile using the AWS-LC FIPS signer/CSR/TLS challenge/transport path with machine-readable boundary evidence. | FIPS-required configuration fails closed on any non-validated ACME crypto path and passes the web-server lifecycle fixture on supported environments. |

## Phase 8 - Every Published Specialist ACME RFC

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.72.0` | RFC 8739 STAR directory/order fields, lifetime/date policy, recurrent certificate retrieval model, and strict separation from ordinary orders. | Section-level encoding/parsing and invalid lifetime/date fixtures pass. |
| `v0.72.1` | STAR recurrent retrieval, cancellation, expiry, renewal scheduling, persistence, and crash recovery. | Timing/restart/cancellation/expiry fault suite passes. |
| `v0.73.0` | RFC 8823 email identifier normalization, S/MIME CSR/certificate profile, address binding, and capability model. | Address/canonicalization/profile/identifier substitution fixtures pass. |
| `v0.73.1` | `email-reply-00` presentation/evidence, MIME/message validation, replay/spoofing defense, manual workflow, and cleanup. | RFC fixtures and malformed/replayed/spoofed message recovery suite passes. |
| `v0.74.0` | RFC 9115 delegation metadata, credentials, CSR templates/extensions, order fields, and authorization boundaries. | Encoding/profile/confused-deputy and scope fixtures pass. |
| `v0.74.1` | Delegated STAR issuance, recurrent retrieval, termination/revocation, duplicate handling, persistence, and crash recovery. | Expired/unknown delegation, duplicate request, revocation, and restart suite passes. |
| `v0.75.0` | RFC 9444 subdomain authorization and policy integration, including delegation limits and identifier-set binding. | RFC vectors, parent/child confusion, scope escalation, wildcard interaction, expiry, and revocation tests. |
| `v0.76.0` | RFC 9447 authority-token challenge framework, token-type registry, typed request/evidence, issuer/purpose policy, and extension API. Externally issued token signatures remain verification evidence only. Any locally handle-backed signed authority-token request, proof, or extension effect must consume `v0.10.15` immutable-identity dispatch and matching locally constructed `VerifiedSignature`; unverified provider output cannot become authority-token bytes or evidence. | RFC encoding and unknown/unsupported token-type fixtures pass. Wrong-key valid signatures, malformed output, verifier unavailability, alias/object replacement before dispatch or verification, cross-token-type/purpose substitution, and raw/`UnverifiedSignature` constructor negatives prove local signed effects cannot become usable before exact verification. |
| `v0.76.1` | JWT authority-token profile using RFC 7519/8725 algorithm/audience/expiry/issuer/subject constraints and replay defense. | JWT algorithm, audience, expiry, substitution, nested-token, and replay corpus passes. |
| `v0.77.0` | RFC 9448 TNAuthList authority-token profile, identifier normalization, certificate profile, and `tkauth-01`. | RFC fixtures, range/canonicalization, token binding, profile mismatch, and authorization aggregation tests. |
| `v0.78.0` | RFC 9799 `.onion` identifier parsing, version/checksum/key binding, in-band CAA fields/policy, and CSR/certificate profile. | RFC identifier/CAA/profile and key mismatch fixtures pass. |
| `v0.78.1` | `onion-csr-01` challenge construction, CSR evidence, replay/expiry handling, workflow persistence, and cleanup. | CSR substitution/replay/expiry/restart/cleanup suite passes. |
| `v0.79.0` | RFC 9891 DTN `bundleEID` identifier normalization, certificate SAN/profile rules, and capability model. | RFC endpoint/profile and malformed identifier fixtures pass. |
| `v0.79.1` | `bp-nodeid-00` presentation/evidence, validation workflow, replay/timeout policy, persistence, and cleanup. | Evidence binding/replay/timeout/restart/cleanup suite passes. |
| `v0.80.0` | NfInstanceId registry/profile support with `tkauth-01`, isolated external-profile mapping, typed capability result, and certificate checks. | Registry fixtures, profile revision pin, identifier/token mismatch, unsupported-profile, and extension round-trip tests. |
| `v0.81.0` | Stable extension registration API for directory, identifier, challenge, order, and problem values with bounded unknown retention and conflict prevention. | All published extensions register concurrently; duplicates, known-field override, unstable ordering, and resource attacks fail. |

## Phase 9 - Production Stores, Deployment, And Operations

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.82.0` | SQLite schema, migrations, account/job/certificate/audit persistence, transactions, and CAS revisions. | Real database transaction/lock/migration and store-conformance suites pass. |
| `v0.82.1` | SQLite outbox, leases/fencing, encrypted-field references, single-node scheduling, and cleanup queue. | Lease/outbox/crash and ciphertext-reference fault injection passes. |
| `v0.82.2` | SQLite corruption detection, online backup, restore, migration rollback, disaster recovery runbook, rollback capability publication, and `v0.33.8` external-witness integration or explicit reduced-assurance operator-declared profile. | Corrupt/truncated/locked database, store/witness commit-skew, unavailable/reset witness, unnoticed/declared rollback, high-assurance startup refusal, and backup/restore drills pass. |
| `v0.83.0` | PostgreSQL schema/migrations, transactions, CAS revisions, and store-conformance baseline. | Isolation-level, lock, migration, and compatibility suite passes. |
| `v0.83.1` | PostgreSQL transactional outbox, leases, fencing, account/intent concurrency, and cleanup queues. | Multi-worker stale leader and exactly-once-application interleavings pass. |
| `v0.83.2` | PostgreSQL HA failover, network partitions, connection recovery, migration rollback, backup/restore, external rollback-witness integration with leader/cluster identity, and bounded load. | Multi-node failover/partition/restore/load plus store/witness commit-skew, split-brain, clone/reset, key-rotation, and high-assurance startup suites pass without duplicate activation or overclaimed rollback detection. |
| `v0.84.0` | Tenant identity/authorization boundaries and row/key namespace isolation across accounts, jobs, keys, deployments, and audit events. | Cross-tenant access/property and confused-deputy tests pass. |
| `v0.84.1` | Per-tenant encryption-key references, rotation, deletion/crypto-shredding policy, and backup implications. | Key rotation/wrong-key/deleted-key/restore cases are explicit and safe. |
| `v0.84.2` | Tenant quotas, account concurrency/rate policy, intent single-flight, noisy-neighbor backpressure, and fair scheduling. | Quota races, starvation, overload, and duplicate intent properties pass. |
| `v0.85.0` | Nginx filesystem/config-test/reload/health/rollback deployment adapter. | Real Nginx failed reload, stale generation, permission, and rollback tests pass. |
| `v0.85.1` | Apache filesystem/config-test/reload/health/rollback deployment adapter. | Real Apache failed reload, stale generation, permission, and rollback tests pass. |
| `v0.85.2` | HAProxy runtime/file activation, health verification, generation fencing, and rollback deployment adapter. | Real HAProxy reload/runtime failure and rollback tests pass. |
| `v0.85.3` | systemd credentials/path/timer/service notification deployment adapter with atomic generations and rollback. | Real systemd credential permission, reload, restart, stale generation, and rollback tests pass. |
| `v0.86.0` | Kubernetes Secret deployment with resource versions, server-side conflict policy, immutable options, rollout verification, rollback, and least-privilege RBAC. | Kind integration, conflict/partition/watch loss, namespace isolation, RBAC denial, stale worker, and rollback tests. |
| `v0.87.0` | Generic in-process deployment callback contract with generation fencing, health evidence, timeout/cancellation, and rollback. | Failed/hung/replayed callbacks and stale generation tests pass. |
| `v0.87.1` | Purpose-specific authenticated remote deployment agent protocol through `v0.4.26` with canonical bounded effect/observation encoding, schema/capability negotiation, mutual endpoint identity, tenant/policy/effect/request-digest/target/generation/deadline/recovery-epoch binding, explicit dispatch acknowledgement, replay/reordering/duplication/downgrade/cross-session defense, target-scoped capabilities, generation fencing, and key-material policy. It receives no live Rust authority and returns observations only. | Malformed/version, compromised-client/agent, replay/reorder/duplicate, cross-session/tenant/target/generation/epoch substitution, false acknowledgement, partial activation, observation overclaim, remote evidence-minting, key leakage, and scope tests pass. |
| `v0.87.2` | Reference remote deployer, partition recovery, health evidence, rollback, and hostile-network integration. | Compromised/partitioned agent tests prove containment and correct rollback. |
| `v0.88.0` | Redacted structured lifecycle event model and identifier privacy modes with stable event schemas. | Snapshot/redaction tests prove no secret fields leak. |
| `v0.88.1` | Tracing and metrics traits, bounded labels/cardinality, queue/renewal/deployment health, and exporter failure isolation. | Cardinality, backpressure, exporter failure, and redaction tests pass. |
| `v0.88.2` | Audit records and canonical serialization with actor/tenant/effect/deployment provenance. | Complete event provenance and secret-exclusion tests pass. |
| `v0.88.3` | Signed hash-linked audit log, key rotation, checkpoints, verification, export, and retention. Every handle-backed audit checkpoint/signature uses `v0.10.15` immutable-identity dispatch and locally constructed `VerifiedSignature`; no signed record, checkpoint, export, or rotation marker becomes durable or externally usable from raw or `UnverifiedSignature` bytes. | Tamper/deletion/reordering/truncation/rotation tests plus alias/object replacement before admission, dispatch, and verification; valid wrong-key signatures; malformed output; verifier loss; cancellation/ambiguity; admission reuse; and cross-tenant/log/checkpoint substitution prove only exact verified signatures enter the audit chain. |
| `v0.89.0` | Read-only CLI for directory/account/order/certificate/ARI/policy/status inspection and safe human/machine output. | Golden output, schema, corruption, and redaction tests pass. |
| `v0.89.1` | Mutating CLI for account operations, begin/resume/cancel/manual challenge, renew-now, revocation, and confirmation gates. | Command misuse, confirmation, cancellation, resume, and interrupted mutation tests pass. |
| `v0.89.2` | CLI configuration/profile validation, shell completion, exit-code contract, signal handling, and automation compatibility. | Cross-platform invocation and stable exit/output contract tests pass. |
| `v0.90.0` | Single-node daemon process, configuration, worker supervision, health/readiness, graceful shutdown, and SQLite profile. | Restart/kill/config failure and long-running single-node soak tests pass. |
| `v0.90.1` | Durable renewal scheduling, cleanup workers, backpressure, live safe configuration reload, and operational controls. | Clock/queue/reload/overload and shutdown drain tests pass. |
| `v0.90.2` | PostgreSQL HA daemon profile, leader/lease behavior, rolling upgrades, partitions, and multi-replica observability. | Leader changes, network partition, stale replica, rolling upgrade, and soak tests pass. |
| `v0.91.0` | Remote validation/deployment agent enrollment and least-privilege reference packaging. | Enrollment, expired credential, target scope, and package permission tests pass. |
| `v0.91.1` | Agent credential/key rotation, revocation, re-enrollment, lost-state recovery, and operator audit. | Rotation/revocation/recovery and hostile-network tests pass. |
| `v0.91.2` | Agent protocol-version compatibility, signed upgrades, rollback, system packages/containers, and deployment hardening. | Upgrade/rollback/tamper/old-new compatibility and compromised-node exercises pass. |
| `v0.91.3` | Android Keystore key-provider adapter with transactional generation/import/adoption, fresh-session authority reconstruction, immutable key/security-level identity dispatch, and mandatory returned-signature verification through `v0.10.13`-`v0.10.15`/`v0.56.8`-`v0.56.9`; aliases are discovery-only. It includes purpose/algorithm/exportability capabilities, validation/binding/admission, attestation, lifecycle/authentication constraints, and disposition mapping. | Device/emulator tests replace or retarget aliases after binding/before admission, before/during dispatch, and before verification; cover process death, restored/recreated aliases, valid wrong-key signatures, malformed output, verifier loss, wrong security level, locked user, invalidation, attestation mismatch, rotation, quarantine, backup exclusion, cleanup ambiguity, retained policy, no fallback, and no false destruction. |
| `v0.91.4` | Apple Keychain and Secure Enclave key-provider adapters for macOS/iOS with transactional generation/import/adoption, fresh-session authority reconstruction, immutable persistent-reference/key identity dispatch, and mandatory returned-signature verification through `v0.10.13`-`v0.10.15`/`v0.56.8`-`v0.56.9`. It includes validation/binding/admission, access-control, synchronizability, hardware capability, lifecycle/background policy, and disposition semantics. | Native device/simulator tests replace, restore, or synchronize keychain objects at every binding-to-verification barrier; cover persistent-reference reuse, valid wrong-key signatures, malformed output, verifier loss, process restart, wrong access group, locked keychain, entitlement errors, non-exportability, rotation, synchronization resurrection, quarantine, cleanup ambiguity, retained policy, no fallback, and no false destruction. |
| `v0.91.5` | Windows CNG key-provider and certificate-store adapter with transactional generation/import/adoption, fresh-session authority reconstruction, immutable provider/key-instance identity dispatch, and mandatory returned-signature verification through `v0.10.13`-`v0.10.15`/`v0.56.8`-`v0.56.9`; key names are discovery-only. It includes validation/binding/admission, ACL/export policy, machine/user scope, chain/store interaction, atomic replacement, service-account behavior, and disposition semantics. | Native Windows tests replace key-name/store mappings after binding/before admission, before/during dispatch, and before verification; cover restored/recreated key objects, valid wrong-key signatures, malformed output, verifier loss, restart, wrong provider/scope/store, ACL denial, non-exportability, service identity, replacement rollback, quarantine, cleanup ambiguity, retained policy, no fallback, and no false destruction. |
| `v0.91.6` | Optional bounded PKCS#12 import/export interoperability adapter with explicit password/KDF/MAC/encryption policy, certificate/key matching, friendly-name privacy, default-off secret export, and `v0.10.13`/`v0.56.8` quarantined onboarding for imported private keys. | OpenSSL/platform differential fixtures cover malformed/legacy/weak algorithms, wrong password/MAC, duplicate keys/certs, key mismatch, oversized bags, lost/duplicate import outcomes, validation/binding failure, restart, cleanup obligation, secret redaction, export refusal, and no activation before onboarding completes. |

## Phase 10 - Platform, Formal Assurance, And Draft Isolation

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.92.0` | Linux target qualification for core, transports, filesystems, rustls/OpenSSL, stores, agents, clocks, entropy, permissions, services, and packages. | Native Linux CI and target-specific security evidence passes. |
| `v0.92.1` | Android target qualification for core and implemented adapters, including Keystore, roots, clocks, entropy, storage, and lifecycle constraints; unimplemented profiles remain explicitly unsupported. | Device/emulator CI and explicit supported/unsupported-profile evidence passes. |
| `v0.92.2` | Windows target qualification including CNG/certificate store, reparse-safe paths, ACLs, atomic replacement, roots, services, clocks, entropy, and process behavior. | Native Windows integration and security evidence passes. |
| `v0.92.3` | macOS and iOS target qualification including Keychain/Secure Enclave, APFS/symlink/ACL behavior, roots, clocks, entropy, launch/runtime integration. | Native macOS plus device/simulator iOS evidence passes with explicit capability differences. |
| `v0.92.4` | FreeBSD, OpenBSD, NetBSD, and DragonFly BSD qualification for core and supported std adapters. | Native/cross CI and explicit unsupported-adapter failure evidence passes for each BSD. |
| `v0.92.5` | Cross-target feature/profile parity audit and four-level evidence publication for every crate/backend/adapter combination: compile-checked, emulator/simulator-tested, native-tested, and production-qualified. The matrix also records filesystem durability/atomicity and symlink behavior, wall/monotonic clock semantics, entropy availability, mutable-locator replacement, endian/width assumptions, and atomic support for each relevant target. | Machine-readable target matrix matches CI and release claims; seeded compile-only-as-native inflation, missing emulator/native evidence, fsync/rename/symlink faults, backward clocks, entropy failure, alias replacement, endian/width mismatch, and absent atomics fail or produce explicit typed unsupported profiles at compile/configuration time. |
| `v0.92.6` | Release-to-release target/profile regression matrix covering compile support, feature resolution, runtime probes, filesystem semantics, and platform security decisions. | Every previously supported cell is compared with the prior release; removals or weaker evidence block release unless explicitly approved as a documented breaking change. |
| `v0.93.0` | Future custom-operating-system readiness: no_std low-level API audit, caller-provided alloc/clock/entropy/transport/signer/storage interfaces, C ABI exclusion policy, and porting guide. | A freestanding reference target compiles core/challenge crates and replays a complete deterministic issuance trace with fake effects. |
| `v0.94.0` | Kani qualification and coverage closure for the existing budget, bounded-conversion, identifier/intent normalization, JWS exclusivity, and nonce-ownership harnesses introduced alongside their code. | Harnesses pass at published bounds, every historical counterexample remains a Rust regression, and assumptions/non-proven properties plus coverage gaps are documented. |
| `v0.94.1` | Kani qualification and coverage closure for protocol/workflow transition legality, retry termination, authorization aggregation, and terminal states first modeled during reducer implementation. | Harnesses pass at published bounds, replay generated counterexamples as Rust/model tests, and leave no unclassified safety-critical transition family. |
| `v0.94.2` | Kani qualification and coverage closure for cleanup ownership, leases/fencing, deployment activation, chain selection, and remaining bounded adapter invariants. | Harnesses pass at published bounds with environmental assumptions explicit and every uncovered invariant assigned before release. |
| `v0.95.0` | Loom qualification and coverage closure for nonce queues, challenge presentation stores, and cancellation races whose initial models shipped with the primitives. | All retained and expanded modeled interleavings pass without reuse, deadlock, lost cleanup, or unexplained change from earlier model evidence. |
| `v0.95.1` | Loom qualification and coverage closure for job/account leases, fencing, outbox application, and rate limiting. | All retained and expanded interleavings pass without stale activation or duplicate result application, and prior counterexamples remain regressions. |
| `v0.95.2` | Loom qualification and coverage closure for deployment reload/rollback, scheduler/cleanup queues, and shutdown races. | All retained and expanded interleavings pass without mismatched generations or abandoned cleanup; remaining concurrency exclusions are explicit. |
| `v0.96.0` | Parser/protocol fuzz qualification and corpus closure for the continuously maintained JSON, JOSE/JWK, PEM/DER/CSR, OCSP, CRL, SCT, EDNS, TSIG, DNSSEC canonicalization, link, Retry-After, URL, IDNA, extension, and profile targets introduced with each parser. | Published minimum durations, differential oracles where available, and cumulative work/allocation assertions complete with zero unresolved crashes; every historical crash remains a hashed regression seed and target coverage gaps are closed. |
| `v0.96.1` | Miri program for every applicable safe crate, store model, and challenge ownership path. | Miri matrix is clean or has documented tool limitations with alternate evidence. |
| `v0.96.2` | Native/FFI sanitizer program for crypto, PKCS#11, TPM, OpenSSL, database, and platform adapters. | ASan/UBSan/TSan profiles are clean on supported targets. |
| `v0.96.3` | Timing/leakage evidence program for secret comparisons, software signers, key parsing, and redaction boundaries. | Published methodology/results meet defined thresholds without overstating constant-time guarantees. |
| `v0.96.4` | Pre-production security mutation qualification across nonce, JSON/JOSE, PKIX, trust, signature/MAC evidence, outbox/reconciliation, challenge ownership, cleanup, deployment, and provider fallback controls, extending the `v0.4.23` baseline to every implemented trusted boundary. | Independently authored mutations removing verification, restoring nonce/admission, accepting duplicate JSON members, bypassing certificate checks, retrying may-have-dispatched effects, weakening cleanup ownership, promoting provider assertions, or enabling trust/provider fallback are all killed by named permanent tests; surviving or equivalent untested mutations block qualification. |
| `v0.97.0` | Extend and qualify the `v0.4.1` TLA+/Quint baseline for issuance/finalization/certificate verification/cancellation with complete Rust reducer trace replay. | Model checking and valid/invalid trace parity pass across every retained early-model revision and current implementation state. |
| `v0.97.1` | Extend and qualify the formal model for account rollover and challenge presentation/cleanup with Rust trace replay. | Model checking and crash/retry trace parity pass, including retained cleanup obligations and CA/local-state separation. |
| `v0.97.2` | Extend and qualify the formal model for lease/outbox/deployment activation/rollback/renewal plus `v0.33.7` whole-store rollback discontinuity and `v0.33.8` external witness protocol with Rust trace replay. | Partition/stale-worker/crash and rollback-protected/detecting/unprotected restore trace parity passes against the same traces used by store/deployer fault suites, including store/witness commit skew, witness reset/clone/split-brain/key rotation, epoch invalidation, assurance startup, quarantine, operator declaration, reconciliation, and revalidation. |
| `v0.97.3` | Historical formal-model and generated-trace replay across all retained model revisions. | Current Rust behavior accepts every still-valid historical trace and rejects every retained invalid trace; model changes produce reviewed trace-level diffs. |
| `v0.100.0` | Experimental standards framework: exact draft-revision metadata/naming, default-off registration, fixture isolation, API instability policy, compatibility rules, active/expired/replaced classification, and promotion/retirement process. | Tests prove drafts cannot activate implicitly, override stable registrations, advance revision without an explicit package/API change, or remain claimed after expiry/replacement unnoticed. |
| `v0.101.0` | Exact-revision ACME certificate profiles draft implementation, including directory metadata, order selection, capability policy, and certificate verification binding. | Revision-specific fixtures, unknown profile behavior, downgrade/mismatch tests, and no stable API leakage. |
| `v0.102.0` | Exact-revision persistent DNS validation draft implementation with authorization lifetime, reuse, revocation, and cleanup policy. | Revision-specific lifecycle, stale authorization, takeover, policy downgrade, and crash-recovery tests. |
| `v0.103.0` | Exact-revision DNS account-label draft implementation with account binding, label derivation, delegation, rotation, and cleanup. | Revision-specific vectors plus collision, cross-account, rollover, downgrade, and DNS delegation attacks. |
| `v0.104.0` | Exact-revision device-attestation draft implementation with attestation format/provider boundary, challenge binding, freshness, and privacy policy. | Revision-specific fixtures plus replay, substituted device/key, untrusted root, malformed evidence, and privacy tests. |
| `v0.105.0` | Exact-revision RATS draft implementation with evidence/appraisal interfaces, nonce binding, endorsements, reference values, and result policy. | Revision-specific fixtures plus replay, stale endorsement, verifier confusion, ambiguous appraisal, and resource limits. |
| `v0.106.0` | Exact-revision authority-token constraints draft implementation extending the stable authority-token model without weakening RFC 9447/9448 behavior or the `v0.76.0` signature-evidence boundary. Any new locally handle-backed signed effect inherits `v0.10.15` immutable dispatch and `VerifiedSignature`; draft constraints cannot admit raw/unverified output or weaken stable evidence requirements. | Revision-specific token vectors, constraint precedence, downgrade, audience/scope escalation, stable/draft coexistence, wrong-key/malformed/verifier-unavailable output, and no-unverified-constructor tests pass. |
| `v0.107.0` | Exact-revision quantum-ready ACME TLS/JWS profile draft with hybrid/PQ algorithm identifiers, key-provider capabilities, transport/signing policy, and size budgets. | Revision-specific vectors, downgrade, oversized key/signature, provider mismatch, hybrid binding, and interoperability tests. |
| `v0.108.0` | Exact-revision public-key challenge draft implementation with proof construction, key/identifier/account binding, replay defense, and cleanup lifecycle. | Revision-specific vectors plus substituted key/account/identifier, replay, downgrade, unsupported algorithm, and state recovery tests. |
| `v0.109.0` | Exact-revision ACME integrations for device certificate enrollment, including EST/BRSKI integration roles, challenge delegation, CSR attributes, trust boundaries, and operational flow. Promote to the published-RFC phase if it exits the RFC Editor queue first. | Revision-specific interop diagrams/fixtures, confused-deputy, delegated validation, wrong RA/CA, and enrollment recovery tests pass. |
| `v0.110.0` | Exact-revision identity-controlled validation draft with identity-provider trust/configuration, identifier binding, token/privacy policy, replay defense, and failure semantics. | Revision-specific issuer/audience/identifier/account binding, substitution, replay, privacy, and outage tests pass. |
| `v0.111.0` | Exact-revision post-quantum cryptographic-agility profile with negotiation, downgrade resistance, algorithm lifecycle, CSR/certificate binding, and migration policy. | Revision-specific agility/downgrade/hybrid/classical transition and interoperability tests pass. |

## Phase 10.5 - Release-To-Release Regression Freeze

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.119.0` | Previous-release compatibility gate for public Rust API, feature graph, wire semantics, serialized state/migrations, configuration, CLI machine output, and documented support claims. | The candidate is mechanically compared with the latest release and every retained historical corpus; all differences are either absent or linked to an approved requirement, migration, regression test, and release note. |
| `v0.119.1` | Final live-upstream drift assessment against the pinned RFC/IANA/profile/draft and provider-observation baselines. | A signed review classifies every live difference and assigns required work to a pre-1.0 version; no drift is silently accepted or allowed to make offline release tests non-reproducible. |

## Phase 11 - Production Qualification

| Version | Goal and required deliverables | Milestone-specific proof |
| --- | --- | --- |
| `v0.120.0-alpha.1` | Full API completeness and freeze candidate: no unversioned TODOs, no placeholder functions, complete docs/examples, stable error taxonomy, feature matrix, migration guide, and performance budgets. | Public API review, documentation tests, semver checks, zero uncovered public functions/branches under the documented coverage policy. |
| `v0.120.0-alpha.2` | Core interoperability matrix: in-house CA, Pebble, Boulder subset, RFC 8555, Web PKI extensions, staging providers, and controlled commercial provider. | Reproducible reports have no skipped mandatory core/Web PKI cases and classify provider limitations. |
| `v0.120.0-alpha.3` | Specialist RFC/draft and challenge-method interoperability matrix. | Every published extension and enabled exact draft revision has pinned reproducible evidence. |
| `v0.120.0-alpha.4` | Backend/store/deployer/web-server interoperability matrix across crypto, transports, DNS providers, databases, HSM/KMS, agents, and target profiles. | Pairwise profile reports have no unexplained skips or configuration-dependent backend choice. |
| `v0.120.0-beta.1` | Resource-exhaustion qualification: CPU/memory/body/header/identifier/queue budgets, backpressure, rate control, and bounded concurrency. | Load/soak tests meet published budgets without secret leakage or unbounded growth. |
| `v0.120.0-beta.2` | Resilience/disaster qualification: DNS/CA/KMS/database outages, partitions, clock faults, process/host loss, backup restore, and incident runbooks. | Chaos/restore exercises preserve safe state and serving certificate continuity. |
| `v0.120.0-beta.3` | Supply-chain and release qualification: cargo-vet policy, reproducible builds, signed provenance/tags, SBOM verification, tool/action freshness, packaging, and dependency freeze review. | Two clean builders reproduce artifacts; vet/deny/audit/provenance/SBOM gates pass. |
| `v0.120.0-rc.1` | Independent parser/JOSE/nonce/protocol-core security audit and remediation with attributable auditor identity and signed external attestation. | The signed attestation binds the reviewed implementation commit/tree and permanent regression tests; the tag passes the `v0.3.3` finalization-diff verifier. |
| `v0.120.0-rc.2` | Independent workflow/policy/PKIX/storage/deployment security audit and remediation with attributable auditor identity and signed external attestation. | The signed attestation binds the reviewed implementation commit/tree and permanent regression tests; the tag passes the finalization-diff verifier. |
| `v0.120.0-rc.3` | Independent crypto/FIPS/transport/DNS/TLS/KMS/agent/platform adapter audit and remediation with attributable auditor identity and signed external attestation. | The signed attestation binds the reviewed implementation commit/tree and permanent native/remote regressions; the tag passes the finalization-diff verifier. |
| `v0.120.0-rc.4` | FIPS production-profile qualification on supported environments, exact validated-module documentation, operator evidence bundle, and no-fallback verification. | Independent compliance review and automated runtime/configuration evidence precisely match validated boundaries. |
| `v0.120.0-rc.5` | Final release candidate: frozen APIs, complete manuals, upgrade/rollback rehearsal, all target profiles/RFC tiers/artifacts, and no known critical/high findings. | Full release gate twice from clean environments plus a signed independent pentest attestation bound to the reviewed implementation and a zero-unapproved-diff tagged release. |
| `v1.0.0` | First serious production-ready Vardheim release: complete RFC 8555, Web PKI tier, every published specialist ACME RFC, all planned runtime/crypto/FIPS/store/deployment/operations/platform boundaries, and isolated experimental drafts. | Final 1.0 audit and pentest PASS, all matrices green, reproducible signed artifacts, verified SBOM/provenance, and maintainer authorization to tag/publish. |

## Completeness Register

The following requirements are deliberately assigned and may not disappear
from the plan:

| Requirement | Assigned versions |
| --- | --- |
| `no_std`, executable `no_alloc`, optional native secret memory, Rust 1.90-1.97, machine portable/native crate tiers, feature powersets, OS portability and evidence tiers, future custom target | 0.1, 0.3.6, 0.4.6-0.4.7, 0.4.13, 0.4.24, 0.6.7, 0.52-0.55, 0.92-0.93 |
| Full errata text/applicability, registry-reference closure, typed-source requirement/work DAG, requirement database, upstream drift | 0.3.2, 0.3.4, 0.3.7-0.3.9 |
| Release evidence binding and reproducible SBOMs | 0.3.3, 0.3.5, 0.120.0-beta.3 |
| Pure reducer, sealed observation/evidence promotion, orthogonal effect observations, transient context-bound evidence, explicit static/alloc local executor boundaries, generic remote-executor prohibition and private real-provider/executor/store feasibility | 0.4.5, 0.4.8-0.4.12, 0.4.26, 0.6.1, 0.6.6, 0.7.2-0.7.3, 0.31.2-0.31.3, 0.32-0.38.5 |
| Provider-neutral digest/MAC, purpose-bound legacy hashing, signing, strict signature evidence, verification, entropy, security-sensitive identity issuance, key generation, public-key validation, domain-separated `BoundSigner`, local exact-request signer/MAC admission, asymmetric-key and symmetric-secret transactional onboarding, typed secret-content binding, narrow secret-binding bootstrap, orthogonal peer-effect outcomes/reconciliation, safe live-authority reconstruction, immutable signature/MAC dispatch, multi-axis verifier identity/assurance, mandatory returned-signature verification, independently verified versus provider-asserted versus cryptographically attested MAC evidence, and honest disposition/reconciliation | 0.10.2-0.10.23, 0.28.14, 0.33-0.34.1, 0.41.5, 0.45.1-0.45.3, 0.56-0.66.5, 0.76.0, 0.88.3, 0.91.3-0.91.6, 0.106.0 |
| Strict JSON, JOSE/JWK/JWA, composed EAB account creation, rollover, linear non-secret nonces and authenticated nonce admission/reservation | 0.4.14, 0.5-0.14.3, 0.15.2, 0.16.2-0.16.4, 0.33.5-0.33.6, 0.37.5 |
| Complete RFC 8555 operations including `newNonce`, existing-account adoption, and `newAuthz` | 0.14-0.25.2 |
| Method/specification/schema-aware challenge registry/receipts, HTTP-01, DNS-01, TLS-ALPN-01 | 0.20.4, 0.26-0.28, 0.42-0.51 |
| DER/ASN.1 feasibility/build decision, sequencing and structural closure, strict PKIX time/DN equality, certificate-envelope and public-key prevalidation, durable key-rotation modes, test crypto, issued trust snapshots/providers, dedicated PKIX/context evidence, CSR, full RFC 5280, chain-wide OCSP/CRL, distinct CT v1/v2 plus Merkle auditing, hardened public-PKI fetch/AIA, alternate chains | 0.4.25, 0.28.1-0.31.3, 0.52.4-0.52.7 |
| Durable workflow, evidence-safe snapshots, restored-store rollback discontinuity and external witness, stores, clock, crash recovery, staple-aware deployment, certificate adoption and renewal bootstrap | 0.32-0.39.5, 0.82-0.87 |
| Multi-issuer isolation/migration and explicit no-silent-failover policy | 0.19.3, 0.39.5 |
| Certificate-key/account-key rollover, compromise, retirement, honest disposition, inventory, status/Must-Staple response, lifecycle facade, and CT inclusion/split-view monitoring | 0.18-0.18.1, 0.41.2-0.41.12 |
| Public ACME/PKI/CT-audit transport, HTTP framing/privacy/cache/version/replay semantics, clock, and DNS/EDNS/TCP/Cookie/authoritative-discovery contracts/implementations plus shared adapter/composed-effect conformance | 0.14.2, 0.30.13-0.30.28, 0.37.4-0.38.4, 0.42.1-0.44.13, 0.52-0.55 |
| Complete local DNSSEC validation, authenticated denial, trust-anchor rollover, and resolver evidence policy | 0.44.0-0.44.13 |
| Historical evidence replay, prior-release comparison, state/API/platform/provider drift | 0.3.8-0.4.4, 0.4.14-0.4.26, 0.33.3, 0.33.7-0.33.8, 0.39.2, 0.69.3, 0.92.6, 0.96.4-0.97.3, 0.119.0-0.119.1 |
| ARI | 0.40-0.41 |
| RFC 2136/TSIG and production DNS update adapter, including transactional symmetric-secret onboarding/binding, narrow binding bootstrap, orthogonal peer-effect outcomes, durable reconciliation, and handle-backed MAC authority/evidence | 0.10.8, 0.10.16-0.10.21, 0.28.9, 0.33.5, 0.45.0-0.45.3, 0.56.10-0.56.12, 0.57.9-0.57.13, 0.61.2-0.66.5 |
| rustls/OpenSSL stapling, transactional PKCS#8/legacy-key and symmetric-secret onboarding, software-key/secret validation/binding/onboarding/reconstruction/immutable-dispatch/signature verification/MAC evidence/disposition, purpose-specific HMAC/RSA-PSS/legacy-hash/DNSSEC, ring, RustCrypto, aws-lc-rs, AWS-LC FIPS capability tables | 0.51, 0.56-0.60.1 |
| PKCS#11, TPM2, AWS KMS, Azure Key Vault, OpenBao, remote signer/MAC agent, provider-native asymmetric/symmetric transactional onboarding/reconstruction/immutable-dispatch/signature verification/MAC assertion-attestation evidence, and disposition reconciliation | 0.61-0.66.5 |
| CAA, IP, provider profiles and staging conformance | 0.67-0.71 |
| Every published specialist ACME RFC and registry extension | 0.72-0.81 |
| SQLite, PostgreSQL HA, tenancy, deployers, CLI, daemon, agents, platform key-store onboarding/reconstruction/immutable-dispatch/output-verification/disposition, PKCS#12 | 0.82-0.91.6 |
| Continuous Kani, Loom, fuzzing, differential parsing, eight independent state models, configuration-bound no-allocation/stack evidence, and mutation testing; final Miri/sanitizer/assurance closure | 0.4.1, 0.4.14-0.4.26, 0.5.2, parser/state/concurrency milestones, 0.94-0.97 |
| Shared adapter semantic conformance and hostile ACME fault server | 0.37.4, 0.38.0-0.38.2, every later adapter |
| All active/IESG and selected related draft families in the 2026-07-12 official snapshot | 0.100.0-0.111.0 |
| Production web-server replacement and regulated ACME acceptance | 0.51.2, 0.71.1-0.71.2 |
| API freeze, interoperability, resilience, supply chain, audits, RCs | 0.120 series |

There is no “post-1.0 someday” bucket in this plan. Newly discovered required
work must receive a new pre-1.0 version or split an existing milestone.
